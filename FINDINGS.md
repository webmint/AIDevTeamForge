# Findings — file-less, ownerless

Relocated **verbatim** from `CLAUDE.md` `## Active work` on 2026-08-17. Each entry below is a FINDING with **no plan file and no owner** — do not grep for a `<NN>-*-PLAN.md` for any of them. The entries are reproduced byte-for-byte, including their internal positional references ("above", "in this list", ordinal counts like "the THIRD file-less finding") — those refer to their ORIGINAL positions in the CLAUDE.md Active-work list, whose full plan entries now live in `PLAN-STATUS-ARCHIVE.md` (placeholders there mark where each finding sat). The deliberately-unedited stale ordinal counts inside the entries stay unedited here; do not "fix" them.

If a finding is ever drafted into a plan, it takes the normal `<NN>-<TOPIC>-PLAN.md` shape — replace its entry here with a pointer and add the plan to the ledger in `PLAN-STATUS-ARCHIVE.md` + the CLAUDE.md index. (Note: `81-INFERENCE-RULES-PLAN.md` D4 PROPOSES its F3 take ownership of finding 4 — unratified as of 2026-08-17; finding 4 stays ownerless until that plan's Phase 0 ratifies.) (2026-08-18: that Phase 0 HAS ratified and the F3 fix shipped — finding 4 is now OWNED; per plan 81 Phase 6 its entry below is amended in place, not replaced.)

---

## 1. The §3.6 Narrowing FALSE BINARY (escalated: prefers the empirically failing shape) — original position: between plans 73 and 74

- **FINDING (no plan file, no owner) — the constitution's §3.6 Narrowing rule may present a FALSE BINARY. Maintainer decision, explicitly NOT settled.** Recorded 2026-08-12 inside `73-LITERAL-ARCHAEOLOGY-EVIDENCE-ARM-PLAN.md`'s `## External benchmark evidence` section, but it is **not plan 73's subject and no plan owns it** — surfaced here so it is findable from the index rather than only from inside another plan's evidence appendix. `src/constitution.md` §3.6's `**Narrowing (restricting shared-code behavior):**` block (plan 66 WI-2, extended by plan 69 WI-C) names exactly TWO forms for restricting shared-code behavior — a **caller-scoped opt-in** ("a parameter, option, or wrapper the affected caller passes", `:122`) and a **layer-wide policy change** inside the shared code (`:124`) — and correctly prefers the former. A THIRD form is not named as a first-class option: **deriving the restricting condition INSIDE the shared function from arguments it already receives.** That is precisely the form that covers callers nobody enumerated, and enumeration-completeness is the failure mode the surrounding rules spend their words on. In the current text it surfaces only inside the fallback arm at `:123` ("a layer-wide change keyed on data every caller already passes"), reachable **only** when the needing-caller set cannot be fully established, and it then inherits `:124`'s obligation to name every current caller — so the rule structurally files the enumeration-independent form under the disfavored branch. The preference propagates into the consumer-facing surfaces that carry it: `src/commands/plan/main.md` (architect sub-question 7 + its echo sites) and `src/agents/architect.md` (Rule 9). *(Verified 2026-08-12: `grep -rl "caller-scoped opt-in" src/` → exactly 3 files — the constitution plus those two.)* **This is the only finding from that session that could be ACTIVELY PRODUCING worse designs rather than merely failing to catch them** — every other open item is a detection gap. Deciding it means deciding whether to name intrinsic derivation as a third form and, if so, reconciling `:123`'s fallback framing, `:124`'s naming obligation, and plan 66's anti-KISS tie-breaker at `:125`. **Nothing has been changed. Do not read this entry as a decision.** **ESCALATED 2026-08-13 by external benchmark evidence — the finding is now stronger than "may present a false binary", and the earlier sentences above are deliberately left in their original hedged wording.** The 16-run v1 benchmark handoff behind plan 77 (see that entry; the raw file is untracked private-client evidence, never to be committed or quoted into a tracked file) reports that **guard shape alone predicted the probe outcome 17/17**, that **15 of the 16 runs added a caller-side opt-out parameter** — structurally the `:122` form §3.6 PREFERS — and that the sole run to pass the handling probe did NOT take that shape: it derived the restricting condition inside the shared function from a state value the function already receives and fixed the hidden surface with ZERO edits to that surface's file, which is exactly the enumeration-independent third form §3.6 relegates to the `:123` fallback arm. With one pass across 16 runs and that pass not among the 15, the preferred form is 0-for-15 on this ticket class while the disfavored form is 1-for-1. That upgrades the claim from "may present a false binary" to "**prefers the empirically failing shape**". **The same dataset simultaneously argues against the obvious fix:** its null results — artifact volume 24× and test volume 14× with zero effect on the probe, investigation depth inverting the relationship — are direct evidence that MORE PROSE does not move the outcome, which supports plan 77's OQ-4 stance that a prose-only constitution change would not repair this. **Maintainer decision 2026-08-13:** `src/constitution.md` is deliberately NOT edited before plan 77's Phase-1 blind v2 baseline runs — a `src/` edit would contaminate the very baseline that plan's D5 exists to protect — and plan 77's OQ-4 / Phase-3 outcome is the designated resolution path (a passing probe MOOTS this finding rather than answering it; a fired delete branch leaves it open and untouched). **Still NOT settled. Nothing has been changed, and this escalation is not a decision to change it.** **AMENDED 2026-08-15 — that designated resolution path NO LONGER EXISTS.** Plan 77's Phases 1 and 3 were **WAIVED** (waived, not deferred — see that entry), so the blind baseline will not run and the handling probe will never be read: neither the MOOT branch nor the delete branch can fire, and plan 77's ratified OQ-4 decides only that THAT plan makes no constitution change, which resolves nothing here. **The stated reason for the hold on `src/constitution.md` — contaminating a baseline — is therefore gone, and whether the hold itself stands is UNDECIDED.** This finding is **untouched, still open, still ownerless, and now without any designated route**; nothing has been changed and this amendment is not a decision to change it. **AMENDED 2026-08-17 (post-relocation):** `81-INFERENCE-RULES-PLAN.md` D5 names its F1 rule as a CANDIDATE resolution route for this finding — IF ratified at that plan's Phase 0 AND validated at its Phase 7; unratified as of 2026-08-17, so the "without any designated route" statement above STANDS until then. Two things that candidate route carries, recorded with it. **First, the nuance from the v2 benchmark run plan 81 is built on, and it cuts AGAINST a constitution edit rather than for one:** as that plan's D5 records, the run's impossibility analysis considered the enumeration-independent intrinsic form FIRST and rejected it — on the authority of an acceptance criterion protecting a state no reachable construction site produces, **not** on §3.6's stated preference. Removing the preference would not have changed that rejection. That is why plan 81 makes **NO `src/constitution.md` edit** and offers F1 as the mechanical TRIGGER this finding has lacked — an identical REACHABLE argument tuple presented by two call sites is the condition under which the intrinsic third form is the only form that can work — rather than as a rewrite of the rule; so this run is evidence that a constitution edit ALONE would not repair the finding, which is the same direction the null results recorded above already point. **Second, the ESCALATED reading above is untouched by this:** F1 answers none of it, and even a ratified-and-validated F1 would give this finding a route to be DECIDED on, never a decision. **Nothing has been changed. This finding is still open, still ownerless, and this amendment is not a decision to change it.** **AMENDED 2026-08-18: plan 81's Phase 0 RATIFIED and its Phase 2 SHIPPED the F1 rule (`9a3300b`, widened in place by Phase 3 `af16cb3` — plan Rule 5 note + architect Rule 9), so the candidate route's FIRST condition is met. Its SECOND condition — Phase-7 validation (the frozen-prompt re-run, user-driven, model pinned) — has NOT run, so per the 2026-08-17 amendment's own terms the route remains a CANDIDATE and the "without any designated route" statement above STANDS unchanged until Phase 7 lands. Even a validated F1 gives this finding a route to be DECIDED on, never a decision. Still open, still ownerless; nothing here changes the constitution (plan 81 D5, ratified: no §3.6 edit).**

---

## 2. "76" — the intake rubrics' blanket `--accept-gaps` escape — original position: between plan 75 and finding 3

- **76 — UNDRAFTED FINDING: the intake rubrics' blanket escape flag. NO `76-*-PLAN.md` FILE EXISTS at repo root — do not grep for one.** Identified 2026-08-12; recorded here because nothing else records it. **The inconsistency:** both intake commands expose a single blanket escape on their rubric-finalize verb — `research_helper symptom-finalize --accept-gaps` (`src/devforge/lib/_research/_cli.py:385`; logic in `_research/_cmds_phase0.py:279,289`) and `discover_helper scope-finalize --accept-gaps` (`_discover/_cli.py:319`; logic in `_discover/_cmds_scope.py:238-241`) — which is a bare `action="store_true"` boolean that waves through **every** Partial/Missing rubric dimension at once and records a single `memo.override_recorded = True`, with **no per-dimension reason captured anywhere**. **The same helper package already contains two escapes of the correct shape**, both requiring stated prose: `record-no-shared-callers-justification --justification` (`_research/_cli.py:728-743`, the check-8 escape from plan 67) and `set-recommended-approach --single-layer-justification` (`_cli.py:596`, check 13's). **And the specification command sets the stricter precedent** — `/specify` requires a PER-ITEM disposition for every unresolved decision point via `set-dp-deferral --dp-id --deferral-kind --reason`, with `--reason` **required** (`src/devforge/lib/_specify/_cli.py:223-238`). **Recommended fix:** make `--accept-gaps` require a per-dimension justification, mirroring the justified-escape shape the same package already uses — the helper owns the shape, the LLM fills the values. **Worth knowing before scoping it:** on the discover side `--accept-gaps` is not only a rubric wave-through — it is the **sole sanctioned override** of that command's verdict-flip invariant (`discover/main.md:550`, `:592`: an unfavorable fit or major-refactor estimate MUST force a `Reconsider` verdict *unless* `memo.override_recorded == True`, "set only by `scope-finalize --accept-gaps`"), so its blast radius there is strictly larger than on the research side and a change must not silently weaken that closed override-set. **⚠ THE ORIGINAL FRAMING WAS WITHDRAWN — do not re-attach it.** This was first written up as "the escape is what caused the observed miss." That claim does **not** hold: the arm that produced the better result **has no such rubric at all**, so a blanket escape cannot explain a difference between a command that has it and one that has nothing like it. It stands as an **internal inconsistency worth fixing**, NOT as a diagnosed cause — anyone reviving it must carry the correction, not the original diagnosis. **NOTE ON THIS ENTRY'S SHAPE — a precedent this list did not previously set:** every other item above is backed by a `<TOPIC>-PLAN.md` file at repo root, per the convention below. This one and the FALSE BINARY item above are **findings with no plan file**; they are marked as such in their first sentence so no session searches for a file that does not exist. If either is drafted, it takes the normal `<NN>-<TOPIC>-PLAN.md` shape and this entry is replaced by a plan entry.

---

## 3. Post-plan-63 catalog trim — `/plan`'s constitution-backed obligations have no always-on presence — original position: between finding 2 and plan 77

- **FINDING (no plan file, no owner) — NO `<NN>-*-PLAN.md` FILE EXISTS for this; do not grep for one. After plan 63's catalog trim, the per-obligation `/plan` mechanics that plans 36, 53, 66 and 71 had accumulated in the consumer overlay have NO always-on presence in a consumer install.** Recorded 2026-08-12, surfaced while correcting plan 69's WI-C/D3 record (the `src/CLAUDE.md:85` Narrowing one-liner those decisions name is gone — plan 63's `0a32009` replaced the entire `/plan` Command-Details paragraph with an argument-syntax stub). **The sweeping form of this finding — "no obligation from plans 36/37/53/65/66/71 has any always-on presence" — does NOT hold; it was checked by grep over the current `src/CLAUDE.md` and falsified. Do not propagate it; propagate the narrower statement below.** **SURVIVING always-on:** `/plan`'s approval gate (its `#### /devforge:plan` stub still carries "Requires approval before `/devforge:breakdown`."), and — in the Spec-Driven Development Flow one-liner list — plan 37's WIP-commit behavior plus plan 65's spec Draft→Approved flip; plan 53's design anchor and plan 36's re-entry seed also survive in that Flow list, but attached to `/specify` and `/grill`, the PRODUCERS, never to `/plan`'s consumption of them. **GONE from always-on text entirely, anywhere in the file:** plan 66's §3.6 Narrowing completeness condition (as conditioned by plan 69 WI-C), plan 66's pure-builder targets, plan 71's change-induced dead code, plan 53's `/plan`-side anchor read, and plan 36's `/plan`-side REVISE-PLAN re-entry consumption — `grep -in "Narrowing\|caller-scoped\|pure.builder\|dead.code" src/CLAUDE.md` returns one irrelevant hit, the CBM structural-query line. Each now surfaces ONLY when `/plan`'s command body loads on invocation. **This is an accepted, architecturally-coherent tradeoff, not a defect** — plan 63's own rationale is that a model-invocable command's `description` frontmatter IS in model context, making the catalog's per-command mechanics redundant as awareness; and the maintainer ratified the `src/CLAUDE.md` Narrowing removal as superseded (2026-08-12) rather than restoring the line. **The part that may nonetheless deserve a decision:** a constitution-backed obligation now has zero standing presence in model context. What remains always-on for the constitution is a POINTER, not content — `**Constitution is law**` in the overlay's `### Always` list, a per-task "read `constitution.md`" pre-flight line, and the `## References` link `[Constitution](constitution.md) — Project rules and patterns` — and none of the three names any specific obligation (`grep -n "constitution\.md" src/CLAUDE.md` returns 6 hits; the other three are incidental mentions, not pointers — `/devforge:grill`'s adversary-reads list, `/devforge:constitute`'s output description, and a wrapper-mode location note). Whether a pointer suffices for rules the framework calls law is UNDECIDED. The §3.6 Narrowing rule is the concrete instance, which makes this the REACH counterpart to the FALSE BINARY finding above: that entry questions the rule's CONTENT, this one questions whether the rule is ever in context to be applied. **Nothing has been changed and nothing has been decided. This is a finding, not a plan and not a defect — do not read it as a decision, and do not restore trimmed catalog text on the strength of it.** **NOTE ON THIS ENTRY'S SHAPE:** this is the THIRD file-less finding in this list; the entry immediately above enumerates the first two and its "this one and the FALSE BINARY item above" wording predates this entry. Like the FALSE BINARY entry, this one takes NO plan number, deliberately — numbering it would reserve an `<NN>` slot for a plan nobody has decided to write. If it is drafted, it takes the normal `<NN>-<TOPIC>-PLAN.md` shape and this entry is replaced by a plan entry.

---

## 4. An AC that pins a value the same spec REMOVES — a conflict shape nothing refuses — original position: between plan 81 and finding 5

- **FINDING (no plan file, no owner) — an AC that pins a value the same spec REMOVES is a conflict flag, not a regression pin, and nothing in the pipeline refuses one. NO `<NN>-*-PLAN.md` FILE EXISTS for this; do not grep for one.** Recorded 2026-08-13 from the v1 benchmark handoff behind plan 77 (`77-EVIDENCE-DISCOVERY-TO-LOCK-INVERSION.md` at repo root — UNTRACKED private-client evidence, never to be committed or quoted into a tracked file). **The shape:** an acceptance criterion of the form "path P still contains / still emits value F" written into a spec that REMOVES F elsewhere. In the v1 evidence such ACs were not written from product intent at all — they were written from INFERRED intent ("we did not intend to touch this file") — and they became the single strongest lock artifact in the run, because the next stage pins them with a passing regression test and the defect is thereafter defended by the suite rather than merely present in it. **The natural mechanical home is `/spec-check` (plan 62):** the shape is exactly ACs-contradicting-each-other, which Z3 catches whenever the formalizer binds the removal AC and the pin AC to shared variables, and it needs no new solver capability. **But `/spec-check` is opt-in and ADVISORY by ratified D14** — blocking belongs to the deterministic gates, and that plan's entry above explicitly warns a future session not to strengthen it — **so a MANDATORY refusal of this shape has no owner today**: routing it to `/spec-check` buys detection for the specs that opt in and nothing at all for the specs whose author was confident, which are the ones that produce this AC. Plan 77's R3 covers only the instruction-side half (a `/specify` directive requiring quoted product intent for such an AC), which is prose and therefore skippable. The cheap complementary detector — grep the pipeline's own artifacts for assertions that a removed value is still present — is likewise unowned; plan 77's R2 uses that same grep as a probe SIGNATURE, which is a measurement, not a gate, and must not be mistaken for one. **Nothing has been changed and nothing has been decided; this is a finding, not a plan and not a defect.** **AMENDED 2026-08-17 — this finding now has a PROPOSED OWNER: `81-INFERENCE-RULES-PLAN.md`'s F3 (preservation-AC admission), via that plan's D4. Ownership is PROPOSED, not settled — plan 81 is NOT STARTED and its Phase-0 ratification has not run, so the "nothing has been decided" sentence above stays true, and this entry is NOT yet replaced by a plan entry.** Two things the amendment adds. **First, a sibling shape of this class was reproduced on v2** — not the pins-a-removed-value form recorded above but an AC over a state no reachable construction site produces (an unfalsifiable guard rather than a conflict flag); D4's claim is that both are answered by ONE admission rule requiring the subject state's reachability to be shown before an AC enters as behavior preservation. **Second, and this is the part that bears on the "prose is skippable" argument above: plan 77's R3 `/devforge:specify` directive was PRESENT, correct, and shipped (`src/commands/specify/main.md:619`, naming the "we did not intend to touch that file" trap by name) — and the AC was written anyway**, almost certainly because it was framed as lane-behavior preservation rather than value presence and so never matched the directive's lexical trigger. F3's answer is to re-key the trigger on the AC's declared ROLE (the shipped `add-ac --subsection behavior_preservation` key) rather than on its phrasing, which narrows the dodge surface without closing it (plan 81's OQ-2 records that a prose trigger cannot be made non-dodgeable, and OQ-1 recommends NO mechanical detector for v1). **The `/devforge:spec-check` observation above is unchanged and plan 81 proposes no change to that command** — its opt-in ADVISORY D14 stance stands. See the `81-INFERENCE-RULES-PLAN.md` entry above for the full chain. **AMENDED 2026-08-18 — OWNERSHIP RATIFIED AND THE FIX SHIPPED.** Plan 81's Phase 0 ratified D4 (F3 owns this finding) and its Phase 4 shipped the F3 directive (`59b5152`, `src/commands/specify/main.md` Step 4.4): every AC entering §5.2 Behavior preservation — keyed on the declared `behavior_preservation` ROLE, however phrased, matrix or no matrix — must cite the construction site producing its subject state, traced via the CBM chain; an uncitable subject state routes to §8 Open Questions + §9 Risks instead of entering as an AC. The ratified residual stands as recorded: the class moved from unowned-and-prose to OWNED-and-prose — the role key narrows the dodge surface without closing it (an author can still file under another subsection), and OQ-1 ratified NO mechanical detector for v1 (prose-first, plan-66 precedent), revisitable as a separate plan on empirical miss-rate. Per plan 81 Phase 6 this entry is amended in place, NOT replaced by a plan pointer. Consumer validation pending: plan 81 Phase 7 has not run. **AMENDED 2026-08-19 — the "a MANDATORY refusal of this shape has no owner today" clause now HAS an owner on the formalization side, and the split is stated plainly: F3 = admission at AUTHORING (plan 81, shipped 2026-08-18); `82-SPEC-CHECK-SUBJECT-RESOLUTION-MANDATORY-PLAN.md` = refusal at FORMALIZATION (Phases 1–6 shipped 2026-08-19).** Two of plan 82's changes bear on that clause. **First, the mechanical counterpart:** the `spec-formalizer` now resolves every IR variable's SUBJECT against the codebase BEFORE formalizing — a construction site in the existing code, or, for a state this feature introduces, the spec's own new-behavior declaration, with preservation ACs restricted to the code arm — and an AC whose subject resolves through neither is `unresolved_subject`: not formalized into constraints, counted as neither `formalized` nor a skip, named in an `## UNRESOLVED SUBJECTS` section of the report, and on its own enough to stop the clean verdict and put the question to the human. **Second, the opt-in premise the quoted clause rested on is gone:** plan 62's D14 was amended in place the same day — `/devforge:spec-check` is run-mandatory, since `/devforge:plan` blocks until a fresh `spec-check.md` exists for the resolved spec. **The narrow reading travels with that:** what is mandatory is that the check RAN and its report is FRESH; **the verdict never binds**, so the human still owns every non-clean disposition. **The bound, stated plainly and never to be dropped when this is cited:** subject resolution directly catches the unreachable-subject SIBLING, while THIS entry's own shape — an AC pinning a value the same spec removes — is caught by Z3 only when the formalizer binds the removal AC and the pin AC to SHARED variables. Code-grounded resolution makes that shared binding MORE LIKELY (both subjects resolving to the same construction site pulls toward one variable) and **does not guarantee it**. So this finding is NARROWED, not closed, and neither plan may be written as closing it. **Consumer validation is pending on both halves** — plan 81's Phase 7 and plan 82's Phase 7 are each deferred user-driven HARD GATES, so neither the authoring rule nor the formalization refusal has been observed to work on a real run. Per the convention plan 81 set here, this entry is amended in place, not replaced by a plan pointer, and no finding is renumbered. **NOTE ON THIS ENTRY'S SHAPE:** this is the FOURTH file-less finding in this list. The ordinal counts inside the earlier file-less entries — the `76` entry's "this one and the FALSE BINARY item above" and the catalog-trim entry's "this is the THIRD file-less finding" — were written before this entry existed and were deliberately left unedited; do not "fix" them. Like those, this one takes NO plan number, because numbering it would reserve an `<NN>` slot for a plan nobody has decided to write; if it is drafted it takes the normal `<NN>-<TOPIC>-PLAN.md` shape and this entry is replaced by a plan entry.

---

## 5. A hard constraint that eliminates a candidate discriminator records no cost note — original position: final entry of the list

- **FINDING (no plan file, no owner) — a hard constraint that eliminates a candidate discriminator is not required to record what that candidate would have achieved, so the winning design can be excluded invisibly. NO `<NN>-*-PLAN.md` FILE EXISTS for this; do not grep for one.** Recorded 2026-08-13 from the same handoff (`77-EVIDENCE-DISCOVERY-TO-LOCK-INVERSION.md` — UNTRACKED private-client evidence, never to be committed or quoted into a tracked file). **The observation:** two independent v1 runs each wrote a hard spec constraint FORBIDDING the one discriminator that covered both affected surfaces, and neither recorded what that discriminator would have achieved — so the constraint reads as ordinary scope hygiene and the eliminated candidate leaves no trace for a reviewer to weigh. **Proposed rule shape:** any hard constraint that eliminates a candidate discriminator must state what that candidate would have achieved. **The caution is the load-bearing half of this entry and must be carried by any session that drafts it:** the naive form of that rule likely FAILS plan 77's own visibility bar — *does the rule produce an artifact that is visibly wrong when the analysis wasn't done?* — because an absent or perfunctory cost note does not read wrong on its face, which is the same defect for which that plan records "enumerate the consumers" and a completeness count as REJECTED candidates. A drafting session must design against that bar rather than ship the naive rule, and must not fold this into plan 77's Phase 2: that plan's named risk is phase accumulation destroying its own measurement, and its Phase 2 is measurement-locked to a fixed instruction-only file set (R1's re-scope already changed which files those are, so no count is restated here — go to the plan for the current set). **(AMENDED 2026-08-15: plan 77's Phase 2 has SHIPPED and its measurement arms, Phases 1 and 3, were WAIVED, so the measurement-lock rationale just given is DEAD — do not read it as a live constraint. The do-not-fold instruction stands on the different ground that the phase is closed; the visibility-bar caution above is a criterion rather than a phase and is unaffected.)** **No owner. Nothing has been changed and nothing has been decided.** **NOTE ON THIS ENTRY'S SHAPE:** this is the FIFTH file-less finding in this list and the second added on 2026-08-13; the entry immediately above carries the count-staleness note that covers both. It takes NO plan number for the same reason given there; if it is drafted it takes the normal `<NN>-<TOPIC>-PLAN.md` shape and this entry is replaced by a plan entry.
