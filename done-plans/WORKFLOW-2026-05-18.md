# WORKFLOW-2026-05-18

**Date**: 2026-05-18 (handoff drafted 2026-05-17)
**Branch**: `develop-2.0-init` (HEAD: `5eaa704` feat(research): Patch 4)
**Owner**: next Claude instance + user
**Status**: Execution playbook — read top-to-bottom before touching code.

## Purpose

Coordinate execution across four open plans surfaced 2026-05-17. Plans were drafted in three separate sessions; cross-checked here for dependencies + conflict. This doc is the **canonical execution order** + handoff brief for tomorrow.

## Plan inventory (state at draft time)

| Plan | Status | Owner | First step |
|---|---|---|---|
| [RESEARCH-FRAMING-REGRESSION-PLAN.md](RESEARCH-FRAMING-REGRESSION-PLAN.md) | V1 SHIPPED (Patches 1-5: `832aa4e` / `b8e6098` / `c8a9ca3` / `5eaa704` / `4a6a519`); EMPIRICAL VERIFY FAILED 2026-05-17 (1.5/3 vs 2/3 baseline) | — | superseded by V2 |
| [RESEARCH-FRAMING-REGRESSION-PLAN-V2.md](RESEARCH-FRAMING-REGRESSION-PLAN-V2.md) | **V2 SHIPPED + EMPIRICALLY VERIFIED 3/3 on 2026-05-18** — Gap 6 + Gap 7 closed (adapter-trace + id-stability axis) | — | superseded by V3 |
| [RESEARCH-FRAMING-REGRESSION-PLAN-V3.md](RESEARCH-FRAMING-REGRESSION-PLAN-V3.md) | **IN-FLIGHT 2026-05-18** — Gap 8 (literal-archaeology) + Gap 9 (argument-duplication shape-check); hardened against 4 pre-commit refinements (R1 6-value enum lock / R2 literal-token regex JS+Python+hex+BigInt+scientific+backtick / R3 optional chaining in identifier regex / R4 multi-refactor anchor edge deferred to /audit) | python-engineer + instruction-author | save `feedback_literal_archaeology_and_argument_duplication.md` to memory → Patch 8 (literal-archaeology gate) → Patch 9 (argument-duplication shape-check) |
| [PLAN-COMMAND-REDESIGN-PLAN.md](PLAN-COMMAND-REDESIGN-PLAN.md) | IN-FLIGHT — Steps 1-5 landed; Step 6 parity 4-run gate OPEN | user (4-run gate is user-driven) | run 4 fresh testForge20 sessions per Step 6 procedure |
| [CONSTITUTION-DRIFT-DETECTOR-PLAN.md](CONSTITUTION-DRIFT-DETECTOR-PLAN.md) | DRAFTED 2026-05-17 | python-engineer | Step 1 — `_parse_universal_blocks` |
| [COMMAND-VERIFY-GATES-PLAN.md](COMMAND-VERIFY-GATES-PLAN.md) | DRAFTED 2026-05-17 — 3 independent steps | python-engineer | Step 1 — `generate_docs_helper verify-all` |
| [THREE-LAYER-SEPARATION-PLAN.md](THREE-LAYER-SEPARATION-PLAN.md) | DRAFTED 2026-05-17 — Gaps A→B→C→E sequenced; Gap D deferred | python-engineer + instruction-author | Gap A Step A.1 — `specify_helper record-constraint` kind split |
| [RESEARCH-HANDOFF-PLAN.md](RESEARCH-HANDOFF-PLAN.md) | DRAFTED 2026-05-17 — POST-18-MAY (gates on 18-May patches landing) + POST-V2 (schema must absorb V2 fields) | python-engineer + instruction-author | Step 1 — lock `handoff.json` schema |

Closed sister plans (do NOT re-execute; cross-ref only):

- [CONSTITUTION-STRENGTHENING-PLAN.md](CONSTITUTION-STRENGTHENING-PLAN.md) — 6 patches applied 2026-05-16
- [RESEARCH-HELPER-API-ENUM-PLAN.md](RESEARCH-HELPER-API-ENUM-PLAN.md) — Patches 1-4 shipped (commits `832aa4e` … `5eaa704`)

## Dependency graph (verified 2026-05-17, updated 2026-05-18 for V2 + V3)

```
RESEARCH-FRAMING-REGRESSION-PLAN-V2  (SHIPPED + VERIFIED 3/3 on 2026-05-18)
  ├─ Patch 6: Phase 2.4d data-flow chain trace ✓
  ├─ Patch 7: Phase 2.5 id-stability hypothesis axis ✓
  └─ EMPIRICAL: catches strataFamilyToItemAdapters.ts:5 Math.random ✓
                │
                └─ V3 stacks on top (necessary-not-sufficient — splitOnSNA-class failures remain)

RESEARCH-FRAMING-REGRESSION-PLAN-V3  (IN-FLIGHT 2026-05-18)
  ├─ PRE-FLIGHT: save feedback_literal_archaeology_and_argument_duplication.md to memory
  ├─ Patch 8: literal-archaeology gate (record-literal-archaeology + check 17 + Phase 2.5b)
  ├─ Patch 9: argument-duplication shape-check (--proposed-call-shape + check 18)
  └─ EMPIRICAL VERIFY: fresh /research on testForge20 splitOnSNA topic
     MUST propose wrapper-default S3 in turn 1 (not turn 3)
     AND record literal_archaeology entry with intent=inherited-refactor
     │
     ├─ HARD-BLOCKS → 18 May patches  (research output quality must survive verify
     │                                  before downstream /specify hardening propagates
     │                                  potentially-wrong recipe more reliably)
     │
     └─ feeds → RESEARCH-HANDOFF-PLAN Step 1 schema  (handoff.json must absorb
                                                       V2 fields: data_flow_chain + trace_mode
                                                       + value_semantics.stable_across_calls
                                                       + value_production_sites
                                                       AND V3 fields: literal_archaeology[]
                                                       + plan_seeds.proposed_call_shape)

PLAN-COMMAND-REDESIGN-PLAN
  Step 6 parity gate (user-driven, async)
  └─ extended by → THREE-LAYER Gap B + C  (after parity gate clears)

THREE-LAYER Gap A  (specify_helper API change — REMOVES --kind=use)
  ├─ blocks → THREE-LAYER Gap B + C  (Phase 1.5 findings depend on new kind buckets)
  └─ blocks → COMMAND-VERIFY-GATES Step 3  (verify-rendered fixtures use kind enum)

THREE-LAYER Gap E  (cbm_sync_helper per-spec stamp)
  └─ independent — can land any time

CONSTITUTION-DRIFT-DETECTOR  (constitute_helper forge-internal verb)
  └─ independent — complements THREE-LAYER Gap A (maintainer-side defense)

COMMAND-VERIFY-GATES Step 1  (generate_docs verify-all)  → independent
COMMAND-VERIFY-GATES Step 2  (init_helper verify)        → independent
COMMAND-VERIFY-GATES Step 3  (specify verify-rendered)   → BLOCKED on THREE-LAYER Gap A
```

### V2 + V3 hard-gate rationale

**V2 status (2026-05-18)**: SHIPPED + EMPIRICALLY VERIFIED 3/3 on Strata duplicate-options. Adapter Math.random caught across 3 state surfaces. Gaps 6+7 closed. V2 hard-gate CLEARED.

**V3 status (2026-05-18)**: IN-FLIGHT. Addresses splitOnSNA-class failures surfaced after V2 verified — recipe-quality (literal-archaeology + argument-duplication shape-check). V3 acceptance criterion: fresh `/research` on testForge20 splitOnSNA topic proposes wrapper-default S3 in **turn 1** (not turn 3) AND records `literal_archaeology` entry with `intent=inherited-refactor`.

**Block 18 May patches from starting until V3 empirical verify passes.** 18 May patches ship `/specify` §7 kind taxonomy + `verify-rendered` + per-spec drift stamp — all DOWNSTREAM of `/research` output. If V3 verify fails (recipe-quality still poor on literal-replacement bugs), 18 May hardening propagates wrong recipe with full audit-trail legitimacy.

If V3 verify fails → draft V4 against new gap before 18 May. Do not ship `/specify` hardening over broken `/research` recipe substrate.

**Pattern note**: each Vn closes one class of instinct-failure; expect Vn+1 to surface another. Current cycle = V1 (structural framing) → V2 (data-flow + stability) → V3 (literal-history + argument-shape). After V4+ if research_helper bloats past LLM-comprehensible (~300-500 lines), revisit architecture. Not blocking V3.

## Risk mitigations folded in (2026-05-17 audit)

Five risks surfaced during pre-execution audit. Two fully avoidable (patched into plans below); three partial (assumption-disclosure in plans).

| Risk | Where mitigated | Status |
|---|---|---|
| 1 — `verify-rendered` byte-brittle | COMMAND-VERIFY-GATES Step 3 — canonical-form normalization + render-determinism precondition test | PATCHED |
| 2 — `--quantifier "high"` vague-adjective loophole | THREE-LAYER Gap A Step A.1.2 — regex + blocklist validator | PATCHED |
| 3 — `constitution_anchor` validates location not body | THREE-LAYER Gap A Step A.2.3.b — assumption disclosure + DRIFT-DETECTOR cross-defense | DISCLOSED |
| 4 — §4 incomplete = invisible drift | THREE-LAYER Gap E Step E.1.3 — assumption disclosure; Stage 2 deferred | DISCLOSED |
| 5 — 3 kinds = parity variance | THREE-LAYER Gap B Step B.3.2 — re-baseline 5%→7% ceiling acceptable | DEFERRED |

**Read before executing Phase 2.A + 3.A**: briefs below reference these mitigations. Do NOT ship verify-rendered with raw byte-identity; do NOT accept `--quantifier "high"` shape; both will be rejected at review.

## Conflict findings (resolved)

### Conflict 1 — both plans touch `specify_helper`

- THREE-LAYER Gap A: replaces `--kind=use` with `nfr` / `constitution_anchor` / `external_system`.
- COMMAND-VERIFY-GATES Step 3: adds `verify-rendered` subcommand; tests round-trip through real `render`.

**Resolution**: Gap A FIRST. Step 3 test fixtures use new kind enum. If Step 3 lands first, its fixtures break when Gap A removes `--kind=use`. Order locked in execution sequence below.

### Conflict 2 — both plans defend against constitution drift

- CONSTITUTION-DRIFT-DETECTOR: maintainer-side diff between consumer state JSON and canonical `src/constitution.md`.
- THREE-LAYER Gap A `constitution_anchor` kind: consumer-side validation against rendered `<install_root>/constitution.md`.

**Resolution**: complement, not conflict. Maintainer + consumer = two layers same defense. Land in parallel.

### Conflict 3 — both plans touch CBM stamp helper-adjacent territory

- THREE-LAYER Gap E: extends `cbm_sync_helper` with per-spec stamping + `check-spec`.
- No other plan touches `cbm_sync_helper` for 18-May scope.

**Resolution**: no conflict.

## Execution order — 2026-05-18

Land in this sequence. **Each step has an explicit `STOP` checkpoint** — the orchestrator does not advance past a `STOP` until verification passes.

### Phase 0 — Pre-flight (10 min)

0.1 Read this doc top-to-bottom.

0.2 Run state checks:

```bash
cd /Users/mykolakudlyk/Projects/ai-dev-team-forge
git status                                                    # confirm clean / known dirty
git log -15 --oneline                                         # last 15 commits

# V2 (RESEARCH-FRAMING-REGRESSION-PLAN-V2) — SHIPPED + VERIFIED 3/3 on 2026-05-18
grep -nE 'record-data-flow-chain' src/devforge/lib/research_helper.py 2>/dev/null         # V2 Patch 6
grep -nE 'record-value-production-site' src/devforge/lib/research_helper.py 2>/dev/null   # V2 Patch 7
grep -nE 'stable_across_calls' src/devforge/lib/research_helper.py 2>/dev/null            # V2 Patch 7 axis
grep -nE 'Phase 2\.4d' src/commands/research/main.md 2>/dev/null                          # V2 Patch 6 spec text
grep -E 'V2 applied \+ empirically verified \(3/3' RESEARCH-FRAMING-REGRESSION-PLAN-V2.md  # V2 verify status

# V3 (RESEARCH-FRAMING-REGRESSION-PLAN-V3) — BLOCKS 18 May patches
grep -nE 'record-literal-archaeology' src/devforge/lib/research_helper.py 2>/dev/null     # V3 Patch 8
grep -nE 'proposed_call_shape|--proposed-call-shape' src/devforge/lib/research_helper.py 2>/dev/null  # V3 Patch 9
grep -nE 'Phase 2\.5b' src/commands/research/main.md 2>/dev/null                          # V3 Patch 8 spec text
grep -nE 'check 17|check 18' src/devforge/lib/research_helper.py 2>/dev/null              # V3 verify checks
ls /Users/mykolakudlyk/.claude/projects/-Users-mykolakudlyk-Projects-ai-dev-team-forge/memory/feedback_literal_archaeology_and_argument_duplication.md 2>/dev/null  # V3 pre-flight memory entry

# 18 May patches — GATED on V3 empirical verify passing (V2 already passed)
grep -nE '"verify-all"' src/devforge/lib/_generate_docs/_cli.py 2>/dev/null    # COMMAND-VERIFY-GATES Step 1 done?
grep -nE 'add_parser\("verify"' src/devforge/lib/init_helper.py 2>/dev/null     # Step 2 done?
grep -nE '"verify-rendered"' src/devforge/lib/specify_helper.py 2>/dev/null     # Step 3 done?
grep -nE '"forge-internal:verify-universal-defaults"' src/devforge/lib/constitute_helper.py 2>/dev/null  # CONSTITUTION-DRIFT done?
grep -nE 'kind.*nfr.*constitution_anchor' src/devforge/lib/specify_helper.py 2>/dev/null   # THREE-LAYER Gap A done?
grep -nE 'stamp-spec' src/devforge/lib/cbm_sync_helper.py 2>/dev/null  # THREE-LAYER Gap E done?
```

Any `grep` with hits → step already landed; skip. No hits → step pending.

0.3 **V3 hard-gate check**: if V3 patches grepped at 0.2 are absent OR V3 empirical-verify status field in `RESEARCH-FRAMING-REGRESSION-PLAN-V3.md` does NOT say "V3 applied + empirically verified (turn-1-S3 acceptance)" — **STOP all 18 May work**. Execute V3 first per its own "When resuming work" section.

0.4 **STOP** — confirm with user which plans are in scope for today. Default if V2+V3 verified: all four (THREE-LAYER + CVG 1/2/3 + DRIFT-DETECTOR + Gap E). If parity 4-run gate cleared overnight, also start THREE-LAYER Gap B + C.

---

### Phase 1 — Parallel-safe quick wins (morning, ~2-3 hr)

These have NO inter-step deps. Dispatch in parallel.

#### 1.A — COMMAND-VERIFY-GATES Step 1 (`generate_docs verify-all`)

**Brief for python-engineer**:

> Goal: add `verify-all` subcommand to `generate_docs_helper` that walks every recorded concern + package doc and invokes existing `cmd_validate_doc` per path; aggregates to exit 0 / exit 2.
>
> Integration context: `/generate-docs` Phase 5 (src/commands/generate-docs/main.md:573-577) currently has prose for this loop. Plan body: [COMMAND-VERIFY-GATES-PLAN.md](COMMAND-VERIFY-GATES-PLAN.md) Step 1. State files: `.devforge/concerns.json` + `.devforge/packages.json` — verify exact names by reading existing helper code first.
>
> Constraints: helper-owns-shape — aggregation logic in helper, not in spec; reuse existing `cmd_validate_doc`, don't duplicate. Test-first per `feedback_test_first_python_helpers`. Hard 3 test cases: happy path, one-broken path, empty state.
>
> NOT-to-do: do not change `validate-doc` itself; do not touch Phase 2.5 / 3.2 / 3.3 per-doc invocations; do not add emitter changes (new verb on existing helper).
>
> Success: `pytest tests/lib/test_generate_docs_helper.py::test_verify_all -v` green; `grep -n "verify-all" src/commands/generate-docs/main.md` shows spec updated.

Dispatch `python-engineer` → `python-reviewer` → `instruction-author` → `instruction-reviewer` per `feedback_dual_agent_verify_command_statements`.

**STOP** condition: all four agents clean + integration run on testForge20 Phase 5 exits 0.

#### 1.B — COMMAND-VERIFY-GATES Step 2 (`init_helper verify`)

**Brief for python-engineer**:

> Goal: add `verify` subcommand to `init_helper` that cross-checks `.devforge/init.yaml` + `.devforge/index.json` + `docs/structure.md`. Invariants listed in plan Step 2.
>
> Integration context: `/init-forge` (src/commands/init-forge/main.md) currently lacks Step 7. Other commands in 4-command sequence all expose `<helper> verify`. Plan body: [COMMAND-VERIFY-GATES-PLAN.md](COMMAND-VERIFY-GATES-PLAN.md) Step 2.
>
> Constraints: anti-corruption check — walk fixed manifest filenames at depth ≤2 under `project_root`; if any found, `packages_detected` list must be non-empty. Test-first; one test per failure path enumerated in plan.
>
> NOT-to-do: do not change Steps 1-6 of `/init-forge`; do not re-architect the index artifact.
>
> Success: `pytest tests/lib/test_init_helper.py::test_verify -v` green; `grep -n "Step 7" src/commands/init-forge/main.md` shows section added; integration mutation test (remove `default_branch` from init.yaml) → exit 2 with field name on stderr.

Same agent chain. **STOP** on green.

#### 1.C — CONSTITUTION-DRIFT-DETECTOR Steps 1+2 (parsers)

**Brief for python-engineer**:

> Goal: add two internal parser functions to `constitute_helper`:
> - `_parse_universal_blocks(constitution_md_path)` — walks markdown headings; identifies universal sections (§3.5, §3.6, §3.7, §4.1, §4.2, §4.3, §6.1, §6.2, §6.3, §6.4); returns `{section_number: {heading, rules: [{tag_or_label, body}]}}`. §3.6 sub-rules (Open/Closed, LSP, ISP, DI) parsed separately; §4.3 PREFER-bullets parsed separately.
> - `_extract_universal_rules_from_state(constitute_json_path)` — same return shape sourced from `.devforge/constitute.json`; filter by `tag == "universal"`.
>
> Plan body: [CONSTITUTION-DRIFT-DETECTOR-PLAN.md](CONSTITUTION-DRIFT-DETECTOR-PLAN.md) Steps 1 + 2.
>
> Constraints: test-first; both parsers tested using REAL on-disk inputs (current `src/constitution.md` for canonical; round-trip via `constitute_helper render-config` for state JSON per `feedback_test_first_python_helpers`).
>
> NOT-to-do: do not register a subcommand yet (Step 3); do not write the diff logic (Step 3); these are private helpers only.
>
> Success: `pytest tests/lib/test_constitute_helper.py::test_parse_universal_blocks_happy_path -v` green; same for `test_extract_universal_rules_happy_path`; inline Python sanity check from plan Step 1's verify block passes.

**STOP** on green. Steps 3+4 are afternoon work.

---

### Phase 2 — Mid-day core (4-5 hr)

#### 2.A — THREE-LAYER Gap A (kind taxonomy split)

**HIGHEST-LEVERAGE work today.** Load-bearing on every spec. Blocks COMMAND-VERIFY-GATES Step 3 + THREE-LAYER Gap B/C.

**Brief for python-engineer**:

> Goal: split `specify_helper record-constraint` `--kind` enum. Remove `use`; add `nfr` / `constitution_anchor` / `external_system`. Retain `follow` + `not_break`. Per-kind validators:
> - `nfr`: require non-empty `--quantifier` **AND validate quantifier shape — closes Risk 2 from 2026-05-17 audit** (do NOT accept bare adjectives). Helper applies `NUMERIC_THRESHOLD` regex (`\d+\s*(ms|s|min|hr|users?|req/s|rps|qps|tps|GB|MB|KB|TB|%|\$|connections?|rows?|records?)\b`) + `VAGUE_BLOCKLIST` reject (`high / low / fast / slow / scalable / good / acceptable / reasonable / robust / performant / efficient / secure / reliable`). Bare adjective = reject with stderr "vague quantifier '<X>' rejected. Use numeric threshold + unit OR named-class with cite source." Full regex + blocklist in [THREE-LAYER-SEPARATION-PLAN.md](THREE-LAYER-SEPARATION-PLAN.md) Step A.1.2.
> - `constitution_anchor`: require `--constitution-ref` (e.g. `§3.6`); helper greps `<install_root>/constitution.md` for `^### §<ref>` or `^### <ref>`; reject on miss. **Known limitation (Risk 3)**: validates location, not body match. Stale-body case caught by DRIFT-DETECTOR (Phase 1.C + 2.B). Disclose in spec text per Step A.2.3.b.
> - `external_system`: require `--protocol` OR `--contract-doc-ref`.
> - `use`: hard-reject with stderr citing all three replacement kinds.
>
> Plan body: [THREE-LAYER-SEPARATION-PLAN.md](THREE-LAYER-SEPARATION-PLAN.md) Gap A Step A.1.
>
> Integration context: `/specify` Step 4.6 (src/commands/specify/main.md:557-570) currently emits `--kind <follow|not_break|use>`. After API change, spec text needs rewrite (Step A.2 — instruction-author owns that).
>
> Constraints: state file shape migration — rename existing `constraints[*].kind == "use"` entries on read with migration warning. Test-first; cases enumerated in Step A.1.4.
>
> NOT-to-do: do not modify Step 4.6 spec text in this dispatch (instruction-author dispatch follows); do not touch other helper subcommands; do not split §7 into two sections.
>
> Success: helper rejects `--kind use` with helpful stderr; new kinds accepted with their per-kind required flags; legacy state JSON with `kind=use` triggers migration warning. `pytest tests/lib/test_specify_helper.py -k "constraint" -v` all green.

Agent chain: python-engineer → python-reviewer → instruction-author (Step A.2 spec rewrite) → instruction-reviewer + claude-code-guide in parallel (Step A.3 verifies multi-flag enum CLI idiom).

**STOP** conditions:
- helper tests green
- spec text rewrite clean per dual-agent review
- cross-grep (Step A.4) shows no dangling `--kind use` references in `src/` or `tests/`
- end-to-end test on testForge20: synthetic `/specify` invocation with a tempting architecture-pre-lock scenario produces §7 with `nfr` + `constitution_anchor` entries, NO architecture choices

#### 2.B — CONSTITUTION-DRIFT-DETECTOR Steps 3+4 (verb + docs)

Run AFTER Phase 1.C lands. Can overlap with 2.A if separate session.

**Brief for python-engineer**:

> Goal: register `forge-internal:verify-universal-defaults` subcommand on `constitute_helper`. Flags: `--consumer-path` (required), `--canonical-path` (default `src/constitution.md`). Logic enumerated in plan Step 3.
>
> Plan body: [CONSTITUTION-DRIFT-DETECTOR-PLAN.md](CONSTITUTION-DRIFT-DETECTOR-PLAN.md) Step 3.
>
> Constraints: reuse Phase-1.C parsers (`_parse_universal_blocks`, `_extract_universal_rules_from_state`); exit semantics — 0 = in sync, 2 = MISSING or DRIFT findings; stdout = JSON report; stderr = one finding per line.
>
> NOT-to-do: do not write a resync helper that mutates state (explicit out-of-scope); do not ship the canonical template to consumers; do not change `/constitute` user-facing flow.
>
> Success: three tests green (in-sync, missing-section, drift-one-rule); real integration check against `~/Projects/testForge20` exits 2 with DRIFT findings for §3.6 (DI/LSP/ISP/Open-Closed), §3.7, §4.3 (testForge20 was constitute'd before 2026-05-16 patches landed — that exact gap is the canonical positive test).

Then instruction-author for Step 4 (CLAUDE.md + CHANGELOG entry).

**STOP** on real integration check showing expected DRIFT findings for testForge20.

---

### Phase 3 — Afternoon (3-4 hr)

#### 3.A — COMMAND-VERIFY-GATES Step 3 (`specify verify-rendered`)

**BLOCKED on Phase 2.A.** Do not start until Gap A clean.

**Brief for python-engineer**:

> Goal: add `verify-rendered` subcommand to `specify_helper` with `--path <spec.md>`. Logic: read on-disk bytes; invoke existing `render` codepath; **compare via canonical-form normalization, NOT raw byte-identity — closes Risk 1 from 2026-05-17 audit.** Canonical form = LF line endings, no trailing whitespace per line, single trailing newline. Exit 0 = canonical bytes match; exit 2 = stderr cites byte-count diff + first divergent line computed on canonical forms.
>
> **Precondition**: ship `test_render_is_deterministic` test FIRST. Two `render(state)` calls must produce byte-identical output. If render reads clock / env / cwd / random anywhere, fix render BEFORE shipping verify-rendered — gate is meaningless on non-deterministic render. Full normalization function + test list in [COMMAND-VERIFY-GATES-PLAN.md](COMMAND-VERIFY-GATES-PLAN.md) Step 3.
>
> Plan body: [COMMAND-VERIFY-GATES-PLAN.md](COMMAND-VERIFY-GATES-PLAN.md) Step 3.
>
> Integration context: `/specify` Step 4.10 (post-Write) currently has no post-render gate. After landing, append the new shell call to Step 4.10. THREE-LAYER Gap A landed earlier today — test fixtures MUST use new kind taxonomy (`nfr` / `constitution_anchor` / `external_system`), NOT removed `use` kind.
>
> Constraints: test-first; happy path + tamper path + missing-file path. Fixtures generated via real `specify_helper render` round-trip per `feedback_test_first_python_helpers` real-producer requirement.
>
> NOT-to-do: do not change `render` itself; do not gate Steps 4.1-4.9 (those run pre-render, this gate is post-render only).
>
> Success: three tests green; integration on testForge20 — synthetic `/specify` with new kind taxonomy renders, writes, verify-rendered exits 0; manual append-byte triggers exit 2.

**STOP** on green + integration.

#### 3.B — THREE-LAYER Gap E (per-spec drift stamp)

**Independent of all above.** Can land any time today.

**Brief for python-engineer**:

> Goal: extend `cbm_sync_helper` with per-spec stamping:
> - `stamp-spec <spec-path>` — append `(spec_path, git_sha_at_write, cbm_index_version_at_write, timestamp_iso)` to `.devforge/spec-stamps.jsonl`.
> - `check-spec <spec-path>` — return state token: `current` / `missing` / `drift <a>..<b>` / `current-but-stale-cbm`. Drift logic: parse spec.md §4 Affected Areas → for each cited file run `git log <stamp_sha>..HEAD -- <file>` → non-empty = changed → `drift`.
>
> Plan body: [THREE-LAYER-SEPARATION-PLAN.md](THREE-LAYER-SEPARATION-PLAN.md) Gap E Steps E.1-E.5.
>
> Integration context: existing `cbm_sync_helper` stamps repo HEAD (`current` / `missing` / `drift <a>..<b>`). This is a SECOND stamp track, sidecar file `.devforge/spec-stamps.jsonl`. Do NOT regress repo-HEAD stamp behavior.
>
> Constraints: test-first; 4 cases (round-trip, drift-on-cited-file, clean-on-unrelated-file, missing-spec). Append-only JSONL.
>
> **Known limitation (Risk 4)**: drift detection precision = §4 Affected Areas completeness ceiling. If spec author misses a file in §4, drift on that file = silent. `/breakdown` already pressures §4 completeness for task partitioning; same pressure benefits drift detection. Stage 2 CBM-trace_path expansion (depth-1 outbound from §4-cited files) DEFERRED until empirical miss-rate justifies cost. Disclose in `/specify` Phase 4 spec text per Step E.1.3.
>
> NOT-to-do: do not auto-rewrite drifted specs; do not stamp plan.md (separate future plan); do not add CBM index version coupling beyond Stage 1 (git_sha-only); do not implement Stage 2 trace_path expansion in this dispatch.

Then instruction-author for wire-in to `/specify` Phase 4 (Step E.2), `/plan` Phase 0 (Step E.3), `/execute-task` entry (Step E.4).

**STOP** on green + 3 integration scenarios verified.

---

### Phase 4 — End-of-day verification (30 min)

4.1 Cross-grep sanity:

```bash
grep -RnE 'verify-all|verify-rendered|init_helper verify|forge-internal:verify-universal-defaults|stamp-spec|--kind nfr|constitution_anchor|external_system' src/ tests/ scripts/ | wc -l
```

Expect ≥ 20 hits across new verbs.

4.2 Confirm no dangling old references:

```bash
grep -RnE '--kind use[^_-]' src/ tests/    # zero hits expected
grep -RnE '"use"' src/devforge/lib/specify_helper.py   # zero hits in constraint-kind context
```

4.3 testForge20 end-to-end smoke:

```bash
cd ~/Projects/testForge20
bash ~/Projects/ai-dev-team-forge/install.sh
# walk full sequence: /init-forge → /generate-docs → /configure → /constitute → /specify (synthetic)
# confirm: each command's verify exits 0; /specify with new kind taxonomy works; verify-rendered passes
```

4.4 Run constitution drift detector against testForge20:

```bash
cd ~/Projects/ai-dev-team-forge
./src/devforge/lib/constitute_helper "forge-internal:verify-universal-defaults" \
    --consumer-path ~/Projects/testForge20 \
    --canonical-path src/constitution.md
# Expect: exit 2 with DRIFT findings for strengthened sections (positive validation that detector works)
```

4.5 Memory updates per `feedback_default_argue_engage_critically` + each plan's "Memory updates" section:

- `feedback_specify_section7_kind_taxonomy.md` — new kinds shipped
- `project_three_layer_separation_delivered.md` — what shipped (A, E), what deferred (B, C, D)
- `project_command_verify_gates_delivered.md` — Steps 1-3 shipped
- `project_constitution_drift_detector_delivered.md` — verb shipped
- Update `project_cbm_sync_delivered.md` — per-spec drift now active

4.6 Commit. Suggested grouping (separate commits per plan for clean history):

```
feat(generate-docs): verify-all aggregator gate
feat(init-forge): step 7 verify gate
feat(constitute): forge-internal verify-universal-defaults drift detector
feat(specify): §7 kind taxonomy split (nfr/constitution_anchor/external_system)
feat(specify): verify-rendered post-write gate
feat(cbm-sync): per-spec drift stamp + check-spec
```

Co-Authored-By: Claude footer per project convention.

---

### Phase 5 — Decision gate for RESEARCH-HANDOFF (5 min)

After Phase 4 verification clears:

5.1 If wall-clock allows (≥3 hr remaining in work block) → start [RESEARCH-HANDOFF-PLAN.md](RESEARCH-HANDOFF-PLAN.md) Phase 0 state checks + Step 1 (schema lock). First step is ~2-3 hr.

5.2 If wall-clock tight → close 18 May, queue RESEARCH-HANDOFF for next session. Plan's "When resuming work" section is the entry point.

5.3 RESEARCH-HANDOFF depends on ALL 18-May patches landing. If any Phase 4 verification failed, RESEARCH-HANDOFF Phase 0 will block. Resolve 18-May regression first.

---

## What stays OPEN after 2026-05-18

- **RESEARCH-HANDOFF-PLAN** — drafted 2026-05-17 as next-up after 18 May + V3. Gates on 18 May Phase 4 verification clearing AND V3 empirical-verify passing. ~13 hr scope (one focused day or short spillover). Closes manual-paste-handoff loss + research-outcome-unmarked failure modes. Schema must absorb V2 + V3 fields (data_flow_chain + trace_mode + value_semantics.stable_across_calls + value_production_sites + literal_archaeology + plan_seeds.proposed_call_shape). See [RESEARCH-HANDOFF-PLAN.md](RESEARCH-HANDOFF-PLAN.md).
- **PIPELINE-HANDOFF expansion** (deferred per user 2026-05-18) — extend RESEARCH-HANDOFF from research→specify bridge to full-pipeline memory (research/discover → specify → plan → execute-task → verify, each stage append-only via helper). Re-evaluate after V3 verify + 18 May patches ship.
- **PLAN-COMMAND-REDESIGN-PLAN Step 6 parity 4-run gate** — user-driven, asynchronous. Run when ready. After gate clears, start THREE-LAYER Gap B + C (extend that plan as Steps 7-9).
- **THREE-LAYER Gap B + C** — gated on PLAN-COMMAND-REDESIGN parity completion.
- **THREE-LAYER Gap D** — DEFERRED (intent-kind axis; revisit only if consumer-product team adopts forge).
- **L3 maturity jump** — split `intent.md` / `spec.md` / `plan.md` into separate artefacts. Out of scope for entire current cycle. RESEARCH-HANDOFF-PLAN's handoff.json IS a partial structural answer to this — three-layer split across command boundary without separate artefact files.
- **`forge-internal:resync-universal-constitution-rules`** — writer that mutates consumer state JSON to align with canonical. Future-work per CONSTITUTION-DRIFT-DETECTOR out-of-scope.
- **`/breakdown` port** — out of scope per PLAN-COMMAND-REDESIGN.
- **Cross-research consult** at /research time — RESEARCH-HANDOFF-PLAN ships the artefact (handoff.json with structured fields); query loop over handoff corpus is separate future work.
- **`/plan` consumes handoff** — gated on PLAN-COMMAND-REDESIGN parity AND RESEARCH-HANDOFF landing. Future Step 11 added to PLAN-COMMAND-REDESIGN when both clear.

## When resuming work mid-day or next day

1. Read this doc + the relevant plan's `## When resuming work` section.
2. Run Phase 0 state checks to find which steps are done.
3. Resume at the first step whose verification is incomplete.
4. **Sequencing rule**: if THREE-LAYER Gap A is NOT done and you're tempted to start COMMAND-VERIFY-GATES Step 3, STOP. Gap A blocks Step 3. Do A first or pick a different parallel step.

## Cross-references

- Discipline rules in [CLAUDE.md](CLAUDE.md) — "Working process for all changes" + "Code & spec discipline" sections govern every step
- Memory: `feedback_test_first_python_helpers`, `feedback_sentence_level_hallucination_check_specs`, `feedback_cross_check_after_every_change`, `feedback_dual_agent_verify_command_statements`, `feedback_helper_owns_shape_principle`, `feedback_emitter_promoted_cross_check`, `feedback_zero_escape_hatch_policy`
- Article origin (Three-Layer-Separation only): Kapil Viren Ahuja, *"Spec-Driven Development Isn't Broken. It will collapse."* (Medium 2026-05-03)

## Out of scope (this workflow)

- Strategy / direction shifts. Plans are pre-decided; this doc only sequences.
- New plans. If a new gap surfaces mid-day, draft a separate plan; do not inline-extend this workflow.
- L3+ maturity work. Article's higher-level claims (intent-driven, autonomous, dark factory) are aspirational; not on today's slate.
