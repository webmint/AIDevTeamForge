# 70 — Pipeline Wall-Clock Profiling Plan

**Status:** ✅ DONE (build) 2026-08-08 — maintainer disposition. Phases 0–1b shipped 2026-08-07 (ratified; probe answers recorded; `_profile/` + `profile_helper` built, 130 tests, python-reviewer two-pass clean, install-ride verified). **Phase 2 (real-run diagnosis) DEFERRED by maintainer to post-release — not blocking**; its recipe + precondition (post-63 marker confirm) stand below for when release-install runs produce `.devforge/profile/` data. Phase 3 opens only from those numbers.
**Branch:** `develop-2.0-init`
**Date:** 2026-08-05
**Updated:** 2026-08-07 — reconciled with plan 63 (`/devforge:` namespace + model-invocable commands) throughout.

## Problem

A full pre-implement chain (`/research` → `/specify` → `/plan` → `/breakdown`, plus optional `/grill` / `/spec-check`) takes the maintainer ~2h wall-clock on 2.0 vs "much faster" on 1.x. The maintainer's felt cause is "Python added slowness." **That hypothesis is falsified empirically** (see grounding). We do not currently know where the 2h actually goes — the maintainer's own read is "im not sure." Optimizing blind risks cutting the wrong thing (e.g. dialing back quality gates that aren't the hog).

**This plan does NOT optimize. It measures.** It builds a tool that reads a Claude Code session transcript and reports, per pipeline command, the exact wall-clock split — LLM turn time vs helper (Bash) time vs agent sub-session time vs human-answer time. Only after the data names the hog do we open a follow-on plan to cut it.

Measure-first is the correct order and matches repo precedent (RTK `discover`, the CBM empirical probes, plan 41's live-`src/` gate): instrument what exists, then act on numbers.

**Naming.** A bare `/<name>` throughout this plan (`/research`, `/plan`, …) is shorthand for the emitted `/devforge:<name>` typed form established by plan 63 (shipped 2026-08-07); pre-63 consumer transcripts carry the legacy bare form. Where the distinction is load-bearing — transcript parsing — it is spelled out explicitly (OQ3, Phase 0).

## Empirical grounding (verified 2026-08-05)

Python helpers are NOT the wall-clock cost. Helper cold-start timings (measured 2026-08-05):

```
# python3 -c "pass"                      -> 0.05s   (interpreter baseline)
plan_helper        (cold import+dispatch)-> 0.074s
breakdown_helper                         -> 0.075s
research_helper                          -> 0.099s
specify_helper                           -> 0.112s
spec_check_helper                        -> 0.081s
audit_helper                             -> 0.077s
import z3   (spec-check heaviest dep)    -> 0.367s
```

Even 50 helper calls across a whole pre-implement ≈ 5s. Not 2h. **Python is NOT the cost.** What LOOKS like "python running" is the **Opus LLM turn wrapped around** the ~80ms helper call — the helper returns instantly; the surrounding turn (read output → narrate → compose next args → dispatch) is the cost.

Model tiers (`scripts/lib/install_defaults.py:30`, `src/agents-AUTHORING.md:19`): `think→opus`, `do→sonnet`, `verify→sonnet`, `scan→haiku`. Pre-implement leans on `think` agents (architect, devils-advocate, spec-formalizer, api-designer, security-reviewer) = **Opus** = slowest per token. Reviewers/engineers already Sonnet. So nothing is misconfigured to "all Opus" — the cost is turn VOLUME × Opus latency × Opus[1m] context prefill, not tier misassignment.

> **Dated note — 2026-09-04, `94-MODEL-OVERRIDE-AND-NO-DEFAULTS-PLAN.md` D2. The two citations above are DEAD and the tier→model mapping they describe no longer exists.** `scripts/lib/install_defaults.py` was deleted (the `CLAUDE_AGENT_DEFAULTS_BY_TIER` map was its only symbol), so **the framework now picks no model for any tier**: every emitted agent carries `model: inherit`, and a project's actual per-tier models come from its own `/devforge:configure` Q11 answers, applied to `.claude/agents/*.md` — and to eight `.claude/commands/devforge/*.md` — by `configure_helper apply-models`. The tier NAMES also moved: `scan` is retired and `security` added, so the set is `think | do | verify | security` (`src/agents-AUTHORING.md`'s `model_tier` row, re-derived the same day). ⚠ **What this means for the paragraph above: its tier→model assignment is no longer a property of the framework, it is a property of the install being profiled** — so any wall-clock measurement Phase 2 produces must RECORD the profiled install's configured models rather than assume this mapping, and a run on a never-configured install measures whatever the session model is. **Nothing in this plan's reasoning is edited**, and the observation it rests on — that the cost is turn volume rather than tier misassignment — is untouched by the change.

Transcript schema (verified against `~/.claude/projects/<encoded-cwd>/<uuid>.jsonl`; SUPERSEDED in part by the 2026-08-07 Phase 0 probe below — where they disagree, the probe wins):
- One JSON object per line; the parser must tolerate per-line parse failure (the 2026-08-07 probe found 0 unparseable lines in 22,795, but the format is version-unstable — keep the tolerant parse).
- Fields present: `type` (`user` | `assistant` | `system` | `mode` | `attachment` | `file-history-snapshot` | `last-prompt` | `ai-title` | `queue-operation` | `pr-link` | `permission-mode`), `timestamp` (ISO8601 `Z`, millisecond precision) on every `user`/`assistant`/`system` line probed, `message.content[]` carrying `tool_use` (`{name, input}`) and `tool_result`, plus per-line `sessionId` / `version` / `cwd` / `gitBranch`.
- Tool names seen: `Bash`, `Agent`, `AskUserQuestion` (the 2026-08-05 draft said `Task` — the 2026-08-07 probe of harness `version: 2.1.195` transcripts shows the agent-dispatch tool is named `Agent`; the parser must accept BOTH names, older transcripts may carry `Task`).

The data needed for the split ALREADY EXISTS in the transcript. No runtime instrumentation required.

A project's harness transcripts live at `~/.claude/projects/<encoded-cwd>/<uuid>.jsonl` — the same path form as the schema note above, where `<encoded-cwd>` is the project's own absolute root path with every `/` replaced by `-`. Current machine reality (verified 2026-08-05): **mintEnvoy HAS a transcript dir** (`~/.claude/projects/-Users-mykolakudlyk-Projects-private-mintEnvoy/`) — it is the currently-available probe target; **testForge20 does NOT** have a transcript dir under `~/.claude/projects/` yet (its install exists but no harness transcript is present — treat as pending re-install / conditional). The 2h happens in a consumer install like these, not in the forge dev repo.

## Goal

A tool (shipped into consumer installs, per D2) that, given a session transcript (or auto-located latest for the project), prints and stores:

- Per pipeline command (`/research`, `/specify`, `/plan`, `/breakdown`, `/grill`, `/spec-check`): total wall-clock and the split into (a) LLM generation time, (b) Bash/helper time, (c) agent sub-session time (`Agent`/`Task` dispatches), (d) human-answer time (gap while awaiting a user message / `AskUserQuestion` reply — D4), plus counts (turns, helper calls, agent dispatches).
- Session totals + the same split, so the single largest time bucket is unambiguous.

Deliverable is a **diagnosis**, not a perf change.

## Non-code levers (available NOW, independent of build)

These need no code. Items 1 and 2 are zero-quality-cost and recommended now, regardless of what the profiler later finds; item 3 is not a new recommendation (see below):

1. **`/fast`** — Opus faster output, no model downgrade, no quality loss, at a higher per-token cost (research-preview; requires usage credits / org enablement). Cuts latency on every stacked Opus turn in the Opus-heavy pre-implement.
2. **`/clear` between `/research` → `/specify` → `/plan` → `/breakdown`.** Pipeline state persists to disk (the handoff JSON chain — `handoff.json`, `plan-handoff.json`, `breakdown-handoff.json`); the next command re-reads disk. Clearing drops the Opus[1m] context-prefill tax that grows every turn.
3. **`/grill` + `/spec-check` are ALREADY opt-in** per their own specs (plans 23 and 62) — this profiler does NOT change that, and this plan makes NO pre-measurement recommendation to skip them (that would be exactly the "cut a gate before knowing it's the hog" the Problem section warns against). A user who already skips them on low-stakes features for time/cost reasons will simply see that reflected in Phase 2's numbers; whether skipping is worth it is a Phase-3 evidence question, not a lever asserted now.

The profiler validates or refutes the relative value of items 1 and 2 against real numbers.

## Decisions

- **D1 — Transcript post-analysis, NOT runtime hooks.** The harness already timestamps every event (and stamps per-turn `turn_duration` records — per-tool `durationMs` turned out absent, see OQ1's answer; the timestamp joins carry the split). A PreToolUse/PostToolUse hook would re-capture existing data, add runtime overhead + per-install `settings.json` wiring, and only cover future runs. Transcript analysis is zero-overhead, retroactive (can profile a 2h run that already happened if its transcript survives), and reads ground truth from the harness itself. **NO pipeline / hook / `settings.json` change.**

- **D2 (REVERSAL — supersedes the prior draft's maintainer-only `scripts/` stance) — The profiler SHIPS in `src/`, installs into consumers, and runs in the real install.** It lives at `src/devforge/lib/` as a `profile_helper` + `profile_helper.py` launcher shim + a `_profile/` subpackage, mirroring the existing `audit_helper` / `review_helper` / `summarize_helper` launcher+subpackage pattern. **Rationale:** the 2h happens in a consumer install (testForge20 / mintEnvoy), NOT the forge dev repo — which is what the maintainer explicitly does not care about profiling. The maintainer will release, run the pipeline a few times in a real install, and hand the logs over. A maintainer-only `scripts/` tool (plan 41's home) cannot run there. This is the deliberate departure from plan 41's tool home — the tool SHAPE (helper + launcher + `tests/lib/` fixture) still follows plan 41 / plan 53 precedent, only the SHIP surface differs.

- **D3 — Measure first; defer ALL perf changes.** No retiering, no "lite mode," no gate removal in this plan. Those are a follow-on plan gated on this plan's Phase 2 numbers (Phase 3 opens it). Prevents cutting the wrong thing.

- **D4 — Human-answer time is measured and reported SEPARATELY**, never folded into "LLM time." A gap ending at a `user` message (or spanning an `AskUserQuestion` tool_use → next user turn) is the maintainer thinking/typing, not the framework being slow. Conflating them would mis-blame the framework. Since plan 63 made 13 pipeline commands model-invocable, the model can chain those commands itself, so inter-command human gaps may shrink in post-63 transcripts — magnitude unverified, because `src/CLAUDE.md`'s model-invocation rule still requires a human-agreement turn before the model runs a command; Phase 0's post-63 probe will show whether that per-command "user agrees" turn still produces a measurable gap. The bucket DEFINITION above is unchanged either way, but boundary detection must not ASSUME every command span starts at a user message (see OQ3).

- **D5 — Per-run STORAGE in the install.** Each profiler run writes a per-run report `.devforge/profile/<session>-<timestamp>.json` (the raw bucket split) plus a rolling human-readable `.devforge/profile/summary.md`. After N runs the maintainer hands over the whole `.devforge/profile/` dir; the tool (or a session) aggregates across runs to name the hog. These artifacts are **EPHEMERAL-class per plan 49** — add `.devforge/profile/` to `src/files/devforge.gitignore` so the consumer git tree stays clean under plan 49's disposition model. No untrack migration is needed (the dir is brand-new, never previously tracked) — unlike plan 49/56, which had to `git rm --cached` already-tracked files.

- **D6 — Python routes python-engineer → python-reviewer** (repo discipline). This plan makes NO command / agent / `main.md` edits (it is pure tool + storage + gitignore + install wiring), so NO instruction-author / instruction-reviewer / claude-code-guide loop is needed for the CODE — no Claude Code integration claim is authored or changed. (This PLAN doc itself was authored via the instruction-author → instruction-reviewer loop per the maintainer's workflow directive; that governs the doc, not the code.)

- **D7 — No CI exists in the repo; the pytest test against a fixture transcript IS the correctness gate** (same posture as plan 41). The profiler is a diagnostic — it prints a report and writes storage; it has no pass/fail exit semantics.

## Open questions (resolve per the phase noted in each OQ below — do NOT pre-answer here)

- **OQ1 — Duration source. ANSWERED 2026-08-07 (Phase 0 probe, see Phase 0):** `durationMs` is NOT present on ANY `Bash` / `Agent` tool event (0 of 2,130 Bash + 574 Agent results across 20 mintEnvoy transcripts carry it); it appears ONLY on `system` lines with `subtype: "turn_duration"` (589 of 1,018 system lines — per-TURN totals, not per-tool). So the preference order from the draft INVERTS: the timestamp-diff (`tool_result.timestamp − tool_use.timestamp`, joined by `tool_use_id`) is the PRIMARY duration source; the `turn_duration` system lines serve as a per-turn sanity cross-check, not a per-tool source. Phase 1 still implements the tolerant dual path (use `durationMs` if a future harness version puts it back on tool events).
- **OQ2 — Agent sub-session nesting. ANSWERED 2026-08-07 (Phase 0 probe, see Phase 0):** separate files. A session's agent dispatches live at `<transcript-dir>/<session-uuid>/subagents/agent-<id>.jsonl`, each with a sibling `agent-<id>.meta.json` carrying `{agentType, description, toolUseId, spawnDepth}` — `toolUseId` is the join key back to the parent transcript's `Agent` tool_use. CRITICAL wrinkle the draft's assumption missed: background/async agent dispatches return their parent-side `tool_result` seconds after launch (observed: 3s for a multi-minute agent), so the parent tool_use→tool_result gap is NOT the agent's runtime for async dispatches. Agent time must be computed from the CHILD file's first→last event timestamps (joined via `meta.json.toolUseId`); the parent-side gap is only valid for synchronous dispatches, and the profiler must not double-count parent gap + child span for the same dispatch.
- **OQ3 — Command-boundary detection.** How to segment the transcript into per-command spans. Candidate signals, of which the user-typed one is generation-dependent since plan 63: (a) a `user` message whose text starts with a known slash command — post-63 transcripts carry the namespaced typed form (`/devforge:research`…), pre-63 transcripts (including today's mintEnvoy target and any surviving 2h-run transcript) carry the legacy bare form (`/research`…), so the parser must recognize BOTH generations; (b) a MODEL invocation of a command — a channel that did not exist before plan 63, which dropped `disable-model-invocation: true` on 13 pipeline commands (`research`, `discover`, `specify`, `plan`, `breakdown`, `implement`, `pr-review`, `audit`, `review`, `verify`, `summarize`, `finalize`, `report-bug`), so in a post-63 transcript a command span may begin mid-session with no user message at its start; the other 7 commands keep the flag and stay human-typed-only (the 4 setup commands + `/grill` + `/spec-check` + `/fix`), so this plan's two optional pipeline members `/grill` and `/spec-check` always begin at a user-typed message; (c) the first `Bash` call whose command contains `<cmd>_helper` — generation-independent, unaffected by either invocation channel, unchanged by plan 63. How signals (a) and (b) actually APPEAR in a transcript is undocumented (verified 2026-08-07 via the claude-code-guide agent against docs.claude.com: the session-transcript JSONL format is explicitly internal and version-unstable, and the docs specify no transcript representation for either a user-typed or a model-invoked custom slash command) — so the REPRESENTATION question joins OQ1/OQ2 as a Phase 0 probe item, and any specific marker shape is an UNVERIFIED candidate until that probe records it here. The SELECTION of the primary segmentation signal is still a pure design choice that does NOT need the probe — decide at Phase 1 (segmentation-signal selection is part of building the command-segmentation step). `/clear` starts a NEW transcript file, so a cleared chain spans multiple `.jsonl` — the tool must support a `--dir` mode that stitches a chain by mtime, PLUS a single-file mode.
- **OQ4 — Install wiring (state as Phase-1b TASKS, not settled facts).** The emitter `_PROMOTED` list (`scripts/emitters/claude.py`) is for COMMANDS only — helpers are NOT promoted there; they ride the full-install `cp -R` of `src/devforge/lib`. BUT plan 53 established that some new subpackages must ALSO be added to the `install.sh` + `update.sh` surgical `--only` always-copy set. Phase 1b must therefore: (a) confirm the `cp -R` full install ships `_profile/` + the `profile_helper` launcher; (b) check whether the surgical `--only` set needs the new module; (c) add the `.devforge/profile/` line to `src/files/devforge.gitignore` (D5). These are tasks to verify at build, not pre-decided outcomes.
- **OQ5 — Multi-run aggregation output shape.** How the per-run `<session>-<timestamp>.json` files roll up into a single cross-run verdict the maintainer hands over (e.g. per-command median bucket across runs, or a "largest bucket by run" table). Propose a shape at Phase 1; leave the final form to build.

## Phases

### Phase 0 — Ratify + probe (maintainer gate)

No build. The maintainer confirms D1–D7 and the goal. In the same phase, run a throwaway probe (NOT committed) against a REAL consumer transcript under `~/.claude/projects/`, NOT the forge dev repo — mintEnvoy (`~/.claude/projects/-Users-mykolakudlyk-Projects-private-mintEnvoy/`) is the currently-available target; testForge20 is conditional on a re-install producing a transcript dir — to resolve OQ1 (`durationMs` coverage across Bash/agent-dispatch events) and OQ2 (agent sub-turns inline vs separate file). Record the answers inline in this plan before Phase 1.

Third probe item — **OQ3 invocation representation** (plan-63 reconcile): inspect how a user-typed command marker appears in an existing PRE-63 consumer transcript and record that marker here. mintEnvoy's transcripts are pre-63: plan 63 is committed in the forge repo but not yet released to consumer installs, so no install emits `/devforge:<name>` yet. Then, AFTER the plan-63 release reaches an install, capture one post-63 transcript showing (a) a user-typed `/devforge:<cmd>` and (b) a model-invoked command, and record BOTH markers here as well. The pre-63 half is possible NOW; the post-63 half is conditional on release — the same conditional posture this phase already applies to testForge20.

**Verify:** maintainer sign-off recorded here; OQ1 + OQ2 + the pre-63 half of the OQ3 representation probe each answered with the probe command + its output pasted into this plan; the post-63 markers (namespaced typed form + model invocation) recorded here when a released install has produced a transcript.

**Phase 0 RESULTS (2026-08-07):**

- **Maintainer sign-off:** given 2026-08-07 ("ok go" on the reconciled plan — D1–D7 + goal ratified).
- **Probe:** a throwaway Python script (scratchpad, not committed) parsed ALL 20 mintEnvoy `.jsonl` transcripts — 22,795 lines, 0 unparseable — counting tool_use/tool_result per tool name, `durationMs` placement, and `<command-name>` markers; plus a per-line field-shape sample of one 8.1M transcript (harness `version: 2.1.195`).
- **OQ1 answer:** recorded inline at OQ1 above — no `durationMs` on tool events; timestamp-diff primary; `system`/`subtype:"turn_duration"` lines (per-turn totals, e.g. `durationMs: 2490719, messageCount: 198`) as cross-check.
- **OQ2 answer:** recorded inline at OQ2 above — separate `<session-uuid>/subagents/agent-<id>.jsonl` files + `meta.json` `toolUseId` join; async dispatch makes the parent-side gap invalid as agent runtime (observed 3s parent gap for a background agent); child-file first→last timestamps are the agent-time source.
- **OQ3 pre-63 marker (recorded):** a user-typed command appears as a `type: "user"` line whose `message.content` is a STRING beginning `<command-name>/<name></command-name>` followed by `<command-message>…</command-message>` and `<command-args>…</command-args>` (observed for `/implement`, `/review`, `/clear`, `/fix`, `/plan`, `/grill`, `/verify`, `/finalize`, `/research`, `/summarize`, `/breakdown`, `/specify`, `/spec-check`, `/discover`, `/generate-docs`, `/report-bug` — all bare pre-63 forms, as expected). `/clear` itself is such a marker — a `--dir` stitch can use it as a chain-boundary hint. Post-63 markers: still pending release (unchanged).
- **Bonus schema findings folded into Empirical Grounding:** agent-dispatch tool is named `Agent` (not `Task`) at `version: 2.1.195`; extra line types `queue-operation` / `pr-link` / `permission-mode`; every `user`/`assistant`/`system` line carries `timestamp` + `sessionId` + `version` + `gitBranch`.

### Phase 1 — Build the analyzer (python-engineer → python-reviewer)

`src/devforge/lib/_profile/` subpackage:
- Transcript parse — tolerant line-by-line JSON parse (skip meta/unparseable lines), normalizing each event to `{type, timestamp, tool_name, duration_ms, text}`.
- Command segmentation — split the event stream into per-command spans via the OQ3 signal (single-file and `--dir`-stitched-by-mtime modes), recognizing both pre-63 and post-63 transcript generations. Selecting the primary segmentation signal here settles OQ3's SELECTION half; its REPRESENTATION half (what a typed or model invocation looks like in the transcript) is answered earlier, by the Phase 0 probe's pre-63 half — the post-63 half arrives only at Phase 2 (see that phase's precondition).
- Bucket profiling — per segment + total, compute `wall`, `llm_s`, `bash_s`, `task_s`, `human_s`, `n_turns`, `n_helpers`, `n_agents`. Human time is a distinct bucket, never folded into `llm_s` (D4). Per OQ1's answer: per-tool durations come from the timestamp-diff join (`tool_use_id`), with `durationMs` honored only if present on the event; per OQ2's answer: agent time (`task_s`) comes from each dispatch's CHILD file first→last timestamps (`<session-uuid>/subagents/agent-<id>.jsonl`, joined via `meta.json.toolUseId`), falling back to the parent-side gap only when no child file exists, never counting both for one dispatch; the tool-name match accepts BOTH `Agent` and `Task`. Overlap semantics (async dispatches run WHILE the main thread continues, so child spans can overlap `llm_s`): the summing `task_s` bucket is the PARENT-side dispatch gap (main-thread blocked time — large for sync, ≈0 for async), and the child-file span is reported as a SEPARATE non-summing `agent_busy_s` column, so the four main-thread buckets still sum to wall.
- Report formatting — a plain table with the largest bucket flagged.
- Per-run + rolling storage writer (D5) — write `.devforge/profile/<session>-<timestamp>.json` (raw bucket split) and append/update `.devforge/profile/summary.md`.

`src/devforge/lib/profile_helper` + `src/devforge/lib/profile_helper.py` — the launcher shim (mirrors `audit_helper` / `review_helper` / `summarize_helper`): `--transcript <path>` (or auto-locate latest in the project's transcript dir), `--dir <transcript-dir>` (stitch a `/clear`-split chain by mtime), prints the report and writes storage.

`tests/lib/_profile/` — a hand-authored fixture `.jsonl` exercising, with every function tested + run in the same turn (repo discipline): tolerant parse (a bad line + meta lines skipped); timestamp-diff primary vs `durationMs`-honored-when-present (OQ1 BOTH paths, inverted priority per the probe); one Bash + one `Agent` dispatch (async — parent gap short, child `subagents/agent-<id>.jsonl` + `meta.json` supplying the real span — plus a `Task`-named legacy variant) + one AskUserQuestion → correct four-bucket split; a human-answer gap classified as `human_s` not `llm_s` (D4); multi-command segmentation across both transcript generations, built from whatever markers the Phase 0 OQ3 probe recorded (post-63 markers only once that half of the probe is available — until then the fixture covers the pre-63 form plus the generation-independent `<cmd>_helper` fallback, and Phase 2's precondition is where the post-63 extension lands if the first post-release transcript's markers do not match); and the storage writer (per-run JSON shape + rolling `summary.md`).

**Verify:** the `_profile` pytest suite is green; running the launcher against a real recent consumer transcript prints a coherent table whose per-segment MAIN-THREAD buckets (`llm_s` + `bash_s` + `task_s` + `human_s`) sum (± rounding) to that segment's wall-clock (`agent_busy_s` is a separate non-summing column — it may overlap `llm_s` for async dispatches), and writes `.devforge/profile/<session>-<timestamp>.json` + `summary.md`.

### Phase 1b — Install wiring (the OQ4 tasks)

- Add `.devforge/profile/` to `src/files/devforge.gitignore` (D5, EPHEMERAL-class per plan 49).
- Confirm the full-install `cp -R` of `src/devforge/lib` ships `_profile/` + the `profile_helper` launcher into a consumer's `.devforge/lib`.
- Check whether the `install.sh` + `update.sh` surgical `--only` always-copy set needs the new `_profile/` module (plan 53 precedent) — add it only if the surgical path would otherwise skip it.

**Verify:** a fresh install/update lands `.devforge/lib/_profile/` + `.devforge/lib/profile_helper` executable in a consumer; `.devforge/profile/` is gitignored (a written run leaves the consumer git tree clean); the surgical `--only` decision is recorded (added or confirmed unnecessary).

**Phase 1 + 1b RESULTS (2026-08-07):** Phase 1 SHIPPED — `src/devforge/lib/_profile/` (9 modules: `_parse`, `_segment`, `_agents`, `_bucket`, `_report`, `_locate`, `_storage`, `_cli`, `__init__`) + `profile_helper{,.py}` launcher + `tests/lib/_profile/` (130 tests green; full `tests/lib/` regression 9286 passed excl. pre-existing `_audit`-slow/`_spec_check`-no-z3 exclusions). Built python-engineer → python-reviewer; the reviewer's first pass found 2 HIGH bucket-arithmetic bugs (segment-boundary spillover; parallel-tool-call overlap inflation — both violating the sum-to-wall invariant), fixed by rewriting `_bucket.py` to a three-pass interval model (clip-every-interval-to-segment-window, per-bucket interval union, fixed-priority disjoint partition `human > task > bash`, `llm_s` = true residual — exact sum-to-wall, proven algebraically + adversarially re-probed clean on the second review pass). `durationMs` rule: interval = `[result_ts − duration_s, result_ts]`, start clamped ≥ tool_use ts. Smoke: profiled a real 8.1M mintEnvoy transcript — coherent 27-segment table, buckets sum, largest bucket flagged (a 17h overnight gap correctly classified `human_s`, not framework time). OQ5 resolved: `aggregate` verb prints per-command median+max wall / median bucket split / largest-bucket-per-run tally over `.devforge/profile/*.json`. Phase 1b DONE — (a) full-install `cp -R src/devforge/. → .devforge/` ships `_profile/` + launcher (scratch install-ride verified: launcher `-rwxr-xr-x`, `--help` runs; update path covered by the `src/devforge/lib/** → .devforge/lib/**` manifest glob + the `*_helper` chmod loop); (b) surgical `--only` always-copy set: NOT added, confirmed unnecessary — no command invokes `profile_helper` (the always-copy set exists for command runtime deps: `_shared`/`_implement`/`_artifact`/`_design`); (c) `.devforge/profile/` added to `src/files/devforge.gitignore` + a `profile/` EPHEMERAL row added to `src/devforge/storage-rules.md` (ride verified `git check-ignore` matches).

### Phase 2 — Real-run diagnosis (the deliverable)

**Precondition — close out OQ3's post-63 half FIRST.** Any release cut from current repo HEAD carries plan 63, so every Phase 2 transcript is post-63 by construction, while Phase 1's fixture covers only the pre-63 markers plus the generation-independent `<cmd>_helper` fallback. The FIRST post-release transcript therefore doubles as Phase 0's deferred post-63 OQ3 probe: record its namespaced-typed-form and model-invocation markers in Phase 0, then confirm the segmentation signal Phase 1 selected actually matches them. If it does not, extend the Phase 1 fixture + segmentation to the post-63 markers and re-run the `_profile` suite. Do NOT trust or aggregate any Phase 2 numbers until this holds — unmatched markers mean the segmenter silently mis-splits, and a mis-split table names the wrong hog.

The maintainer releases to a consumer install, runs the pre-implement pipeline a few times; the profiler writes `.devforge/profile/*` on each run; the maintainer hands over the `.devforge/profile/` dir. Aggregate the per-run JSONs (OQ5 shape) into a per-command time-split table and record it in this plan. This is the deliverable — the numbers that name the hog.

**Verify:** the post-63 OQ3 markers are recorded in Phase 0 and the Phase 1 segmentation signal is confirmed against them (or the fixture + segmentation extended and the suite re-run); a per-command time-split table for a real 2h-class run (aggregated across N runs) is recorded in this plan; the single largest bucket (LLM turns / agent sub-sessions / human-answer / helper) is unambiguous.

### Phase 3 — Decide optimizations from the data (opens a follow-on plan)

Read the Phase 2 table. Pick targeted cuts matched to the actual hog — candidates, chosen by EVIDENCE, NOT pre-committed here:
- If LLM-turn count dominates → reduce scratch-chain turn multiplication (fewer verb round-trips per command) and confirm `/clear`-between-stages savings.
- If agent sub-sessions dominate → retier specific `think`→`verify` agents (Opus→Sonnet) or make `/grill` / `/spec-check` skips the default for low-stakes.
- If human-answer dominates → the framework is NOT slow; trim rubric turns only if the maintainer wants (memory: rubric verbosity was previously validated — do NOT cut without an explicit ask).
- If Opus[1m] prefill dominates → institutionalize `/clear` between stages (doc + workflow note).

**Verify:** a new follow-on `<NN>-…-PLAN.md` at the next free plan number is opened citing the Phase 2 numbers as its evidence — 71 is already taken by `71-POST-CHANGE-CONSEQUENCE-PLAN.md`, so check the repo root for the highest existing number before naming it. This plan closes at Phase 2 (measurement delivered).

## Context for next session

- The whole point is **measure before cutting.** Do NOT add a perf optimization inside this plan — that is Phase 3's follow-on.
- Python helpers are proven fast (~80ms); if a future session is tempted to "speed up the helpers," re-read the Empirical Grounding — that is the wrong layer.
- **D2 is a reversal:** the profiler SHIPS into consumers (`src/devforge/lib/_profile/` + `profile_helper` launcher), because the 2h happens in a consumer install, not the forge dev repo. A maintainer-only `scripts/` tool (plan 41's home) could not run there. The tool SHAPE still follows plan 41 / plan 53 precedent.
- Storage is EPHEMERAL-class (plan 49): `.devforge/profile/` is gitignored via `src/files/devforge.gitignore`; no untrack migration needed (brand-new dir).
- Install wiring (OQ4): the emitter `_PROMOTED` is COMMANDS-only — helpers ride the `cp -R`; plan 53 shows some subpackages ALSO need the surgical `--only` set — Phase 1b decides.
- Transcript location: `~/.claude/projects/<encoded-cwd>/<uuid>.jsonl` (`<encoded-cwd>` = the project's absolute root with every `/` → `-`); mintEnvoy has a transcript dir now (available probe target), testForge20 is pending re-install. Schema facts are in Empirical Grounding; re-probe if the harness format has changed.
- **Plan-63 reconcile (2026-08-07).** The emitted typed form is `/devforge:<name>`; 13 pipeline commands are model-invocable and 7 stay human-typed-only (the 4 setup commands + `/grill` + `/spec-check` + `/fix`). That splits transcripts into two generations: pre-63 (bare typed forms — the only probe targets available today, since plan 63 is committed but not yet released to installs) and post-63 (namespaced typed forms plus possible model invocations). How either invocation channel is REPRESENTED in a transcript is a Phase 0 probe item, NOT a known fact — do not assume a marker shape. Plan number 71 is taken (`71-POST-CHANGE-CONSEQUENCE-PLAN.md`), so the Phase 3 follow-on takes the next free number.
- Precedent for tool shape: plan 41 (`scripts/lib/agent_reachability.py` + CLI + `tests/lib/…`, no CI so the pytest IS the gate) for the test-is-the-gate posture; plan 53 for the surgical `--only` install decision; plan 49 for the EPHEMERAL gitignore disposition.

## When resuming work

1. If Phase 0 not signed off → present D1–D7 + goal to the maintainer, run the OQ1/OQ2 probe plus the OQ3 invocation-representation probe against a REAL consumer transcript, NOT the forge repo (mintEnvoy is the available target; testForge20 conditional on re-install), record answers here. The OQ3 probe's pre-63 half runs now; its post-63 half (namespaced typed form + model invocation) waits until the plan-63 release reaches an install.
2. If signed off, Phase 1 not built → dispatch python-engineer for the `_profile/` subpackage + `profile_helper` launcher + `tests/lib/_profile/` (brief: the schema facts above, the four buckets, D4 human-time separation, OQ1 dual duration path, D5 storage writer), then python-reviewer.
3. If Phase 1 green → do Phase 1b install wiring (gitignore line + `cp -R` confirm + surgical `--only` check).
4. If Phase 1b done → hand the maintainer the Phase 2 run recipe; the maintainer releases, runs the pipeline a few times in an install, hands over `.devforge/profile/`. Satisfy Phase 2's precondition FIRST (the first post-release transcript closes OQ3's post-63 half; confirm or extend the segmentation), then aggregate and paste the table here.
5. If the Phase 2 table exists → open the Phase 3 follow-on plan citing it.
