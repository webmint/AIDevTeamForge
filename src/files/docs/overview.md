# {{PROJECT_NAME}}

{{PROJECT_DESCRIPTION}}

---

> This file is the human-facing project overview. The setup wizard fills in the name and description from Phase 2 answers; `/constitute` and `/onboard` may append deeper context; the tech-writer agent updates this file as features ship.
>
> For technical stack details (framework, language, build commands, per-package breakdown for monorepos), see `CLAUDE.md` / `AGENTS.md` — those are the runtime-facing sources of truth.
>
> For architecture decisions and per-layer rules, see `docs/architecture.md`.
>
> Feature-level documentation lives under `docs/features/<feature-name>.md` — created by tech-writer when each feature ships, not pre-scaffolded.

## What this project is for

_Populated by `/constitute` (goals, users, scope) and `/onboard` (for brownfield projects — extracted from existing code)._

## How it's used

_Populated by tech-writer as the project grows — usage examples, entry points, typical flows._
