---
name: configure
description: Populate config + substitute templates (STUB — full spec authored in CONFIGURE-PLAN.md Step 5)
disable-model-invocation: true
---

# /configure — STUB

This file is a Step 0 scaffolding placeholder. The authoritative spec is authored in CONFIGURE-PLAN.md Step 5 (`src/commands/configure/main.md` rewrite). Until then the command is wired up but invoking it does nothing.

See `CONFIGURE-PLAN.md` at the repo root for the design + work order.
