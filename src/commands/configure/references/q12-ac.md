# Q12 — AC verification mode (STUB)

Q12 picks one of `code-only | tests | runtime-assisted | off`. If `runtime-assisted`: conditional follow-up Q12.1 runtime URL, Q12.2 API base, Q12.3 CLI command. Skip the follow-up triple if any other mode.

Stub placeholder. See CONFIGURE-PLAN.md.
