# AIDevTeamForge

Spec-driven, agent-coordinated workflow framework for AI development. Generates Claude Code runtime files (`CLAUDE.md`, `.claude/agents/`, `.claude/commands/`) into target projects.

**Command naming (plan 63).** Emitted slash commands land at `.claude/commands/devforge/<name>.md` and are invoked as `/devforge:<name>`; their on-demand reference files land OUTSIDE the command scan at `.devforge/command-refs/<name>/`. Throughout this file a bare `/<name>` naming one of the emitted forge commands (e.g. `/plan`) is shorthand for the command whose source lives at `src/commands/<name>/` — the typed form in a consumer install is always `/devforge:<name>`. This repo's own maintainer commands (`.claude/commands/review-helper.md`, `.claude/commands/release.md`) are NOT emitted and stay un-namespaced.

## Active work — read these before starting

When picking up work mid-stream, check the repo root for active plan files. **Read the relevant plan in full before making any changes** — plans encode multi-session context that isn't in the conversation.

This list is a one-line-per-plan INDEX. The full multi-paragraph status entries were relocated verbatim to `PLAN-STATUS-ARCHIVE.md` (repo root) on 2026-08-17 — when resuming a plan, read its archive entry alongside the plan file. When a plan's status changes, amend the archive entry and keep the one-liner here in sync. File-less findings live in `FINDINGS.md`.

Currently active (numbered = execution order):

- `01-CONSTITUTION-FORCING-FUNCTIONS-PLAN.md` — ✅ DONE 2026-05-22. Consumer-side mechanical forcing-functions detector family (substrate + magic-enum, cross-layer, any-leak + pre-commit hook + `/constitute` §3.5 capture); Phase 5b absorbed into plan 07's `/implement`.
- `02-PLAN-COMMAND-REDESIGN-PLAN.md` — SHIPPED 2026-05-24, parity-pending. `/plan` command + `plan_helper` (10 verbs). Pending: Step 6 parity 4-run (user-driven, needs testForge20 re-install); 7.7 deferred YAGNI.
- `1.5-SPECIFY-PLAN-HANDOFF-PLAN.md` — CORE DONE in code (verified 2026-06-18). Typed `spec_seeds` produced by `/specify`, consumed by `/plan`. Residual (cosmetic, optional): the spec-PICKER display still regex-scrapes `spec.md`.
- `03-DISCOVER-HANDOFF-PLAN.md` — Steps 1+3–7 SHIPPED (Steps 2 N/A). Discover handoff chain (`_discover/` schema + build). Pending: Step 8 manual testForge20 e2e.
- `04-PR-REVIEW-PLAN.md` — Steps 1–11 SHIPPED 2026-05-20. `/pr-review <PR#>` + `pr_review_helper`. Pending: Step 12 real-PR validation (manual).
- `05-structural-integration-check-plan.md` — IMPLEMENTED 2026-05-24. code-reviewer §7 Structural Integration (DIR check). Pending: manual smoke tests (DUPLICATE-flag + no-false-positive). **EXTENDED 2026-08-23 by plan 86's F1 — the check gained a function-level arm: a near-verbatim clone of a sibling function added inside a MODIFIED file is `DUPLICATE` at High unless the task file, plan or spec declares and justifies it (file-level `DUPLICATE` stays Critical). Plan 05's own document is NOT edited and still describes the file-only scope.**
- `08-CLAUDE-MD-COMMAND-TRIM-PLAN.md` — SHIPPED 2026-05-24 (`237233e`). Consumer-overlay `src/CLAUDE.md` command-catalog trim. Supersedes `06-CONDITIONAL-CONTEXT-PLAN.md` (ABORTED, archived).
- `07-EXECUTE-TASK-REDESIGN-PLAN.md` — SHIPPED 2026-06-07. `/execute-task` renamed `/implement` (dispatch → scope-aware verify + self-repair → review → forcing-functions gate → WIP-commit). Pending: state-cardinality/departure-flagging e2e (run `/plan`, confirm `States:`/`DEPARTURE:` notes). Extended by plans 17 (panel) and 13 (wrapper).
- `13-IMPLEMENT-WRAPPER-MODE-PLAN.md` — DONE in code (verified 2026-06-18). Wrapper-mode resolver (`_implement/_workspace.py`) + wiring across `_implement/` and pipeline preflights. Standing manual gate: testForge20 wrapper e2e (Phase 9).
- `09-BREAKDOWN-COMMAND-REDESIGN-PLAN.md` — ✅ SHIPPED 2026-05-25 (`362aaea`). `/breakdown` + `breakdown_helper` (~13 verbs); machine contract = sibling `breakdown-handoff.json`. Interactive LLM-driven run user-driven.
- `10-AUDIT-COMMAND-PORT-PLAN.md` — Phases 0–7 SHIPPED 2026-06-01. `/audit` port + `--top N` hotspot mode (17 verbs). Pending: Phase 8 testForge20 e2e (user-driven).
- `11-AUDIT-FULL-SPECTRUM-PLAN.md` — Steps 1–9 SHIPPED 2026-06-01. One default `/audit` run hunts the full spectrum (producer-declared `Category` buckets). Pending: Step 10 e2e (user-driven HARD GATE).
- `12-AUDIT-MULTI-PASS-UNION-PLAN.md` — Steps 1–7 SHIPPED 2026-06-01. Opt-in `--passes N` multi-pass union (`_merge.py`). Pending: Step 8 e2e A/B (user-driven HARD GATE).
- `14-ARCHITECT-NOT-IMPLEMENTER-PLAN.md` — Steps 1–4 SHIPPED 2026-06-07. Architect never codes: rerouted assignment rows + `/implement` architect-guard. Pending: Step 5 e2e + two deferrals (`/fix` drop-vs-fix; inline-source duplication). Mechanical-gate follow-on = plan 38.
- `15-AGENT-STANDARDIZATION-PLAN.md` — Phases 0–5 SHIPPED 2026-06-07. 17-agent canonical skeleton (`src/agents-AUTHORING.md`); 6 reviewers tools-locked read-only; consumers rewired. Pending: full install-ride / testForge20 e2e.
- `16-CONVENTION-CAPTURE-PLAN.md` — ✅ DONE 2026-06-14; e2e PASSED. `/generate-docs` captures `styling` + `state_management` conventions buckets (schema 4 → 6) + `/constitute` Phase 2 routing redirect.
- `17-IMPLEMENT-PER-TASK-PANEL-PLAN.md` — Phases 0–6 SHIPPED 2026-06-07. `/implement` PHASE 5 runs tests; PHASE 6 four-reviewer panel (`merge-review-panel`); PHASE 7 all-findings-fixed (`accept anyway` removed). Pending: Phase 7 e2e (user-driven HARD GATE).
- `19-AUDIT-FALSE-POSITIVE-PRECISION-PLAN.md` — Steps 1–6 SHIPPED 2026-06-15. `/audit` refutation stage (`_verify.py`, PHASE 4.2.5) + confidence-gated report + D7 high-stakes guard. Pending: Step 7 e2e (user-driven HARD GATE).
- `20-REVIEW-COMMAND-REDESIGN-PLAN.md` — Phases 0–6 SHIPPED 2026-06-15. `/review` feature-level emergent cross-task review (5 finders / 4 refuters); refutation engine extracted to `_shared/`. Pending: Phase 7 e2e (user-driven HARD GATE).
- `22-VERIFY-COMMAND-REDESIGN-PLAN.md` — Phases 0–7 SHIPPED 2026-06-17. `/verify` owns the verdict (AC conformance + mechanical checks → APPROVED / NEEDS WORK / REJECTED); `_shared/feature_scope.py` extracted. Pending: Phase 8 e2e (user-driven HARD GATE).
- `23-ADVERSARIAL-GRILLING-PLAN.md` — Phases 0–6 SHIPPED 2026-06-17. `/grill` (`devils-advocate`, roster 18) + architect-excluded refutation + 4-way disposition + ReEntrySeed. **Its opt-in stance was AMENDED by plan 85 (2026-08-26): the run is now required before `/devforge:breakdown`. The amendment NARROWS — the user still owns every non-clean verdict, all four dispositions survive incl. KILL, the cross-pick and re-entry arms are unchanged, and what became mandatory is that the grill RAN, never that its disposition binds. Plan 23's own file still carries the opt-in framing and is owed an in-place narrow-reading amendment.** Pending: Phase 7 e2e (deferred user-driven HARD GATE). Note: the `/research`+`/discover` seed-consumer blocks commit alongside the uncommitted plan-18 intake-gate work.
- `24-SUMMARIZE-COMMAND-REDESIGN-PLAN.md` — ✅ DONE 2026-06-18 (`8f978e1`); e2e VALIDATED. `/summarize` agent-free PR-ready synthesis; no verdict; read-only on inputs.
- `25-FINALIZE-COMMAND-REDESIGN-PLAN.md` — ✅ DONE 2026-06-19; e2e VALIDATED. `/finalize` terminal PR-prep: tech-writer surgical docs + confirmation-gated squash; wrapper dual squash with a traceless source repo (D5).
- `26-REINTRODUCE-FIX-PLAN.md` — DONE (build) 2026-06-19; Phase 5 e2e WAIVED by maintainer (NOT executed — build-verified only). `/fix` proposal-only gated remediation, thin caller over `/implement`'s back-half. Supersedes plan 21's D1 ONLY.
- `28-GENERATE-DOCS-INCREMENTAL-MODE-PLAN.md` — CLOSED 2026-08-07, OUTDATED — do not build (cost addressed by cbm_sync `detect_changes` + `source_stamp` skip).
- `32-GENERATE-DOCS-GREENFIELD-LANE-PLAN.md` — CLOSED 2026-08-07, OUTDATED — never ratified, do not build; stub-passes-the-gate stays the accepted greenfield workaround.
- `33-FINALIZE-STAGES-SPEC-ARTIFACTS-PLAN.md` — SUPERSEDED by plan 37 (per-step commits replace finalize-only). Never started.
- `34-VERIFY-HYGIENE-FALSE-POSITIVE-PLAN.md` — BUILD DONE 2026-06-23 (`0be91f0`). Hygiene scans file-gated (denylist) + demoted to ADVISORY (never blocks the verdict); ac-verifier rule 13. Pending: testForge20 e2e (reinstall, user-driven).
- `35-GRILL-REFUTER-ROUTING-FIX-PLAN.md` — ✅ CLOSED 2026-06-24 (`06b78be`); e2e deferred (empirically validated in-session). `/grill` PHASE 4 now composes present refuters so routing actually crosses authors.
- `36-GRILL-UNIVERSAL-REENTRY-PLAN.md` — ✅ CLOSED 2026-06-24 (`06b78be`); e2e deferred. `/plan` = 4th ReEntrySeed consumer (REVISE-PLAN → `target_stage="plan"` → PHASE 0a.7).
- `39-GRILL-SEED-VERDICT-GATED-PLAN.md` — ✅ CLOSED 2026-06-24 (`a908e91`); e2e deferred. `write-seed` moved into PHASE 7's matching re-entry arm — no orphan seed on user override.
- `37-PER-STEP-ARTIFACT-COMMIT-PLAN.md` — ✅ DELIVERED 2026-08-18 — closed by maintainer statement (*"plan 37 delivered"*); that statement is the whole closure authority, not a fresh per-phase verification. Shared `artifact_helper commit-artifacts`; every pipeline command WIP-commits its own artifacts. Supersedes plan 33. The pre-closure per-phase notes are retained in the archive entry as history.
- `38-BREAKDOWN-AGENT-ROSTER-VALIDATION-PLAN.md` — Phases 1–5 SHIPPED 2026-06-24. `breakdown_helper verify-agent-roster` HARD gate (PHASE 3.5 + finalize chokepoint, fail-closed) + host/runtime-entrypoint assignment row. Pending: Phase 6 e2e (user-driven). Extends plan 14.
- `40-DESIGN-FIDELITY-FORCING-FUNCTION-PLAN.md` — Phases 0–8 SHIPPED 2026-06-24. Design fidelity made gate-blocking: constitution §3.8 + disposition manifest + `verify-design-tokens` + design-auditor wire-in. Runtime half + manifest model since REWORKED by plan 53 (the static Checks 1–4 gate survives). Pending: Phase 9 e2e (user-driven HARD GATE).
- `41-AGENT-EXECUTOR-REACHABILITY-PLAN.md` — WI 1–3 SHIPPED 2026-06-24. qa-engineer wired as executor; confirmed-Medium `/review` findings gate `/verify` (NEEDS WORK, never REJECTED); maintainer reachability gate (`scripts/lib/agent_reachability.py`). Gate mechanizes type-1 orphans ONLY. Pending: Phase 5 e2e (user-driven).
- `42-DESIGN-MANIFEST-TRIGGER-FORCING-FUNCTION-PLAN.md` — ✅ DONE 2026-06-25 (`f0a9e95`); Phase 6 e2e deferred. `verify-manifest-present` 4th PHASE 3.5 gate + finalize chokepoint + WARN tripwires (since retargeted to the plan-53 binding).
- `45-SEAM-TRUST-PROPAGATION-PLAN.md` — Steps 1–3 SHIPPED 2026-06-27; no pending steps listed. Seam E `correctness_vetted` caveat; Seam A spacing check re-anchored on reference `data-ref`s. `REGRESSION-ANCHORS.md` shipped.
- `48-REVIEW-MANDATORY-GATE-PLAN.md` — SHELVED 2026-06-30 — keep the warning, NOT built. Revival trigger: an OBSERVED `/review` skip; proportionate first cut = flip `verify/main.md:153` into a halt (alt-(a)), not the full verb.
- `49-DEVFORGE-RUNTIME-STATE-DISPOSITION-PLAN.md` — Phases 0–5 SHIPPED 2026-07-04. `.devforge/` 3-class disposition + `devforge.gitignore` template + shared migration script (kills the dead session-state ignore line). Pending: Phase 6 consumer e2e (user-driven). Version bump deferred.
- `53-DESIGN-ANCHOR-FIRST-CLASS-PLAN.md` — Phases 1–8 SHIPPED 2026-07. `design_anchor` first-class (anchor vs binding split; two-tier auditor engine — Python decides, JS measures). SUPERSEDES plan 52 (do not build it); retires the data-ref/disposition model + `_design_tokens` Check 5. Figma = fast-follow sibling. Pending: Phase 9 e2e (user-driven HARD GATE).
- `56-DEVFORGE-CODE-GITIGNORE-PLAN.md` — Phases 1–4 DONE 2026-07-10. `.devforge/lib|bin|templates` gitignored + untracked (CODE class, the node_modules model); `.devforge/template/` SINGULAR deliberately stays tracked. Pending: Phase 5 consumer e2e (user-driven). Version bump deferred.
- `62-SMT-REQUIREMENTS-CONSISTENCY-PLAN.md` — Phases 1–7 SHIPPED 2026-07-15/16. Opt-in ADVISORY `/spec-check` (Z3 AC-consistency; `spec-formalizer`, roster 19; fixed 2-pass quorum; verdict-gated REVISE-SPEC seed). NEVER strengthen into a gate (D14) — **AMENDED 2026-08-19 by plan 82's ratified D6: run-mandatory (`/devforge:plan` blocks on a fresh report), and the VERDICT never binds; the amendment block under D14 carries the narrow reading and the standing "no verdict condition, no override flag, no skip arm" constraint**. Remaining per the entry: Phase 8 (docs — written as "this pass") + Phase 9 e2e (user-driven HARD GATE).
- `65-STATUS-FLIP-ARTIFACT-COMMIT-PLAN.md` — Phases 0–4 DONE 2026-07-17. Upstream `**Status**:` flips now ride the flipping command's own artifact commit (`/plan` + `/breakdown` + `/verify` conditional). Pending: Phase 5 consumer e2e (user-driven HARD GATE). Extends plan 37.
- `66-PROPERTY-BASED-TESTING-AND-NARROWING-RULE-PLAN.md` — Phases 0–4 SHIPPED 2026-08-03. Constitution §3.6 Narrowing rule + architect-named pure-builder property-test lane + 5th PHASE 3.5 gate `verify-property-coverage`. Phase 5 deliberately ABSENT (gated on plan-62 IR persistence). Pending: Phase 6 e2e (user-driven HARD GATE).
- `67-CALLER-ENUMERATION-GATE-MODE-DECOUPLE-PLAN.md` — Phases 0–3 SHIPPED 2026-08-04; CLOSED pending opportunistic e2e (deferred, unit-covered). Check 8 caller-enumeration made mode-independent (+ auditable justification escape); enumeration rides the research handoff as `PlanSeeds.caller_enumeration`. ⚠ The 2026-07-31 solution-comparison doc STILL EXISTS at repo root and its FILENAME is a leak surface — disposition = delete at v2 go-live (plan 78); the orphan-branch migration is recorded in no repo file.
- `68-INTAKE-OWNS-FEATURE-DIR-PLAN.md` — Phases 0–6 DONE 2026-08-05/06 (`4bd613c`..`0e25e3a` + docs). Feature dir + branch born at intake finalize; `/specify` resolves instead of allocating; CLEAN CUT — no legacy readers (D3); D10 two-arm pending predicate. Remaining: Phase 7 (full suite + cross-ref sweep + install ride) and Phase 8 consumer e2e (user-driven HARD GATE).
- `64-SPEC-CHECK-LATEST-SPEC-RESOLUTION-PLAN.md` — ✅ DONE 2026-08-06 (`4099006`); Phase 4 e2e deferred. `/spec-check` + `/grill` no-arg runs resolve the most-recently-modified consumed artifact (mtime), matching `/plan`.
- `69-CALLER-ENUM-RESIDUAL-HARDENING-PLAN.md` — ✅ DONE, CLOSED 2026-08-06 (`bc23b41`..`d5088b9`); Phase 5 e2e deferred. WI-A–F: declared-caller-total (check 9 exact match), per-caller scope classification (check 19 + Step 2b), surface-count frame, mode-independent widenings (checks 8b/13/14/18), Narrowing symmetry on 3 surfaces (read D3's correction note before chasing `src/CLAUDE.md:85`), annotated docs.
- `71-POST-CHANGE-CONSEQUENCE-PLAN.md` — Phases 0–6 SHIPPED 2026-08-06. Change-induced dead-code MUST lane: architect predicts kills → `DeadCodeRow` carry → owning-task `**Dead code removal**:` field + 6th gate `verify-dead-code-coverage` → `/verify` `check-dead-code-removal` (NEEDS-WORK blocker). MAY lane stays advisory, never a gate. Pending: Phase 7 e2e (user-driven HARD GATE).
- `63-SKILL-COLLISION-SUPPRESSION-PLAN.md` — ✅ DONE 2026-08-07; Phase 6 consumer e2e deferred (test after release: on agreement to "verify" the model must invoke `/devforge:verify`; no phantom menu entries). Commands emit to `.claude/commands/devforge/` (`/devforge:<name>`); 13 model-invocable / 7 human-typed-only; references → `.devforge/command-refs/`; installers prune the stale flat layout.
- `70-PIPELINE-WALLCLOCK-PROFILING-PLAN.md` — ✅ DONE (build) 2026-08-08. Consumer-shipped `profile_helper` transcript profiler (per-command wall-clock split, exact sum-to-wall). Phase 2 real-run diagnosis deferred post-release (confirm post-63 segmentation on the FIRST post-release transcript first); Phase 3 opens the optimization plan FROM THE NUMBERS only.
- `72-UPDATE-SH-REPAIR-GUARD-PLAN.md` — ✅ DONE 2026-08-12 (closed by maintainer; Phase 4 reproduction passed mechanically — no pending e2e). `update.sh` lib-only repair sentinel → `FORCE=true`; non-tty clean exit-0 no-op; `bin/` templateOwned + chmod; `install.sh` stamps `.claude/template-version`. Closes plan 56 OQ-1 on the update path ONLY.
- `73-LITERAL-ARCHAEOLOGY-EVIDENCE-ARM-PLAN.md` — Phases 0–6 SHIPPED 2026-08-12; Phase 7 consumer e2e WAIVED 2026-08-13 (build-verified, NOT consumer-validated; no regression anchor was added). Archaeology mode gate dropped (D1); literal-as-evidence trigger arm (3) + per-intent EVIDENCE recovery block (D2/D3); `--rests-on-literal` + mode-independent check 20 (D4) + `--use` (OQ-5) + `classify-finding-literal`; evidence-lane declaration (D7 — the finalize guard is a call-happened check, NEVER per-lane-value); `/discover` absence probes (D6); `/plan` carry (Phase 5). Code committed (`bac6008`); the plan DOCUMENT is deliberately UNTRACKED (private-client identifiers; this repo is PUBLIC). ⚠ EIGHT items remain OPEN — read the plan's `## Context for next session` + `## Non-goals` before touching anything nearby; its `## Build discipline` section is organized by working moment.
- `74-MEMORY-LANE-INTEGRITY-PLAN.md` — ✅ DONE (build) 2026-08-13 (7 commits `e3b63cb`..`ac2a1e0`); Phase 7 consumer e2e DEFERRED — maintainer intends to run it (NOT waived; build-verified, NOT consumer-validated; best retained recipe = the known-answer intake regression anchor). All four mechanisms closed: `_shared/memory.py` single-owner resolver + three-state probe; dead-path sweep (`/review`+`/audit` now read real memory — a behavior FIX); falsifiable `/specify` gate (`absent`/`stub` PASS); intake wire-in; 13/7 disposition table + `verify-memory-lane` gate (Rule 2 CONJUNCTIVE); `install.sh` memory preserve-by-copy (OQ-5 data-loss fix). Plan DOCUMENT deliberately UNTRACKED (private-client identifiers). Follow-ups: two CLOSED by `f2b0366` (project-config preserve; trace-log test premise), two OPEN (six preflights drop `memory_state`; architect Rule 8 double-read route).
- `75-INVESTIGATION-SEARCH-HARNESS-PLAN.md` — ✅ DONE (build) 2026-08-14; Phase 5 consumer e2e DEFERRED — maintainer intends to run it (NOT waived; per D1 it is the ONLY test that the harness noticed the right thing — a known-answer anchor from the originating A/B). Phase 1 BUILT (archaeology sweep widened past the introducing commit: step 4 `git log <sha>..HEAD` over the file + enumerated inbound callers; `--supply-changing-commits` with three states; two pre-existing bugs fixed); Phase 2 CUT (blast radius + anchoring, NOT evidence count — revival needs a scoped trigger or a second observed incident); Phase 3 (D6 lane vocabulary) DEFERRED. Plan DOCUMENT deliberately UNTRACKED (private-client identifiers). D1 spine: search steps, NOT gates — tripwire: no new check number AND no new unnumbered hard-fail validator. Open item 1 CLOSED by `1342ab4` (duty-before-permission reordering; surfaced a NEW open item — the fix-layer twin of the `deliberate` arm); items 2–5 OPEN (permissive-SHA shape in `_cmds_absence.py`; check 9 zero-floor mismatch; no shared SHA accessor; no cross-render field-coverage check).
- `77-POST-CHANGE-OUTPUT-MATRIX-PLAN.md` — DONE (build) 2026-08-15: Phase 0 RATIFIED (all recommendations confirmed; OQ-3 via A1's sibling-file carrier), Phase 2 SHIPPED instruction-only (emission matrix `specs/<dirname>/emission-matrix.md` — produced at `/devforge:research`, consumed/verified at `/devforge:plan`; `/specify` quoted-product-intent AC directive; architect Rule 9; `src/CLAUDE.md` tree line; zero Python, D4 honoured), Phases 1 and 3 WAIVED (NOT deferred) — no blind baseline, no handling probe; D5 overridden, D6 retired. ⚠ The mechanism rests on reasoning, NOT evidence — never read shipped code here as validated. Verdicts are `affected`/`unaffected` only; plan 71's dead-code chain is fed unchanged (enumeration-to-obligation). Consequence: the §3.6 FALSE-BINARY finding (`FINDINGS.md`) is NOT mooted. Evidence file `77-EVIDENCE-DISCOVERY-TO-LOCK-INVERSION.md` is UNTRACKED private-client evidence — never commit or quote it into a tracked file. Known residuals recorded in the archive entry. **AMENDED 2026-08-18 (plan 81 D3, ratified): disposition = KEEP AND AMEND, and plan 81 is the amendment.** An external 20-run, 6-framework benchmark effectively ran this plan’s Phase-3 ARM (a matrix-present run, no Phase-1 blind baseline): the matrix is vindicated ON DISCOVERY — it named the coupled call site 16 of 20 runs never named — while the handling probe FAILED through a chain of wrong INFERENCES from correctly-computed facts (plan 81’s subject). **Bounded reading is mandatory: an "after" with no "before" — consistent with the matrix working, NOT a demonstration that it did** (this plan’s own OQ-6 bound; one run separates nothing). Under the retired D6 a non-flipping probe was the delete branch; the evidence supports keep-and-amend because discovery value is now measured and the failure localizes to named inference steps, which plan 81’s six rules address. Nothing in plan 77’s file is edited and no sentence of this entry is withdrawn.
- `78-TEST-IDENTIFIER-SCRUB-PLAN.md` — NOT STARTED — awaiting Phase-0 ratification (D1–D8 + OQ-1–6 unanswered). Two carve-outs already ratified/applied: plan docs are DELETED at v2 go-live, not scrubbed; one substitution pass applied (suite green, a second in flight). Scope `tests/` ONLY; out-of-repo real-identifier inventory + in-repo synthetic allowlist (D3 inversion); shape-based guard catches SHA/ticket/path classes, structurally MISSES name classes; name-shaped arm ADVISORY — do not strengthen without Phase 4's false-positive data.
- `79-MEMORY-WINDOW-AND-RECEIPT-PLAN.md` — ✅ DONE (build) 2026-08-18; Phase 6 consumer e2e DEFERRED — maintainer intends to run it (NOT waived; build-verified, NOT consumer-validated). `.devforge/memory.md` had two writers appending at EOF and every reader reading the top, so past ~25 accumulated lines the window froze on the stub preamble plus the oldest per-task receipts and no lesson written later was ever visible again. Four moves: (1) section-aware excerpt in `_shared/memory.py` — `## ` sections parsed, installer title dropped, `## Task Outcomes` dropped by NAMED DENYLIST (unknown sections INCLUDED by default), content-free sections dropped heading and all, equal per-section share of a 120-line budget with unused share redistributed once, NEWEST entries survive a cut, every cut DECLARED by a marker under the heading; the `present`/`memory_state`/`excerpt` contract is unchanged, so all eleven `read_memory_context` consumers inherit it. (2) `/devforge:implement`'s per-task receipt writer DELETED (its memory READ stays; `--last-task-number`/`--last-task-title` removed in the same commit as the `main.md` arguments that fed them). (3) `/devforge:verify` PHASE 7 now targets a NAMED bucket (`## What Failed`/`## What Worked`/`## Known Pitfalls`; first matching rubric line wins, heading CREATED when absent, no EOF-append path) — without it the lesson lands in whatever section is last. (4) `/devforge:implement`'s preflight migrated onto that same excerpt (emitted key `memory_digest` → `memory_excerpt`, and the single read brought `memory_present` + `memory_state` with it, so `main.md`'s contract line went six keys → EIGHT) and `read_memory_digest` DELETED, incl. its name in `scripts/lib/memory_lane.py`'s read-call regex; `references/agent-brief.md`'s direct-file-read paragraph KEPT for SELECTION (the orchestrator holds the task contract and picks the entries bearing on this task's `touched_files`), only its now-false digest-emptiness sentence removed. All D/OQ ratified 2026-08-17: D3=denylist (Option A), D4=marker inline in the excerpt string, D5=recency within a section, D6=probe UNTOUCHED (a receipts-only file still probes `populated` while its excerpt renders empty — accepted divergence), D7=migrate to `read_memory_context` and KEEP `agent-brief.md`'s selection paragraph, OQ-1=120-line budget + equal per-section share, OQ-2=delete `read_memory_digest` (executed at Phase 5, incl. removing its name from `scripts/lib/memory_lane.py`'s regex alternation), OQ-3=YES (Phase 4.5's build arm), OQ-4=back-fill is a NON-GOAL, OQ-5=remove the two flags. ⚠ No back-fill: on an install that already ran tasks, lessons sitting inside `## Task Outcomes` stay excluded until an operator moves them out by hand — a Phase-6 failure there is the ratified design, not a defect. Build-verified, NOT consumer-validated.
- `80-CONSTITUTE-CITATION-FALSE-POSITIVE-PLAN.md` — ✅ DONE (build) 2026-08-18 (Phases 0–4; `6896398`/`e74d8df`/`e823da7` + docs); Phase 5 consumer e2e DEFERRED — maintainer intends to run it (NOT waived; user-driven HARD GATE — known-answer anchor: the `composite=0.7955` consumer run, re-run with ZERO constitution-content edits). `/constitute` Dim-2 citation false-positive fix: 4 token filters (placeholder / extension-only / leading-slash / segment-artifact) applied so a filtered token counts in neither numerator nor denominator, plus a `filtered_items` key on `validate`'s stdout JSON so nothing is dropped silently; bounded recursive resolution in BOTH modes (11-name directory denylist — a wrapper token resolvable only via `node_modules` stops resolving, accepted); `.devforge/` runtime-namespace allowance (D6 namespace-wide — hallucinated citations inside that namespace resolve silently, accepted, and a test pins it); F1 leading-slash latent bug (`Path(install_root) / "/x.md"` checked the filesystem root) closed by filter (c) excluding rather than repairing. Thresholds + weights UNCHANGED (D1), producer prose untouched (D7), the Phase 6.2 ship/cancel/fix human gate unchanged.
- `81-INFERENCE-RULES-PLAN.md` — ✅ Phases 0–6 DONE 2026-08-18 (Phase 0 ratified all ten items (a)–(j) as recommended; builds `9e4757a`/`9a3300b`/`af16cb3`/`59b5152`/`92eadfb`/`fe7329b`). Six instruction-only inference rules over facts the pipeline already computes — NOT another verification layer: F4 `--surface` labels derived from construction sites (research Step 2b + matrix step 1); F1 identical-REACHABLE-tuple ⇒ one caller population / AC-conflict escalated per architect Rule 6 (plan Rule 5 note + architect Rule 9); F2 value sets not expressions (matrix Rule 2 + both Phase-2 sites widened in place); F3 §5.2 preservation ACs admitted only on a cited construction site, keyed on the `behavior_preservation` ROLE not phrasing (specify Step 4.4; owns FINDINGS.md finding 4 per D4); F5 behavioral ACs not dischargeable by file-layer evidence (plan PHASE 2.5 step 2 + PHASE 1.5 marker carve-out; ac-verifier Rule 14 — OQ-3 seam decided: agent rule); F6 grill RE-ENTER-UPSTREAM attribution traces to the INTRODUCING artifact — `research-handoff.json`/`discover-handoff.json` mandatory trace inputs, `spec` introduces only with no upstream source (grill PHASE 5 Q2, one-sentence widening + input pointer). Zero Python, zero new checks/validators — the tripwire held in every phase's diff. D3 = plan-77 KEEP AND AMEND with its no-baseline bound (an "after" with no "before"); D5 = NO constitution edit — the FALSE BINARY finding stays OPEN, F1 a candidate route only after Phase 7; `src/CLAUDE.md` untouched — deliberate no-op, no user-visible command contract changed (verified against its `/devforge:grill` catalog entry: seed schema/routing/consumers unchanged). Phase 7 consumer e2e DEFERRED 2026-08-18 — maintainer intends to run it (NOT waived; build-verified, NOT consumer-validated): frozen benchmark prompt, model PINNED `claude-fable-5`; pass = the hidden surface stops emitting the removed values WITHOUT the prompt naming it and WITHOUT that surface's file in the diff; the two artifact greps + F6's separate grill known-answer anchor are MANUAL observations, never gates; one run attributes nothing to any single fix. ⚠ Evidence file `81-EVIDENCE-V2-BENCHMARK-RUN.md` at repo root stays UNTRACKED private-client evidence — never commit or quote it into a tracked file. Created 2026-08-17; **AMENDED 2026-08-18 to add F6 (pre-ratification; no new D-number or OQ-number — F6 ratifies as Phase-0 item (j))**. Inference rules over facts the pipeline already computes — NOT another verification layer: six instruction-only fixes (F4 derive surface labels from construction sites; F1 identical-reachable-tuple ⇒ same-lane-or-AC-conflict; F2 value sets not expressions; F3 preservation-AC admission — D4 proposes it OWN the AC-pins-removed-value finding in `FINDINGS.md`; F5 no file-layer discharge of behavioral ACs; **F6 attribution traces to INTRODUCTION, not restatement** — `/devforge:grill` PHASE 5 Q2's RE-ENTER-UPSTREAM arm gains the feature's `research-handoff.json` + `discover-handoff.json` as MANDATORY trace inputs, so `spec` is the introducing stage only when the conclusion has no upstream source; origin = a SECOND observed incident 2026-08-18 (maintainer-reported consumer run, NOT the benchmark, no artifact in this repo); single-site widening of `src/commands/grill/main.md`'s nearest-introducing-stage sentence at new Phase 5b, letter-suffixed so Phases 6/7 keep their cited numbers; the `research` route already exists at `src/commands/research/main.md` Phase 0.6 and F6 does NOT build it); zero Python, zero new validators (plan 75's tripwire, both halves). Origin of F1–F5 and of the plan itself: the 20-run benchmark where v2's best-in-set run still FAILED the handling probe via a seven-step chain from ONE intake mislabel. D3: plan 77 is KEEP AND AMEND (matrix vindicated on discovery; an "after" with no "before" — not a demonstration it worked). D5: NO constitution edit; the FALSE BINARY finding stays open — its "no designated route" statement STANDS until F1 is ratified AND Phase-7-validated. Phase 7 = user-driven HARD GATE (frozen prompt, model PINNED — fable-5 vs opus-5 is a live confound). ⚠ Evidence file `81-EVIDENCE-V2-BENCHMARK-RUN.md` at repo root is UNTRACKED private-client evidence — never commit or quote it into a tracked file (an `81-*` glob returns exactly two files — this plan and the evidence file; the unrelated plan that briefly shared the number is now `83-DOWNSTREAM-REENTRY-SEED-PLAN.md`, so that collision is resolved).
- `82-SPEC-CHECK-SUBJECT-RESOLUTION-MANDATORY-PLAN.md` — **✅ DONE (build) 2026-08-19 (Phases 0–6; commits `55fa1ab` / `b6d834b` / `e1ba862` / `5d56a47` / `45bc760` / `5b73269` / `24256c5`; Phase 0 ratified every item — D1–D8 + OQ-2–OQ-3 — with the three forks picked explicitly). Phase 7 consumer e2e DEFERRED 2026-08-20 — maintainer intends to run it (NOT waived; build-verified, NOT consumer-validated; the five known-answer cases are the anchor).** ⚠ Expect on that run, by design and not as a defect: a `spec-check.md` written BEFORE this build carries no `**Spec hash**:` line, so the gate legitimately blocks until `/devforge:spec-check` is re-run for that spec. Phase 6 delivered: plan 62's D14 amended IN PLACE (narrow reading + the four narrowing mechanisms + what did NOT change), the `src/` sweep (`spec-check/main.md` description + body + Rule 1, `src/CLAUDE.md` chain/catalog/gate row, `specify/main.md`, `spec-formalizer.md`, `storage-rules.md`), `FINDINGS.md` finding 4's further additive amendment, plan-81 reconciliation notes, and the `CHANGELOG.md` entry. Created 2026-08-17; OQ-1 was RESOLVED 2026-08-17 and ratified nothing else. Subject resolution in the `/devforge:spec-check` formalizer: every AC subject must resolve to a construction site, `UNRESOLVED-SUBJECT` on failure (not formalized, not counted as `formalized` or as a skip — a fourth `COVERAGE_STATUSES` value + its own ledger term); then mandatory + auto-accept-clean, REVERSING plan 62 D14 **BY EXPLICIT MAINTAINER DIRECTIVE** — **D6 RATIFIED 2026-08-19 WITH the narrow reading confirmed**, and D14 WAS amended in place at Phase 6 (2026-08-19), never silently. Narrow reading is load-bearing: mandatory means the check RAN and its report is FRESH, NEVER that its verdict binds (the gate checks presence + freshness only). **The three explicit forks, as ratified 2026-08-19: D5 = (a-ii)** — `/devforge:plan` gets the fail-closed preflight (a NEW `plan_helper` verb + a NEW PHASE 0a.x block modeled on `specify_helper find-handoffs --require`: exit 2, BLOCKED, no override flag), but `disable-model-invocation: true` STAYS on `/devforge:spec-check` and the blocked message names the command for the USER to type — so **plan 63's 13/7 carve-out is NOT reopened, NO description trim is owed, and this plan contributes NO count delta**; **D7 = (c)** fail closed on a missing z3 with the one-time `pip install z3-solver` message (plan 62 D10's stdlib-clean stance survives and `install.sh` is untouched; option (a) install-time z3 is the recorded fallback if Phase-7 friction proves too high; (b) degrade-to-warning stays REJECTED as an escape hatch); **D2 = the greenfield two-arm fork ADOPTED** (arm (a) a construction site in the EXISTING code, arm (b) the spec's own new-behavior declaration, **preservation ACs arm-(a)-only**, keyed on the `behavior_preservation` role plan 81's F3 shipped at `/devforge:specify` Step 4.4 — arm (b) stays a self-attestation escape that narrows the dodge surface without closing it, accepted as the recorded bound rather than argued away). **OQ-2 = the CONTENT HASH of `spec.md`, recorded in `spec-check.md` at render time and re-hashed by the gate** — not mtime, not the sibling-JSON stamp — **ratified identically with plan 85's D4 (the CARRIER differs, the PREDICATE does not); plan 85's Phase 0 now owes the reciprocal naming.** **OQ-3 = the per-subject search is bounded explicitly in the emitted instruction**, the exact bound chosen at Phase 2 and the observed false-UNRESOLVED rate recorded at Phase 7 (the bound and that rate are one dial). ⚠ **OQ-1 RESOLVED 2026-08-17** — the maintainer's *"i decided to make grill mandatory"* is a FRESH decision that has landed NOWHERE in the repo (grill is still `disable-model-invocation: true`, its Rule 1 still "Opt-in, never an auto-gate"; plan 48 SHELVED; the only grill+mandatory hit still says "not a mandatory gate" — **all three clauses were true as dated; as of plan 85's 2026-08-26 build only the `disable-model-invocation` one still is: Rule 1 now reads "Mandatory to RUN, never binding in its VERDICT" and the "not a mandatory gate" catalog bullet is gone**), so D6 inherited no prior art and was RATIFIED 2026-08-19 as first-of-its-kind on the maintainer's own directive, with no precedent cited and none available. A sibling grill-mandatory change IS intended and has its OWN plan — `85-GRILL-MANDATORY-AUTO-ACCEPT-PLAN.md` (created 2026-08-17, NOT STARTED; recorded here 2026-08-19) — and cannot ride along: grill is soft all the way down (LLM findings through the `_shared/` refutation engine, no Z3-analog), so its clean-verdict analog is the weaker "no finding survived refutation"; 4-way disposition and a different pipeline seat (`/devforge:plan` → `/devforge:breakdown`); full finder/refuter fan-out on every feature with no cheap deterministic core, so that plan's Phase 0 owes a wall-clock cost line (cf. plan 70). Only the interrupt-only-on-findings SHAPE transfers. D8: defence in depth with plan 81's F3, NOT supersession — plan 81 states it proposes no change to this command (`:129`, `:201` — digits refreshed 2026-08-19 after plan 81's Phase-0 ratification records shifted them again; grep the quoted claims, not the digits), and Phase 6 RECONCILED it 2026-08-19 with dated notes at both sites plus its plan-62 dependency line, stating the split **F3 = admission at authoring; plan 82 = refusal at formalization**. **D6 was NOT declined, so the separability arm is DISCHARGED: all six build phases stand, not just 1–4.** Phase 7 = user-driven HARD GATE (phantom / clean / greenfield / gate / z3-absent), NOT yet run. Verified build costs: `plan_helper` has NO preflight verb and `/devforge:plan` has NO mechanical gate at all; the formalizer agent is already tool-capable (`Read, Grep, Glob`) but is instructed *"from that input alone"* — an instruction change, no tools change.
- `83-DOWNSTREAM-REENTRY-SEED-PLAN.md` — **✅ DONE (build) 2026-08-20 (Phases 0–5; commits `c453929` Phase 0 ratification / `0734697` Phases 1+4 / `70db52b` Phase 2 / `2ca7b34` Phase 3, plus the Phase-5 docs commit this entry rides in). Phase 6 consumer e2e DEFERRED 2026-08-20 — user-driven HARD GATE, NOT run (build-verified, NOT consumer-validated; the known-answer anchor is the plan's own Phase-6 procedure — a scope-change bounce that TODAY ends the turn and leaves nothing behind must write the seed, and `/devforge:specify` must announce re-entry naming the seed's `source` + `feature` and how the run addresses `must_satisfy`).** Created 2026-08-17; **numbered 83, renumbered from `81-` — the old `81-DOWNSTREAM-*` name no longer exists** (the collision plan 81's entry below records as resolved). `/devforge:fix` becomes the THIRD `ReEntrySeed` producer, and the first downstream of `/devforge:plan`: its PHASE-1 scope-change bounce already named WHICH item and WHY and then discarded that diagnosis, so the re-run re-derived it. **Phase 0 ratified every item — D1–D6 + OQ-1–OQ-6 — on 2026-08-20**, most as recommended, two divergences called out in the plan's own status line: **D1** = one new source token `"fix"`, `/devforge:fix` the SOLE producer (the counter-argument is ACCEPTED, not answered — a plan proven wrong mid-`/devforge:implement` has no route to `/devforge:fix` until the tasks drain, so this covers the diagnosis moment and misses the discovery moment); **D2** = the `spec` and `plan` routes only, **NO `breakdown` target stage** (its real cost is a new consumer block + its reachability gate + a three-way triage discriminator that does not exist — the PHASE-1 triage is a flat binary — and shipping the enum member without it would orphan the target stage), and ⚠ **the `plan` route ships UN-EXERCISED this build**: the widened glob is deliberate consumer future-proofing, but the flat-binary bounce emits `target_stage="spec"` only, so no producer reaches that route and Phase 6 does not validate it; **D3** = verdict-gated three-arm `AskUserQuestion` at the bounce (`re-enter specify` recommended / `drop and re-run` / `stop`; no authored "Other" — the tool injects it), the seed written ONLY on the matching pick, because `/devforge:specify` treats a seed as a binding directive and an unratified one is the orphan plan 39 exists to prevent; **D4** = `specs/[feature]/fix-seed.json` (the `<source>-seed.json` convention — what makes `/devforge:specify`'s two glob call-sites see it with zero code change, and what mechanically FORCES OQ-4's option (i)); **D5** = a new `_fix/_seed.py` + a `write-seed` verb through `_fix/_cli.py`'s documented extension point (helper-owns-shape, so an empty required field exits 2 instead of shipping an invalid directive; the thin-caller asymmetry is ACCEPTED as recorded, not refuted); **D6** = no `scope_grade` field — `target_stage` carries the gradation. OQ answers: **OQ-1** the bounce still ends the turn after writing (naming the four-command cycle; `/devforge:fix` never runs `/devforge:specify` itself); **OQ-2** `cycle_count` = 1 unless this run itself re-entered from a prior seed, and NO cap logic; **OQ-3** de-enumerate the consumer prose (read the `source` field) with **exactly ONE illustrative value list kept**, at `specify/main.md`'s `source`-field bullet; **OQ-4**'s three sub-cases — the `conversational (in-window user report; no report file)` provenance literal, the evidence-quoting rule for a written finding (`invalidating_evidence` MUST quote that finding's own `evidence` string plus the one-line classification reason; the bare classification judgment is permitted ONLY for the conversational case), and option **(i)** ONE seed for a multi-item bounce with per-item reasoning in `carried_findings`, forced by D4's fixed filename rather than preferred; **OQ-5** no — `/devforge:review` / `/devforge:verify` do NOT become producers; **OQ-6** no producer-side union of a sibling seed's `carried_findings` (the cross-cycle compounding is left unaddressed and recorded as such). Two deferrals recorded, neither fixed: the shared `--feature` argparse asymmetry across the `_fix` / `_spec_check` CLIs (error-message SHAPE only — functionally exit-2 correct — deliberately NOT fixed, since touching `_spec_check` is a plan non-goal), and the project-wide `/devforge:plan` PHASE 0a.7 glob's cross-feature misbinding window (a pre-existing divergence from `/devforge:specify`'s deliberately dir-scoped glob, now recorded in that block rather than repaired). **Plan-85 coordination rule, still live: whichever of plans 83 and 85 ships SECOND reads the LIVE `specify/main.md:154`-area opt-in sentence and re-derives its rewrite from what it finds — never a pre-computed one.** Plan 83 shipped first, so plan 85 owes that live read. Untouched by decision: `/devforge:grill` and `/devforge:spec-check` behavior (the pattern being mirrored), seed lifecycle/deletion, and any mechanical check over seed contents.
- `84-ARCHITECT-CONSULT-ACCUMULATION-PLAN.md` — ✅ CLOSED — NO CHANGE 2026-08-21 (created 2026-08-17; Phase 0 ratified the same day it closed — D1 with its D1a tie-break, D2–D5 and OQ-1–OQ-3 all resolved inline). D1 = Option B (static + artifact-derived **U**, the count of the five conditional architect-consult sub-questions a feature left unexercised) with its pre-committed threshold, MOOT in execution because OQ-1's answer fired the `insufficient sample` arm first — the 2026-08-21 machine-wide sweep found testForge20 with 1 completed `specs/*/plan.md`, jira-clone with 1 and nothing else with any, so no install reaches the required ≥ 5, no U was ever computed, and Phases 1–4 correctly never ran (their non-execution IS the close, not an open item); D1a = a 2.5 median resolves to close. One ratified carve-out shipped the same day — D4 = Option A separable, EXERCISED via the cheaper sub-option: `src/agents/architect.md:153` Rule 9's heading `9. **Minimal scope.**` → `9. **Minimal scope & the six decision-recording forcing steps.**`, EXTENDED not replaced because `src/agents-AUTHORING.md`'s Rules-section closers fix `Minimal scope` as the canonical closer name; number kept, all six bold forcing steps byte-identical, zero citations touched (the five `plan/main.md` sites plus `architect.md:85` cite by rule number or by step name, never by heading text). **That heading is the ONLY `src/` diff this plan produced.** D2 = Option A, the two surfaces decided as two problems, with the brief↔Rule 9 duplication (sq7↔Narrowing, sq9↔Consequence, sq11↔Emission-matrix) explicitly OUT of scope and recorded as a known residual; D3 = the candidate set {(a), (b), (c), (d)} plus the sequencing rule ratified as drafted, then resolved by the close to candidate **(d) do nothing** — any future remedy requires RE-OPENING this plan with a fresh ≥ 5-feature sample and re-entering at Phase 1; D5 = Option A, the record lives in the plan file's own `## Phase 0 close record`; OQ-2 = `/devforge:breakdown`'s architect brief recorded as UNMEASURED, no symmetry assumed; OQ-3 = "no addition without eviction" recorded as a candidate convention only, nothing built.
- `85-GRILL-MANDATORY-AUTO-ACCEPT-PLAN.md` — **✅ DONE (build) 2026-08-26 (Phases 0–4 COMPLETE — the Phase-2 wiring gap recorded in an earlier revision of this entry was CLOSED by `1b6b27a`). Phase 0 CLOSED — every D-item and OQ ratified 2026-08-24/25 (`10cc8ec` D3+D4, `ad0cd50` the rest); phases reconciled `3f86a58`; Phases 1 / 2 (helper half only) / 2b / 3 / 4 BUILT (`56f7a74` / `58e01be` / `63262d3` / Phase 3 + the Phase-4 sweep this entry rides in). Phase 5 consumer e2e DEFERRED — user-driven HARD GATE, NOT run (build-verified, NOT consumer-validated; anchors = planted-defect / clean / stale-report, and the run is OBLIGATED to record measured wall-clock). **Deferred by maintainer decision 2026-08-26: the run happens ONCE, after the implementations queued behind this plan are done, rather than per-plan — so this is a BATCHING decision, not a waiver.**** `/devforge:grill` is mandatory: `/devforge:breakdown` refuses to decompose a plan until a grill has run for it, and a run nothing survived is accepted without a question. ⚠ **The wiring gap this entry once carried is CLOSED (`1b6b27a`).** `/devforge:breakdown` PHASE 0a.6 now invokes `breakdown_helper verify-grill-ran`, and PHASE 0a.5's exit arms route into it rather than past it — without that second edit the gate would have been textually skippable. Every "blocks until" sentence in the emitted text now describes observed wiring rather than a verb's contract. **Recorded rather than deleted**, because the way it happened generalizes: Phase 2 spanned two routes, only the Python half was dispatched, and the delivered half's honest "not yet called" docstring read as a calm fact rather than an alarm.  **no command invokes the gate today** and every "blocks until" sentence the Phase-4 sweep wrote into `src/CLAUDE.md` / `src/commands/grill/main.md` / `README.md` describes the gate's contract, not observed runtime behavior — wire the entry-side block (after breakdown PHASE 0a's plan resolution, before PHASE 0b's status flip; modeled on `/devforge:plan` PHASE 0a.8) before that claim is true. **The gate's ratified predicate reads presence + a recorded adversary status and NOTHING else:** `specs/<feature>/grill.md` exists AND `grill-state.json`'s `adversary_status` is `complete` or `clean` — never the disposition (a **KILL report satisfies it exactly as a PROCEED one does**), never freshness (a **stale report PASSES**), no `--force`, no `--skip`. **The amendment NARROWS and this list must survive every future edit:** the USER owns every non-clean verdict at PHASE 7; all four dispositions survive, KILL included; the cross-pick and re-entry arms are unchanged; `/devforge:grill` still never modifies `plan.md` or `spec.md`; **what became mandatory is that the grill RAN, never that its disposition binds.** ⚠ **Two ratifications DIVERGE from the plan's own recommendations and are argued at their decision: D3** (the fail-closed FRESHNESS gate is REJECTED — it penalized acting on the report's own findings, since ignoring a finding let you proceed while acting on one cost another full adversarial run, unboundedly; the gate now reads PRESENCE + recorded `adversary_status` only, one mandatory run per feature, re-runs never forced, staleness RECORDED not enforced) **and D2** (a 2-pass quorum IS built, using `/devforge:audit`'s UNION merge — explicitly NOT `/devforge:spec-check`'s MAJORITY rule, which would suppress single-pass findings and invert the purpose). **D1** = the STRICT clean predicate (`confirmed` + `contested` + `uncertain` all empty; `dismissed` allowed) — `disposition == PROCEED` is strictly WEAKER and was not used. **D4** = the plan hash rides the already-committed helper-owned `grill-state.json`, RECORDED for a human reader and enforced nowhere. **D5** = PHASE 7 is ENTERED on the clean arm with its human gate not firing, never skipped — PHASE 7 owns the ONLY `rm -rf "$WORKDIR"` sweep (Rule 10). **D6** = `disable-model-invocation: true` KEPT, so the counts were read live and are still **13 model-invocable / 7 human-typed-only** — this plan contributed NO delta to plan 63 and owed no `description` trim — but plan 63's OQ-2 keep-7 reason QUOTED the frontmatter closing sentence that WAS rewritten (`Opt-in — never an auto-gate.` → `Human-typed only — /devforge:breakdown requires that it RAN, never that its disposition binds.`), so that reason was AMENDED in place 2026-08-26 alongside plan 82's 2026-08-19 amendment of its other half; neither quoted description survives now, the KEEP decision stands under both. **D7 (the wall-clock cost line plan 82's OQ-1 says this plan owes in three tracked files)** = the cost is ACCEPTED: a full two-pass adversary + refutation fan-out on every feature with no cheap deterministic core, **no measured number exists yet** (plan 70 Phase 2 is deferred), Phase 5 must produce one for the two-pass shape with no single-pass baseline, and **on unacceptable cost the route is plan-70 optimization, NEVER gate dilution**. **D8**: two mandatory adversarial stages now sit in sequence (`/devforge:spec-check` then `/devforge:grill`) — the cost is the SUM. The strongest objection stays UNRESOLVED, not answered: an auto-accepted clean verdict can read as an endorsement, while a clean grill is the ABSENCE of a surviving attack. Plan 48 is NOT revived. **Phase-4 sweep, as executed:** `src/commands/grill/main.md` (frontmatter `description`, the overview + stance paragraphs, PHASE 0's preflight line, Rule 1), `src/CLAUDE.md` (chain de-bracketed → the bracket LEGEND DELETED because grill was the last bracketed step and zero remain; the human-typed-only sentence's `(opt-in)` → the obligation; the catalog bullet; the `#### /devforge:grill` entry, both modeled on the ratified `/devforge:spec-check` wording; a parallel Hard-Gates row), `src/commands/plan/main.md` (PHASE-4 stakes hint — the `never gates /devforge:breakdown` clause retired, the hint's own always-exit-0 advisory behavior unchanged), `src/commands/research/main.md` + `src/commands/discover/main.md` (seed-absence rationale re-derived off the human gate instead of off opt-in), the three pipeline-chain diagrams (`plan` / `breakdown` / `implement`), `README.md`, `DEVELOPMENT-STATUS.md`. **Plan 82's expiring grill clauses were retired CLAUSE BY CLAUSE in both ledger files** — the *"Rule 1 still 'Opt-in, never an auto-gate'"* and *"the only grill+mandatory hit still says 'not a mandatory gate'"* clauses are marked false-as-of-2026-08-26; the *"still `disable-model-invocation: true`"* clause is TRUE and stands. **NOT done by this phase and still owed:** `23-ADVERSARIAL-GRILLING-PLAN.md`'s in-place narrow-reading amendment (its index line above IS amended; the plan FILE is not), and `_plan/_stakes.py`'s rendered *"Consider running /devforge:grill"* hint text (Python, deliberately untouched — this phase was prose only). `src/devforge/storage-rules.md` deliberately untouched: D4's carrier introduced no new artifact. Created 2026-08-17; **numbered 85, not the 83 its brief named — `83-` and `84-` were already taken** (a `83-GRILL-*` file never existed); sibling of plan 82, same maintainer decision (*"i decided to make grill mandatory"*), independently ratified. **The pre-ratification draft record — including the reasons D2 and D3 were expected to go the other way — lives in the plan file; read it there, not from this line.** ⚠ **One drafting-time claim is now stale and is corrected here so it is not re-inherited:** the entry gate was called *"entirely NEW, not a strengthening"* on the ground that `src/commands/breakdown/` carried ZERO grill references — that observation was correct when written and the gate IS the first entry-side gate in that command, but it is no longer a statement about the current tree once the wiring above lands. Plan 63's coordination rule was honoured: the 13/7 counts were read LIVE, not derived from a remembered delta, and they did not move.
- `86-FOWLER-REFACTORING-GAPS-PLAN.md` — ✅ DONE (build) 2026-08-23 (Phases 0–6; commits `88832f3` Phase 0 / `d6f51be` Phase 1 / `aa03419` Phase 4 / `e0fb8e4` Phase 2 / `6b46678` Phase 3 / `e249898` Phase 5 — Phase 4 landed third, the phases being independent — plus the Phase-6 docs commit this entry rides in; Phase 0 ratified D2–D5 + OQ-1 on 2026-08-23, D1 + Phase 1 having been pre-ratified by maintainer directive 2026-08-18 and OQ-2 resolved from the code). Phase 7 consumer observation DEFERRED TO POST-RELEASE — user-driven HARD GATE, NOT run; maintainer stated 2026-08-23 they will run it after the release, so it is NOT WAIVED (build-verified, NOT consumer-validated). Created 2026-08-18. Five **instruction-only** closures of the gaps a Fowler-grounded evaluation of this framework's refactoring discipline found — **the same evaluation found the framework STRONG on behavior preservation, small steps, the Rule of Three (verbatim, `src/constitution.md:111`), the Long Function guideline and duplication hunts at feature + codebase scope, so this plan is the REMAINDER, not a verdict on the framework**: **F1** a function-level arm on `src/agents/code-reviewer.md`'s check 8 — a near-verbatim copy of ONE existing sibling function, differing only in literals or a single argument, added inside a **MODIFIED** file is `DUPLICATE` at **High** (file-level `DUPLICATE` stays Critical), with the **declare-and-justify → `INTENTIONAL_PARALLEL` path PRESERVED** because the origin incident legitimately used it (the gap was its **UNDECLARED twin**, claimed by no per-task layer and ruled out of scope at `/devforge:review` by construction); **F2** the two-hats surface partition (behavior-changing vs behavior-preserving, preserving surfaces carrying preservation postconditions in `### Produces`) at three sites — a titled `**Two-hats …**` block appended after Narrowing at the end of `src/constitution.md` §3.6 (D2: a new NUMBERED subsection is out of bounds, `_UNIVERSAL_SECTIONS` being a closed literal tuple), a `### Two-hats partition` subsection in `/devforge:breakdown` PHASE 3 riding free-form `## Change Details` (never a header field — those are flag-gated in `render_task_file`, so a third one would be Python), and appended `code-reviewer` check **10** (nine checks → ten; 1–9 keep their numbers). OQ-1 = **MIXED TASKS ONLY** on an objective two-conjunct trigger (the Files table touches an existing function the task does not delete AND the task changes observable behavior; the every-task fallback was declined), and check 10 also carries an **existence arm** ratified as a scope amendment during the build — both conjuncts visible with no partition written is **Medium**, an authoring omission, weaker than a contradicted declaration at **High**; **F3** the regression-net declaration on restructuring tasks over untested code — a net-first ordering paragraph in `/devforge:breakdown` PHASE 1's existing-codebase branch plus a `### Regression-net declaration` subsection in PHASE 3, **D3 = DECLARE-ALWAYS, never an OR** (the obligation is *declare*; only the content varies — `Regression net: precedes — task NNN` or `Regression net: window accepted — <reason>`, net-first the default). ⚠ **Two honest bounds ship in the emitted text and must not be overstated: NOTHING CHECKS the declaration** (no gate, no validator — the claim is that its ABSENCE is visible, not enforcement) **and the trigger rests on a BELIEF about coverage, not a measurement** (no phase runs a coverage tool); **F4** six named Fowler smells in `/devforge:audit`'s `references/best-practices-checklist.md` as a `Responsibility placement & data shape` section — Feature Envy (`system_design`), Data Clumps / Primitive Obsession / Message Chains (`best_practice`), Shotgun Surgery + Divergent Change (`system_design` or `duplication` per evidence, with a per-smell "which one and why" rule); **the `Category` enum does NOT grow**, `audit/main.md`'s reference enumeration was updated to match, every entry demands quoted code evidence, and there is **no smell-specific confidence carve-out** (D4 declined the three-smell trim — Phase 7's dismissal count is the only trim evidence there will be); **F5** the preparatory-refactoring lane at three sites — a permissive (**MAY**) lane on `/devforge:plan`'s architect sub-question 5 (a preparatory restructuring recorded as its own Key Design Decision, sequenced first, behavior-preserving, carrying its own preservation contract), plus one appended sentence in `src/constitution.md` §6.1 and one inserted sentence in `src/agents/architect.md` Rule 9 (a Phase-0 scope amendment: Rule 9 declares the minimal-scope mandate SOVEREIGN over an advisory MAY lane, so without it the architect could refuse the guidance it was just given). **Sites 2 and 3 exist ONLY to remove a contradiction and add no mechanism**; the **plan-document condition is the load-bearing clause** — objective and externally checkable (the unit is in `plan.md` or it is not), both sentences state the negative half (restructuring not recorded there stays banned), Rule 9's heading/number/six forcing steps are untouched, and **the opportunistic-refactoring ban is KEPT** (§4.2's *"Never modify code outside your task scope"* line took no carve-out). A purely preparatory decision **fails F2's second conjunct, so the Two-hats partition and check 10 do NOT apply to it** — said that way in the shipped text, never as a trivial pass. **ZERO Python, zero validators, zero new PHASE-3.5 gate numbers, zero gates (plan 75's tripwire, both halves)** — "check number" in that constraint means the PHASE-3.5 `verify-*` gate sequence, NOT `code-reviewer.md`'s prose checklist, which Phase 2 legitimately grew from nine items to ten. ⚠ **Consumer-install drift is expected, by design and not a regression**: canonical §3.6 gained a rule, so `constitute_helper verify-universal-defaults` reports DRIFT/MISSING against any install whose `.devforge/constitute.json` predates this build and the WARN-only update-time drift check fires; **back-porting into shipped installs is an explicit NON-GOAL** — they arrive via `install.sh` / `update.sh`. Phase 7 = user-driven HARD GATE, five known-answer anchors: undeclared clone ⇒ `DUPLICATE`/High · declared clone ⇒ `INTENTIONAL_PARALLEL` and PASSES (the motivating incident replayed) · a genuinely new function beside same-shaped siblings ⇒ NOT flagged · the mixed-surface case · and an `/devforge:audit` run recording the surviving-smell-finding count as a **NUMBER**. **Cases 1 and 3 are scored as a PAIR** — a rule that catches 1 by flagging everything fails 3 — and Phase 7 is the ONLY place F1's false-positive rate is observed; nothing shipped here measured it.
- `87-ARTIFACT-LANGUAGE-GUARD-PLAN.md` — ✅ DONE (build) 2026-08-23 (Phases 0–3; Phase 0 ratified in-session the same day — D1–D5, nothing left open). Phase 4 consumer observation DEFERRED — user-driven HARD GATE, NOT run (build-verified, NOT consumer-validated; known-answer anchor = one Cyrillic word committed through `artifact_helper commit-artifacts` → stderr warning + exit 0). **Origin**: the maintainer set Claude Code's user-level `language` key to `uk` on 2026-08-23; that key INSTRUCTS the model and never filters its writes, so every prose artifact (`spec.md`, `plan.md`, task files, code comments, commit messages) was exposed — **no incident behind the plan and none claimed**. Ratified: **D1** ADVISORY WARN, never blocks (plan 34's hygiene flags + plan 44's drift check are the precedents; **strengthening trigger = the FIRST CONFIRMED LEAK the WARN failed to stop** — fail-closed is NOT built, because plan 83's `conversational (in-window user report; no report file)` seed evidence legitimately quotes a non-English user and a hard fail would need a carve-out the zero-escape-hatch policy forbids); **D2** Cyrillic-only U+0400–U+052F (Basic + Supplement) via stdlib `re`, the historic Extended blocks deliberately out, other scripts recorded but NOT built; **D3** stderr `commit-artifacts: warning: …` in the shape the function already emits, **both stdout JSON shapes byte-unchanged**, `stage_errors` (an exit-1 channel) untouched; **D4** emitted `CLAUDE.md` `### Always` item **15 "English in files"** — pure append, nothing renumbered, names no settings key so it cannot rot, and its final clause permits verbatim quotes of user-reported words; **D5** the `README.md` `## Install` note naming `~/.claude/settings.json` / `.claude/settings.local.json` and steering OFF `.claude/settings.json` (which `install.sh` overwrites), with an `install.sh` closing-message `echo` recorded as the NAMED FALLBACK if the note proves unseen. ⚠ **Coverage bound, do not over-read the plan's title**: the detector rides `artifact_helper commit-artifacts` ONLY — `implement_helper wip-commit` is a second commit path, deliberately NOT covered, so a leak inside a per-task or `[WIP] fix:` code commit is invisible to this guard. Zero new checks, zero validators, zero gates (plan 75's tripwire, both halves).
- `88-COLD-FIX-BUGS-LANE-PLAN.md` — **✅ CLOSED 2026-08-27 (build) — closed by maintainer statement (*"the test is a separate story — mark it as done"*); Phases 0–4 complete 2026-08-26 (Phase 0 ratified 2026-08-26, every item AS RECOMMENDED, nothing left open; commits `92592c5` Phase 1 / `bf0c4b9` Phase 2 / `7ec8f69` Phase 3, `d09b7e2` Phase 4). ⚠ Phase 5 consumer e2e DEFERRED AS A SEPARATE EFFORT — user-driven HARD GATE, **NOT WAIVED and NOT run**; the close covers the BUILD and is not a pass (build-verified, NOT consumer-validated; three known-answer anchors — happy path → one clean `fix(scope):` commit plus exactly three changed bug-file fields; scope-change bounce → names `/devforge:research`, NO seed, bug stays `Open`; the three-arm offer observed in conversation — with anchors 1 and 2 scored as a PAIR).** The three forks were picked explicitly: D3 = (a) extend `implement_helper wip-commit` with a final-commit mode + (i) wrapper keeps `[TICKET-ID] - <title>`; D4 = (b) the `close-bug` helper verb (so Phase 1 carried TWO deliverables); D6 = reading (i) — `/devforge:report-bug` Rule 8 byte-unchanged, no forward pointer added. ⚠ **D6's honest bound, which no phase closed: the three-arm conversational rubric is ADVISORY model judgment with nothing checking it — the ONLY mechanical net in this plan is the in-command bounce, and it fires after the user has already typed the command.** ⚠ **SUPERSEDES `55-STANDALONE-BUG-FIX-LANE-PLAN.md`** (DEFERRED 2026-07-10, marked superseded 2026-08-26), which designed this same lane: its D-command-surface (Option 1) and D-batch (single) leans were followed, its D-writeback lean (manual) was REVERSED, and two things it designed were NOT built — the CBM blast-radius tripwire and decoupled standalone firing of stage-coupled floor gates. ⚠ **Its instrumentation trigger (3–4 bugs measured) was never satisfied**: this plan was authorized by a maintainer statement that the friction was already observed plus the verified no-consumer finding, so it must NOT be cited as proof the middle band was measured. Created 2026-08-25. `bugs/NNN-*.md` becomes the **THIRD `/devforge:fix` findings source**, consumed by a new **feature-less COLD MODE** whose trigger is MECHANICAL — an explicit `/devforge:fix bugs/NNN-<slug>.md` argument, no judgment call and no severity threshold. **Origin** (2026-08-25, maintainer-confirmed as ALREADY OBSERVED, not predicted): a cold small bug has no proportional route — hand-fix (bypasses every gate) or the full ~10-command chain — and, separately, `bugs/` files have **no consumer at all** (`src/commands/research/main.md` carries ZERO `bugs/` mentions, yet `/devforge:report-bug`'s forward pointer names it). The rejected alternative is recorded WITH its reasoning: a "light lane through the main pipeline" (a `minor` classification thinning artifacts across research/specify/plan/breakdown) fails because ceremony cost lives in GATE COUNT, not artifact size, and an "if minor" branch inside the main chain is exactly the escape hatch the zero-escape-hatch policy forbids. **D1** plan 26's D2 free-text ban is **EXTENDED, not reversed** — a bug FILE is a written finding (title/severity/evidence/file table), so *"consumes findings, never invents them"* stays literally true — and the code-confirmation duty is NOT droppable (a capture is not a confirmation; OQ-1 STOPs the run when the defect cannot be located in live code). **D2** the three feature-bound sub-phases are bypassed on the cold path — PHASE 0.2 feature resolution, 0.3 `in-fix-window`, **and 0.4 `read-findings`** (all three need a `--feature` that does not exist; `in_fix_window` would return `no_tasks_dir` and gate a cold run OUT by construction, and no `findings.json` is ever produced, so PHASE 1's triage inputs need their own carve-out) — as an **explicit dated amendment to plan 26's in-window rule**, narrow because that rule's subject is a sealed FEATURE UNIT which a cold run does not have. ⚠ Cold mode therefore also loses the `not_all_tasks_complete` protection against a concurrent `/devforge:implement` — recorded as an accepted unguarded state, NOT closed. **D3** the lane ends in ONE clean conventional `fix(scope):` commit — **no `[WIP]`**, because no `/devforge:finalize` will ever squash it — with both forks RATIFIED 2026-08-26: fork 1 = **(a)** extend `implement_helper wip-commit` with a final-commit mode (over a new `fix_helper` verb — re-affirming plan 26's own 2026-06-19 "extend the one binary, never a second composer" note), fork 2 = **(i)** wrapper keeps `[TICKET-ID] - <title>` — the ticket falls back to the full branch name, so a cold wrapper fix on `develop` reads `[develop] - <title>`, **a string seen at ratification and ACCEPTED**. The `--final` + `--scope` shape stays a RECOMMENDATION Phase 1 may diverge from with a stated reason. ⚠ **Named trap: `--final` must NOT clear `.devforge/wip.md`** — `wip-commit` clears it unconditionally today, and only the window gate D2 drops ever made that safe. **D4** exactly ONE `bugs/` write, on the ONE consumed file — `**Status**` → `Fixed`, the empty `**Fixed**:` line dated, `## Fix Notes` filled (⚠ read the LIVE schema: there is no `Fixed date` field and no bold `Fix Notes` field) — amending `/devforge:fix` Rule 7 NARROWLY (the in-window path still writes none; `/devforge:report-bug` stays the only CREATOR) and **collecting plan 26's own chartered `close-bug` deferral**; the carrier fork was RATIFIED 2026-08-26 as **(b)** — a `close-bug` helper verb over a new function in `_shared/bug_file.py`, on helper-owns-shape plus plan 26's chartered deferral (a divergence from the drafting brief's "only D3 is Python" framing, acknowledged at ratification rather than smoothed — Phase 1 stays the only Python phase, now with TWO deliverables). ⚠ D4's blast radius is undercounted by a naive grep: the falsified "the lifecycle is manual" claim sits at **THREE** `report-bug/main.md` sites (`:15` / `:90` / `:100` — the third INSIDE Rule 7, which is SPLIT, never deleted) sharing only the anchor `Open → In Progress → Fixed`, so a `maintained manually` grep MISSES `:90`; plus TWO in `storage-rules.md`, FOUR in `fix/main.md`, and a FIVE-clause `src/CLAUDE.md` catalog paragraph (*"the only place the model sees what they do"* for a human-typed command) plus a sixth workflow-prose window claim. **D5** a cold bounce recommends **`/devforge:research`**, never `/devforge:specify` (which is mechanically unreachable — it blocks until a research/discover handoff exists in a feature dir), and writes **NO seed** (plan 83's seed model is feature-scoped by construction — `write_seed` joins onto `feature_dir`), leaving the bug `Open`; the lost diagnosis is recorded as an accepted bound, not repaired. **D6** `src/CLAUDE.md`'s two-arm "Conversational fix-or-file offer" grows to THREE arms (in-window `/devforge:fix` / cold `/devforge:report-bug` → `/devforge:fix bugs/…` / full chain from `/devforge:research`), discriminator = **defect-repair vs change, NEVER file count**, an ADVISORY nudge and **never a gate** (precedent: `/devforge:plan`'s stakes-hint) — the honest bound being that the offer is model judgment and D5's in-command bounce is the only mechanical net. ⚠ The two-reading fork over `/devforge:report-bug` Rule 8 (*"Never call `/devforge:fix` from here"*) was RATIFIED 2026-08-26 as **reading (i)** — no conflict, the rubric is not the command body, **Rule 8 stays byte-unchanged and NO forward pointer is added there** — **with its counter-argument retained UNRESOLVED**: the command's own PHASE-4 pointer and the rubric can give a user two different answers in one session, and a Phase-5 observation of that contradiction is the named trigger to re-open as reading (ii). D4's three-site correction lands in that file under BOTH readings. **D7** NO numeric scope cap — v1's ≤5-files guard is DELIBERATELY not reproduced (plan 27 D3's *"routing state, not a size metric"* is the precedent). **Plan 63's 13/7 counts UNCHANGED** — `/devforge:fix` stays `disable-model-invocation: true` and human-typed; no description trim, no count delta (OQ-6). **Plan 75's tripwire holds, both halves**: the cold lane adds a MODE, not a gate — zero new `verify-*` gates, zero new check numbers, zero hard-fail validators; **Python is Phase 1 ONLY** (D3's commit mode + D4's chartered `close-bug`), every other phase instruction-only. Phase 5 = user-driven consumer e2e **HARD GATE** with three known-answer anchors (happy path → one clean `fix(scope):` commit plus three bug fields flipped and every other byte unchanged; scope-change bounce → names `/devforge:research`, NO seed written, bug stays `Open`; the three-arm offer observed in conversation), anchors 1 and 2 scored as a PAIR.
- `89-TEST-FOUNDATION-HARDENING-PLAN.md` — **✅ DONE (build) 2026-08-27 — Phases 0–5 complete (`3521e34` Phase 0 close / `1ead52d` Phase 1 / `8f4ffec` Phases 2+2b / `27cb30e` Phase 3 / `77beba1` Phase 4, plus the Phase-5 docs commit this entry rides in; python-reviewer + instruction-reviewer both SHIP-READY, full `tests/lib` suite green — 11124+ passed, `test_breakdown_helper` 426). Phase 6 consumer e2e DEFERRED 2026-08-27 by explicit maintainer decision — maintainer intends to run it (NOT waived; build-verified, NOT consumer-validated; the five known-answer anchors below are the recipe), user-driven HARD GATE, NOT run. D1–D7 + OQ-1–OQ-6 ALL ratified as recommended, the five explicit picks answered in-session: D1 §3.5 host / D2 + D4 arm (b) BOTH taken / D6 arm (b) / D7 IN FULL with clause 1 KEPT / OQ-6 ≥ High floor. Created 2026-08-26.** The test OBLIGATION made as standing as the test RUN. **Ships FIRST of the 89/90 pair, so plan 90 owes the LIVE re-read of every shared surface.** **Origin** (2026-08-26): one maintainer review question — does the framework cover the code it generates with tests — plus the `src/` sweep it triggered. **PREDICTED-GAP plan in plan 87's class: no consumer incident, no failing run, no measurement — the plan requires that this be said wherever it is summarized.** The sweep's split IS the plan: test RUNNING is solid at three independent layers (`/devforge:implement` PHASE 5 `verify-touched`, `/devforge:verify` PHASE 4's assembled suite + regression gate, `qa-reviewer`'s AC→test mapping) and **nothing here touches any of it**; test OBLIGATION is soft everywhere it could stand — constitution §3.4 is a bare `{{TESTING}}` placeholder, the task skeleton's four standing Done-When lines carry no test analog, `### Always` item 8 is *"Lint everything"* with no counterpart, and a package whose `test_command` is `null` runs no tests SILENTLY while every Done-When box still ticks. **Linting is obligated in three places, testing in one — the asymmetry is the finding.** **D1** one universal block appended to `src/constitution.md` **§3.5, NOT §3.4**, and the reason is mechanical: `_UNIVERSAL_SECTIONS` is a CLOSED eleven-entry tuple without `§3.4`, so `verify-universal-defaults` never looks there, while §3.5 parses as one whole-body rule and needs no parser change (the counter-argument — a reader hunting testing rules still meets §3.4's placeholder — is ACCEPTED as a real cost and routed to OQ-3, whose recommended arm (i) is accept-and-add-nothing). ⚠ Designed consumer drift, not a regression, and **shipping in the same release as plan 86 shows consumers TWO drift findings (§3.5 and §3.6)**; back-porting is an explicit NON-GOAL. **D2** a fifth standing Done-When line `Tests pass on changed files (see Development Commands section)` in `_DONE_WHEN_FIXED_LINES` — **the plan's ONLY Python** (one list literal plus its tests) — UNCONDITIONAL, the drafting brief's *"skip only when …"* phrasing rejected as the escape hatch the meta-rule forbids; blast radius EIGHT verified sites — **FIVE edited, THREE recorded no-ops** (the headline/enumeration mismatch was reconciled before Phase 0 closed), plus four further no-ops the builders verified and recorded (`implement/main.md:36` — a D2 no-op but a D4 SITE — and `:321`; `/devforge:fix` needs no D4 mirror, having no scope-and-approve path; Phase 4 has zero test blast radius, no test pins the `### Always` count), and ⚠ **`test_four_fixed_done_when_lines_verbatim` still PASSED with a fifth line — only its NAME was falsified, so a green suite was never evidence that site was handled; it was renamed at Phase 1, and a NEW test now pins `storage-rules.md` ↔ `_DONE_WHEN_FIXED_LINES` byte-consistency as a standing regression.** **Ordering resolved APPEND-LAST** (byte-stability, no invented grouping, house append precedent). **D3** a sixteenth `### Always` Key Rule, pure append with items 1–15 byte-identical (plan 87 D4's precedent) — ⚠ bound stated in the vendor's own words, *"context, not enforced configuration"*: presence in every session, compliance in none. **D4** the silent no-test path closes **INSTRUCTION-ONLY** — a `verify-touched` `pass` whose `test_commands_run` array is EMPTY (already on the wire, never named by the spec) makes `/devforge:implement`'s approve path pass the Tests box as `--unverified-box`, leaving a durable annotated box instead of a falsely green one; the stderr-WARN arm is the NAMED strengthening arm with an observable trigger. ⚠ **D4 DEPENDS ON D2** — D4 with D2 declined is unbuildable, D2 with D4 declined ships a box that can read `[x]` over a run in which nothing ran; **Phase 0 took BOTH and recorded the pairing in writing, so neither ships without the other.** ⚠ **Build finding, recorded 2026-08-27: the contradicting paragraph in `implement/main.md` had TWO sentences, not the one the plan named** (the closing *"On a clean `pass`, pass NO `--unverified-box` arguments"* AND an opening enumeration asserting a clean `pass` confirms ALL conditions), **plus a THIRD site falsified by omission at `:36`** — a build that greps only for the closing sentence passes its own Verify while shipping the contradiction. **D5** NO numeric coverage threshold at any layer — DECLINED, its counter-argument ACCEPTED not answered (without a number "coverage" is unfalsifiable; a future plan owes a reported, non-gating measurement FIRST). **D6** documentation-only, SHIPPED at Phase 5: `/devforge:configure` never named `REGRESSION_GATE`, a live key with an enum, a `"full"` default, a `set-regression-gate` setter and a documented `/devforge:verify` PHASE-4.3 consumer — now documented at the end of Phase 5.1 where `render-config` writes `project-config.json`, with **no new prompt and no count moves** (the file's own arithmetic shows the gap: schema 30 fields, 29 set = 23 detected + 6 prompted, the 30th set by no phase). **D7** (ADDED 2026-08-26 by maintainer directive — the obligation must not be satisfiable by tests written merely so tests exist, nor by invented ones) three FALSIFIABLE clauses appended to D1's block and mirrored in `qa-reviewer`, each naming an observable SHAPE and never a quality (the words *meaningful* / *reasonable* / *quality* appear in no emitted text): expected-value provenance **scoped to the changed behavior** (running the implementation and recording its output is not a derivation; a test pinning unaltered existing behavior is outside the clause), *"for every test there is a change to the code under test that makes it fail"* with THREE named vacuous shapes (a mock's own configured return asserted back; a literal or just-assigned value asserted against itself; a call asserting nothing about result, raised error or observable effect), and no restating the type checker. **DEFENCE IN DEPTH FOR D2, not an independent idea — D2's checkbox is a target and a vacuous test ticks it**, so ⚠ **taking D2 and declining D7 would have shipped the incentive without the counterweight; Phase 0 took BOTH and recorded the pairing**, and the reviewer edit EXTENDS a pre-existing *"Flag weak, vacuous, or implementation-coupled assertions"* sentence — **what shipped is falsifiability, not the concept.** ZERO Python: the detection layer is a lettered **Phase 2b** that EXTENDS `qa-reviewer`'s pre-existing *"Flag weak, vacuous, or implementation-coupled assertions"* sentence at Approach step 4 + Rule 3 — making a bare adjective falsifiable, NOT introducing the concept — with no new section, no new agent, no `tools:` change and the vocabulary still exactly `ADEQUATE / GAPS FOUND`; `src/agents/qa-engineer.md` is a VERIFIED NO-OP because fact 15's inline tests are the per-engineer majority path that agent is never dispatched on, while the panel hands `qa-reviewer` every test in the change. **OQ-6** floors a clause-2 finding at ≥ High and High forces `GAPS FOUND` (clauses 1 and 3 deliberately get NO floor). ⚠ **NOTHING MECHANICAL CHECKS meaningfulness — an LLM reviewer judging LLM-written tests, and weaker than plan 86's F3 in one way: F3's reader is a human opening a task file, D7's is a model in an autonomous loop**; vacuous tests do not become impossible and no phase measures how many get through. **Non-goals**: any gate / `verify-*` / hard-fail validator (plan 75's tripwire, both halves), a coverage threshold, mutation testing (`mutmut` / `Stryker` / `cargo-mutants` — the MECHANICAL answer to clause 2, recorded as the NAMED strengthening path with an observed trigger, never a refusal), a new test-quality agent / section / verdict value, AC→test-outcome mapping (chartered to the testForge20 e2e), strengthening plan 86's F3, changing who writes tests, any `compute-verdict` input, any `verify-touched` behavior change, a new numbered constitution subsection, back-porting into shipped installs, guessing an ecosystem-default test command, and any plan-63 13/7 delta. **Coordination with plan 90** (drafted the same day; execution order 89 → 90): THREE surfaces shared unconditionally — `src/CLAUDE.md`'s `### Always` list, `src/devforge/storage-rules.md`'s Done-When skeleton, and ⚠ **the newest and least obvious, added 2026-08-26 by D7**, `src/agents/qa-reviewer.md`, where this plan's Phase 2b edits Approach step 4 + Rule 3 while plan 90 edits a DIFFERENT sentence beside step 6 — **different sentences in one file, so the hazard is a CLOBBERED EDIT on a stale read, not a contradiction**, and both plans leave the fenced `yaml` meta-block byte-unchanged — plus `src/agents/qa-engineer.md` as a CONDITIONAL FOURTH, joining ONLY if OQ-3 ratifies its NOT-recommended arm (iii) (⚠ D7 considered that file and left it alone DELIBERATELY, so D7 does NOT move it into the shared set); **whichever of the two ships SECOND reads those surfaces LIVE and re-derives its own edits, never a pre-computed diff — the rule binds the READ, not the edit.** Phase 6 = user-driven consumer e2e HARD GATE, FIVE known-answer anchors (configured-test path ticked clean / null-test path UNTICKED and annotated / the §3.5 drift finding / the Key Rule shipped and loaded, whose part (b) rests on a Claude Code tool-behavior claim and owes a `claude-code-guide` check BEFORE the run / **a planted vacuous test AND its genuine twin in one task**, recording the finding's severity and the returned verdict token as STRINGS). **Anchors 1+2 are scored as a PAIR, and so are anchor 5's two plants** — a reviewer that flags every test passes one half and fails the other — and **anchor 5 is the ONLY place D7's false-positive rate is ever observed; nothing this plan ships has measured it.**
- `90-E2E-TEST-LANE-PLAN.md` — **✅ DONE (build) 2026-08-27 — Phases 0–5 complete (`1a06463` Phase 0 close / `c1718ee` Phase 1 / `9cee99c` Phase 2 / `182f514` Phase 3 / `d4d7928` Phase 4, plus the Phase-5 docs commit this entry rides in — Phase 1 landed fourth, the phases being independent). Phase 0 CLOSED 2026-08-27 — D1–D8 + OQ-1–OQ-7 ALL ratified as recommended, the four explicit picks answered in-session (D3 = advisory fork (i) + reasons-only; D1 = top-level `E2E_COMMAND`, configure composes the default from manifests; D5 placement = dedicated e2e directory; D5 section = rename `## Mobile Testing`). Phase 6 consumer e2e DEFERRED 2026-08-27 by explicit maintainer decision — maintainer intends to run it (NOT waived) — user-driven HARD GATE, NOT run (build-verified, NOT consumer-validated; three known-answer anchors, anchors 1 and 2 pair-scored, and anchor 1 records the wall-clock delta as a NUMBER — the first this plan family will ever have). Created 2026-08-26. Ships SECOND (execution order 89 → 90).** **Amended 2026-08-27 (Phase-0 close): the D1 clause below reading *"seeded from `.devforge/init.yaml`'s `test_infra.e2e`"* is SUPERSEDED — the ratified arm has `/devforge:configure` compose the default from the `MANIFESTS_JSON` manifests it already parses, `init_helper` is NOT touched and `test_infra.e2e` is read by nothing; the *"Phase 0 picks"* clauses in D1 and D5 are DISCHARGED (dedicated e2e directory; rename-and-widen), and D3's *"a ratifier who takes (i)"* is now an accepted cost, fork (i) having been taken.** **Amended 2026-08-27 (staleness reconciliation, nothing ratified): plan 89 shipped first — all three shared surfaces moved and its OQ-3 resolved arm (i) so `qa-engineer.md` is NOT shared; `configure/main.md` gained plan 89's D6 `REGRESSION_GATE` note as a sixth count site Phases 1/5 must reconcile; the plan file carries the per-site dated notes.** A user flow DECLARED at `/devforge:plan`, OWNED by a `qa-engineer` task at `/devforge:breakdown`, RUN once per feature at `/devforge:verify`. **Origin** (2026-08-26): one maintainer question — are e2e tests needed in the emitted pipeline and what would adding them take — plus the sweep it triggered. **No incident behind it and none claimed; predicted-gap plan in plan 87's shape.** The sweep found ZERO web-e2e vocabulary in the emitted instruction set (a `playwright|cypress|selenium|puppeteer|webdriver` grep over `src/**/*.md` returns nothing; the only E2E vocabulary anywhere is mobile-only and advisory, in `qa-engineer` + `qa-reviewer`) and — **the central structural problem — no test-tier taxonomy to hang one on**: one `test_command` per package plus a flat `TEST_COMMANDS`, nothing distinguishing unit from integration from e2e. Hence *"just put e2e in `test_command`"* is REJECTED on two correctness grounds, not on cost: it would run per task under `/devforge:implement` and be re-paid up to three more times by the helper-owned self-repair cap, and it is broken by construction in `regression-gate`'s baseline half — a detached worktree with nothing built and nothing served, where an app-dependent suite fails for reasons unrelated to the feature and the gate then reads its own failure as `baseline-failing` and stops gating (**a gate that silently disarms itself is worse than no gate**). **D1** one top-level `E2E_COMMAND` key (Python: a SIX-file config surface whose fails-closed-if-missed member is `_cmds_verify.py`'s required-field loop, which violates on any `None` scalar) whose load-bearing constraint is that it stays **INVISIBLE to `verify-touched` and to `regression-gate`** — mechanically true, since both read only `PACKAGE_STACKS[].test_command` and `TEST_COMMANDS[0]` — seeded from `.devforge/init.yaml`'s `test_infra.e2e` and inheriting `test_command`'s *"do NOT invent an ecosystem-default guess"* rule. ⚠ **That seeding carries real build cost because today's detection is DEAD end to end**: no command spec ever invokes `detect-test-infra` / `set-test-infra` (the grep hits only `init_helper.py`) and the record's only reader filters bucket values through a schema-valid set excluding playwright and cypress, so the `e2e` value is unreachable by construction — Phase 0 picks which arm makes it real (smallest: `/devforge:configure` composes it from manifests it already parses), and the `_probe_tier.py` unreachability is RECORDED, deliberately not repaired. **D2** the declaration is plan-side — architect **sub-question 12** → a conditional `### E2E Scenarios` `plan.md` subsection, the property-lane SHAPE minus its gate, carried to `/devforge:breakdown` with ZERO Python (read from `plan.md` directly, as the dead-code table already is) — giving `architect.md`'s `qa-engineer` consult row its first consumer; ⚠ its bracket text must NOT promise a gate: **the claim is visibility, not enforcement.** **D3** the execution seat is a new fail-soft `/devforge:verify` **PHASE 4.5** running the suite ONCE at HEAD in the real `source_root` (never a worktree, never the merge-base), ALWAYS exit 0, statuses `off` / `inconclusive` / `e2e-clean` / `e2e-failing`, **fork (i) ADVISORY in v1 — no status gates the verdict** — with a named strengthening trigger; ⚠ the strongest counter-argument is recorded and NOT fully answered: a ratifier who takes (i) will see APPROVED printed beside a red suite. **D4** NO seventh PHASE-3.5 gate — the sequence stays at six and plan 75's tripwire holds in both halves; `verify-e2e-coverage`'s shape is recorded with an OBSERVED-dropped-scenario trigger rather than built. **D5** a dedicated `qa-engineer` task (no new Agent Assignment row, NO flag and NO header field — scenarios ride `## Change Details`), an escape-hatch-free framework rule (write in the framework the configuration names; empty configuration ⇒ BLOCKED and say so; the Playwright recommendation is a `/devforge:configure` DEFAULT, never an agent choice), plus **the e2e-spec PLACEMENT rule against the runner collision** — Playwright and Vitest/Jest share `*.spec.ts`, so an unscoped unit glob runs an e2e spec under the wrong runner and burns the self-repair cap on a non-defect; Phase 0 picks ONE placement arm (dedicated directory vs runner-config exclude) and only the picked arm reaches the emitted text, and a second fork picks rename-and-widen of `qa-engineer.md`'s `## Mobile Testing` section vs appending a second one. **D6** the SUITE owns the app lifecycle — the framework starts nothing — and `ac_runtime_*` stays an uncoupled channel. **D7** the wall-clock cost line plan 85's D7 makes obligatory: ONE suite run per `/devforge:verify` run, not per task and not doubled — structural, not numeric, because plan 70's Phase-2 numbers do not exist; Phase 6 records the delta as a NUMBER. **D8** a greenfield or no-e2e-infra install is a clean SILENT no-op at every seat — no question, no subsection, no task, status `off`, **no nagging line anywhere**. OQs: no CI claim (OQ-1); mobile text byte-identical (OQ-2); the AGENT runs the suite, the helper does not, bounded by D5's placement rule (OQ-3); flake handling explicitly OUT OF SCOPE and load-bearing for D3 (OQ-4); a timeout is `inconclusive`, never `e2e-failing` (OQ-5); no `/devforge:summarize` / `/devforge:finalize` change and NO plan-63 13/7 delta (OQ-6). **Coordination: shipping second, this plan owes the LIVE read of THREE shared surfaces** — `src/CLAUDE.md`'s `### Always` list, `storage-rules.md`'s Done-When skeleton (plan 89 moves both), and ⚠ **the one to miss**, `src/agents/qa-reviewer.md`, shared only since 2026-08-26 when plan 89 gained its D7: plan 89's Phase 2b edits Approach step 4 + Rule 3 there while THIS plan edits a different sentence beside step 6, **so the hazard is a CLOBBERED EDIT on a stale read, not a contradiction**, and both plans commit to leaving the fenced `yaml` meta-block byte-unchanged. `src/agents/qa-engineer.md` stays a CONDITIONAL FOURTH — plan 89's RESOLVED OQ-3 must be read before treating it as contested (its D7 deliberately left that file alone, so D7 did not move it) — and this plan proposes NO new `### Always` item. ⚠ **`D7` is an overloaded token across the pair: plan 89's D7 is test-meaningfulness, THIS plan's D7 is the wall-clock cost line — every cross-reference names the plan.** Phase 6 = user-driven consumer e2e HARD GATE, three known-answer anchors (declared happy path → `e2e-clean` / silent install → `off` with zero `e2e` mentions in the transcript / broken flow → `e2e-failing`, verdict recorded as a STRING), anchors 1 and 2 scored as a PAIR. **What Phases 1–5 actually built, each verified live at the Phase-5 sweep:** Phase 1 the `e2e_command` field across the six-file config surface (`FIELD_SCHEMA` → **31**, a `FIELD_DEFAULTS` baseline of `""` so `configure_helper verify` needs NO exemption, the `set-e2e-command` setter, `E2E_COMMAND` appended last to `_PROJECT_CONFIG_KEY_ORDER` → **39**, and the `AC verification` display group per the `regression_gate` sibling precedent rather than a new single-field group); Phase 2 architect **sub-question 12** + the conditional `### E2E Scenarios` subsection (four columns — scenario / acceptance criteria / flow steps / preconditions) plus two same-file consistency edits (the architect-authored-list entry and the approval-summary line); Phase 3 `/devforge:breakdown` PHASE 0a.5's read-the-table-from-`plan.md` sub-block + PHASE 3's `### E2E scenario tasks (declared full-stack flows)` + `qa-engineer`'s renamed section + `qa-reviewer`'s step-6 web mirror; Phase 4 `verify_helper e2e-gate` (`off` / `inconclusive` / `e2e-clean` / `e2e-failing`; a parsed config with the key absent or blank → `off`, a missing or corrupt config → `inconclusive`, a 1800s timeout → `inconclusive`) + `compute-verdict --e2e`'s reasons-only fold + `verify/main.md` `### 4.5`; Phase 5 the `/devforge:configure` COMPOSER at three sites (the Phase-2 compose bullet, the Phase-3 echo-template line, the setter-mapping row) — **without it the field would render, be settable, and stay `""` forever, which is D1's rejected alternative (c) reached by omission** — plus the count reconciliation, **counted live and never incremented**: 30 fields populated by that command (24 detected + 6 prompted) out of a 31-field schema, and 39 project-config keys = 31 from `configure.yaml` + 5 from `init.yaml` + 3 derived, with plan 89's D6 `REGRESSION_GATE` note **UPDATED IN PLACE** (23 → 24 detection-derived, 29 → 30 populated, 30 → 31 schema) and its `regression_gate` guidance verbatim. **Phase 3 executed D5's section arm: `src/agents/qa-engineer.md`'s `## Mobile Testing` heading is now `## End-to-End Testing`**, carrying a web block beside the mobile block with every mobile bullet byte-identical — **so both mentions of the old heading above record the DECISION, not the live tree.** `src/devforge/storage-rules.md` was read and **deliberately NOT edited**: it mirrors the task-file header fields plus the Done-When skeleton, and plan 86's `Regression net:` and Two-hats prose lanes are equally absent from it, so an e2e mention would have been the first of its kind there. ⚠ **The honest bounds survive the build and no summary may drop them: v1 GATES NOTHING** (no status reaches the verdict — the fold adds one advisory `reasons` line and never a blocker), **NOTHING CHECKS the declaration** (no seventh PHASE-3.5 gate; a declared scenario covered by no task produces no error — the claim is visibility, not enforcement), **the run is LOCAL and makes no CI claim** (OQ-1), and **there is still no incident behind this plan** — predicted-gap in plan 87's class, so a clean Phase 6 would be evidence the lane works and never evidence the gap cost anything.

Five file-less FINDINGS (no plan file, no owner — do not grep for plan files) live in `FINDINGS.md`: (1) the §3.6 Narrowing FALSE BINARY — escalated to "prefers the empirically failing shape"; still open and (post the plan-77 waiver) without any designated resolution path (plan 81's F1 is a CANDIDATE route, unratified); (2) the intake rubrics' blanket `--accept-gaps` escape ("76" — its original causal framing WITHDRAWN); (3) post-plan-63, `/plan`'s constitution-backed obligations have no always-on presence (accepted tradeoff; pointer-sufficiency UNDECIDED); (4) an AC pinning a value the same spec removes — a conflict shape nothing refuses (plan 81 D4 proposes ownership, unratified); (5) hard constraints that eliminate a candidate discriminator record no cost note (the naive rule fails plan 77's visibility bar). Read the full finding before acting on any of them.

Completed plans archived at `done-plans/`. Re-read only if maintaining the named feature.

## Conventions for ongoing work

- **Plans at repo root** as `<TOPIC>-PLAN.md` for any multi-session execution work
- Each plan includes a `## Context for next session` section + per-step `## Verify` criteria + `## When resuming work` instructions
- Plans are committed alongside the work they drive — git history shows plan evolution
- Investigation/journey knowledge lives in **Obsidian** (`20 Projects/AIDevTeamForge/`)
- Execution plans + active-state context lives in **repo**

## Branch state

- `main` — trunk.
- `develop-2.0-init` — current work branch (4-command sequence + all post-pivot work).

## Where to find what

Compact router. The original full-detail cells are preserved verbatim in `PLAN-STATUS-ARCHIVE.md` → "Appendix — Where to find what (full cells)"; the named plans stay the authority.

| Topic | Location |
|---|---|
| Forge architecture decisions | `DEVELOPMENT-STATUS.md`, `CHANGELOG.md` |
| Spec sources | `src/commands/`, `src/agents/`, `src/files/` |
| Generators / emitters | `scripts/emitters/`, `scripts/generate*.py`, `scripts/generate.sh`. `scripts/emitters/claude.py` owns the emitted layout: `_PROMOTED` (20 names, updated by hand) → `.claude/commands/devforge/<name>.md` (`/devforge:<name>`); references → `.devforge/command-refs/<name>/`; 13 commands model-invocable, 7 human-typed-only (`disable-model-invocation: true`: `init-forge`, `generate-docs`, `configure`, `constitute`, `grill`, `spec-check`, `fix`). See plan 63 + archive appendix. |
| Runtime helpers (4-command sequence) | `src/devforge/lib/{init_helper,configure_helper,constitute_helper}.py` + `src/devforge/lib/_generate_docs/` |
| Forcing-functions detectors (consumer-side) | `src/devforge/lib/_constitute/_forcing_functions/` + `constitute_helper verify-*` / `set-forcing-functions` / `list-forcing-functions`; pre-commit template `src/git-hooks/pre-commit-forcing-functions.sh`; update-time drift check `scripts/constitution-drift-check.sh` (WARN-only, plan 44). See plans 01 + 44 + archive appendix. |
| Persistent memory — path, probe, reads (plans 74 + 79) | `src/devforge/lib/_shared/memory.py` — the SINGLE owner of the `.devforge/memory.md` literal, the three-state probe (`absent`/`stub`/`populated`), and the bounded reads. The EXCERPT is SECTION-AWARE, not a positional slice (plan 79): `## ` sections parsed, `## Task Outcomes` dropped by named denylist, content-free sections dropped heading and all, per-section budget share, newest entries survive a cut, every cut declared by a marker. `MEMORY_RELATIVE_PATH` is a forward-slash LITERAL compared for exact equality — never platform-join it. `install.sh` preserves an existing `memory.md` by copy-aside + restore. See plans 74 + 79 + archive appendix. |
| Memory-lane coverage gate (maintainer-side, plan 74) | `scripts/lib/memory_lane.py` + `scripts/verify-memory-lane.py` + `tests/lib/test_memory_lane.py` (the live-`src/` test IS the gate — no CI/Makefile). Rule 2 is CONJUNCTIVE — do not loosen it to a disjunction. See plan 74 + archive appendix. |
| Agent-executor reachability gate (maintainer-side, plan 41) | `scripts/lib/agent_reachability.py` + `scripts/verify-agent-reachability.py` + `tests/lib/test_agent_reachability.py` (the live-`src/` test IS the gate). Mechanizes type-1 orphaned-agent ONLY. See plan 41 + archive appendix. |
| Design-fidelity mechanism (WEB, consumer-side) | Anchor + binding + runtime compare: `src/devforge/lib/_design/` (`design-anchor.json` persisted at `/specify`; binding `design-manifest.json` authored at `/breakdown` PHASE 2.5; `design_helper compare` engine dispatched at `/review` PHASE 2.5 — Python decides, JS measures). Static gate: `_constitute/_forcing_functions/_design_tokens/` (`verify-design-tokens`, Checks 1–4). Backstop: `breakdown_helper verify-manifest-present`. Contract: constitution §3.8. See plans 53 (supersedes 52, absorbs 40's runtime half) + 40 + 42 + archive appendix. |
| Pipeline handoff (research → specify) | `specs/NNN-<slug>/research-handoff.json`; schema `src/devforge/lib/_research/handoff_schema.py`; producer `research_helper finalize-handoff`; consumer `specify_helper import-handoff`. Legacy dated `research/` dirs are inert history (plan 68 D3). |
| Pipeline handoff (discover → specify) | `specs/NNN-<slug>/discover-handoff.json`; schema `src/devforge/lib/_discover/handoff_schema.py` (`handoff_kind = "discover"`); producer `discover_helper finalize-handoff` (Phase 4, AFTER the save prompt); consumer `specify_helper import-handoff` (kind-dispatch); `specify_helper find-handoffs` = one glob with the two-arm pending predicate (plan 68 D10). |
| Pipeline handoff (specify → plan) | `specs/NNN-<slug>/handoff.json`; schema `src/devforge/lib/_specify/handoff_schema.py`; producer `specify_helper finalize-handoff` (typed `spec_seeds`, no `plan_seeds`); consumer `plan_helper read-specify-handoff` + `render-plan-seeds` (`/plan` PHASE 0a.5). |
| Pipeline handoff (plan → breakdown) | `specs/NNN-<slug>/plan-handoff.json`; schema `src/devforge/lib/_plan/handoff_schema.py`; producer `plan_helper finalize-handoff` (`/plan` Phase 4); consumer `breakdown_helper read-plan-handoff` (`/breakdown` Phase 0). |
| `/audit` command (standalone adversarial audit) | `src/commands/audit/` (+ 6 references) + `src/devforge/lib/_audit/` + `audit_helper`; tests `tests/lib/_audit/`. Three scope modes (broad / hotspot `--top N` / narrow); full-spectrum producer-declared `Category` buckets; `--passes N` union merge; refutation stage PHASE 4.2.5 with confidence-gated report + D7 high-stakes guard; scratch chain in `${TMPDIR:-/tmp}/forge-audit`. See plans 10/11/12/19 + archive appendix. |
| `/grill` command (design-level adversarial review) | `src/commands/grill/` + `src/devforge/lib/_grill/` + `grill_helper` + `src/agents/devils-advocate.md`; reuses the `_shared/` refutation engine with an architect-excluded `priority=`. Writes `specs/[feature]/grill.md` + a verdict-gated backward `grill-seed.json` (`ReEntrySeed`; consumers `/research` / `/discover` / `/specify` / `/plan`). The USER owns the verdict at the `/breakdown` gate; plan 85 made the RUN required before `/breakdown` without making the verdict binding. See plans 23/36/39/85 + archive appendix. |
| Helper review-and-fix pipeline | `/review-helper <path>` — see `.claude/commands/review-helper.md` |
| Install / update logic | `install.sh`, `update.sh` |
| `.devforge/` git disposition (plans 49 + 56 + 63) | 4 classes — VERSIONED / EPHEMERAL / FEATURE-SCOPED (plan 49) + CODE (plans 56/63: `lib`/`bin`/`templates`/`command-refs` gitignored + untracked; `.devforge/template/` SINGULAR stays tracked as the agent-merge baseline). Docs: `src/devforge/storage-rules.md`; mechanics: `src/files/devforge.gitignore` + `scripts/devforge-state-migrate.sh`. See archive appendix. |
| Investigation rationale | Obsidian: `20 Projects/AIDevTeamForge/` |

## Working process for all changes

**Apply to every change, not just complex ones.** Even apparently-mechanical changes (typo fixes, single-line edits, "do exactly X" instructions) go through this flow because the user's actual goal lives in their head — what looks fully specified to you may have implicit context you haven't surfaced. Friction up, surprise down.

For every change:

1. **Draft a plan first** — break the work into small steps. Each step should leave the system in a buildable, verifiable state. For a one-line change, the "plan" is a one-line confirmation: *"I'll change `<file>:<line>` from `<X>` to `<Y>` — confirm?"* That counts.
2. **Draft each step explicitly** — what files change, what the verification looks like, how it depends on prior steps.
3. **Argue every step** — give reasoning for why this step exists, what alternatives you considered, why this approach over others. Push back on the user's framing where you have substance to add. Don't rubber-stamp; engage with the trade-offs.
4. **Align with prior work** — every step must follow logically from the previous step. No logical gaps. No hallucinated assumptions about files, branches, commits, or state that you haven't verified. Read the actual code/files before making claims about what's there.
5. **Select the best option** — when alternatives exist, present them, recommend one, explain the trade-offs. Let the user redirect if they prefer a different path.
6. **Implement only after alignment** — once the user confirms, execute. End result of each step must be straightforward and independently verifiable.

The goal is that the user can challenge or redirect at any planning point before code lands. Implementation surprises are a process failure. **One extra exchange per change is the price of staying aligned.**

## Audit format

When asked for an audit, review, or critical evaluation:

1. **Count first** — state how many findings you have before presenting any. Example: *"I found 4 findings: 1 high, 2 medium, 1 nit."*
2. **One finding at a time** — present each finding individually. Wait for user reaction before the next.
3. **Per-finding format:**
   - **Severity** — high / medium / low / nit
   - **Location** — `file:line`, branch name, commit SHA, or section reference (be specific enough to navigate to)
   - **Issue** — what's wrong (concrete, not vague)
   - **Why it matters** — actual impact, not theoretical risk
   - **Fix** — specific suggestion, not "consider doing X"
4. **Cross-reference check (mandatory before proposing any fix)** — grep the entire codebase for references to whatever the fix touches: section numbers, file paths, helper command names, placeholder names, configuration keys. The fix must not leave dangling references, contradict text in other files, or miss a derivative location that needs the same change. State the cross-ref result inline with the fix (e.g., *"grep result: only the heading itself — rename safe"* or *"3 other locations reference this; will update all in the same fix"*). A fix proposed without this check is incomplete and tends to create the next audit's findings.
5. **Wait for user reaction** — they reply with one of: **fix** (apply the suggested fix), **defer** (note for later, don't fix now), **skip** (not a real issue, move on), **discuss** (engage further before deciding). **End every finding with the literal prompt `fix / defer / skip / discuss?` on its own line** — verbatim, not a paraphrase like "Vote?" or "What do you want to do?". The literal options remind the user of the available reactions without forcing them to remember.
6. After their reaction, move to the next finding. Repeat until all are addressed.

Don't batch findings into a single wall of text. Don't recommend without explaining. Don't proceed to the next finding before the user has reacted to the current one.

## Meta-discipline

These rules govern how all other discipline rules are written and applied. Violating a meta-rule erodes every rule downstream.

### Zero-escape-hatch policy

No discipline rule may contain an escape clause. Any rule with "OR", "if X except Y", "use judgment", "when reasonable", "unless trivial", or any equivalent slip-path creates a place I will drop in. When defining or revising a rule: name a single mandatory action, no carve-outs.

Past examples of escape hatches eliminated:
- "Use the claude-code-guide agent OR `docs.claude.com`" → "invoke the agent" (claimed-but-unperformed checks are unfalsifiable)
- "For non-trivial python edits, invoke reviewer" → "for ALL python edits, invoke reviewer" ("non-trivial" is a judgment call I've gotten wrong)
- "Trivial functions (≤5 lines, no branches, no I/O) may skip explicit test" → "every function gets a test" (the bugs we hit fit in <10 lines)

When proposing or revising any rule, apply this check: does the rule have an OR / if / except / unless / when-reasonable clause? If yes, the rule has an escape hatch — close it before adopting.

### Default-argue: engage critically with every request

For every non-trivial user request — code change, workflow decision, naming choice, scope question, design tradeoff, anything beyond a one-line trivial task — engage critically BEFORE or WHILE acting. The user shouldn't have to write the word "argue" to get pushback; it's the default behavior.

What "engage critically" means:
- Identify counter-arguments, simpler alternatives, edge cases the user may not have considered
- Surface conflicts with established patterns, principles, or earlier decisions
- Push back where you have substance; agree explicitly where the request is already correct
- Name tradeoffs (cost, complexity, blast radius) that affect the decision

What "sane" calibration means:
- **Don't manufacture pushback for trivial requests.** Typo fixes, repetitive operations, clearly-defined tasks with no architectural implication — execute, don't argue.
- **Don't argue past the substance.** One well-argued round (your view + reasoning + recommendation). If the user reaffirms, proceed. Multi-round bargaining is not engagement, it's friction.
- **Engagement is informational, not blocking.** Under auto mode, share your argument THEN proceed on what you think is right. The user redirects if they disagree. Engagement isn't an excuse to stall.
- **Agreement is fine.** If after thinking critically you genuinely agree, say so explicitly with the reasoning ("agreed because X") — don't fake disagreement to look engaged.

Failure modes this prevents:
- Rubber-stamping requests that have real tradeoffs (the user wanted critical engagement, got compliance)
- Drifting into anti-patterns because nobody argued for the principle that should have applied
- The user having to re-prompt with "argue" / "what are the tradeoffs" / "are you sure" to get the engagement that should have been default

### No-underspecification when delegating to agents

When invoking any agent (Task tool, subagent invocation, etc.), provide complete context the agent needs to succeed. The agent doesn't have your conversation history, your mental model of the architecture, or your knowledge of constraints — it sees only what you brief it with.

Before delegating, gather:
- The goal (what does success look like?)
- Integration context (where does this fit; what consumes its output)
- Constraints (what conventions / patterns must it follow; what existing code/files matter)
- Edge cases YOU know of (don't make the agent rediscover what you already know)
- Success criteria (how will the agent know it's done; what's the verification step)
- What NOT to do (out-of-scope changes, anti-patterns to avoid)

"Sane" calibration: match brief depth to task complexity. A 10-line helper doesn't need a 5000-word brief; a multi-file refactor needs one. But never under-deliver — agents diverging from intent because of a thin brief is the orchestrator's failure, not the agent's. If the agent surfaces a question or makes a wrong assumption, the brief was incomplete.

## Code & spec discipline

These rules apply to all framework work — not just audits. Audit findings are the consequence of violating these; following them prevents most findings from existing in the first place.

### Test-immediately-after-write for python helpers

Every python function in `scripts/lib/*.py` (or any helper script) must have a test written + actually run in the same turn as the function. No exceptions for size, complexity, "trivial" functions, or "covered by caller test" — every function gets its own test that runs. "I think this passes" is not verification. Tests must use input shapes matching what the function will receive in production — for parsers reading another tool's output, round-trip via the real producer (e.g., `configure_helper reset` + `set-*` verbs → `.devforge/configure.yaml` → `constitute_helper read-configure` parser; `tests/lib/test_constitute_helper.py` does exactly this), not hand-authored fixtures.

### Sentence-level hallucination check for spec docs

Every sentence in spec/instruction docs (`src/commands/*/main.md`, `src/commands/*/references/*.md`, `src/agents/*.md`, `CLAUDE.md`, etc.) must satisfy ONE of: (a) mechanically/definitionally true (restating an established convention), (b) verifiable right now against code/files, (c) explicit forward reference with the future state named (e.g., "Phase 4's `apply-agents` populates this — see §6.4"). Sentences that don't fit any category are hallucination risk. The most dangerous failure mode is sentences that USED to be true but became false after later edits — they look like ground truth and mislead future sessions.

### Cross-check after every change

After any edit (code, spec, doc — not just audit fixes), grep for affected identifiers, paths, section numbers, placeholder names, config keys, helper command names. Any dangling reference, contradiction in another file, or derivative location needing the same change is part of the SAME change to fix. Don't leave it for the next audit.

### Pre-empt future-session hallucination

Before declaring any change complete, ask: "What would a fresh future session falsely believe about this state?" Common seeds: removed function still referenced, renamed file still cited, deprecated pattern documented as current, partial migration mid-flight. Fix the inconsistency now — future sessions can't tell they're hallucinating because the contradictions look like ground truth.

### Verify Claude Code authoring conventions before writing commands/agents

For files that ship into a target project's `.claude/` directory or describe Claude Code integration (slash commands, agent files, command frontmatter), **invoke the `claude-code-guide` agent** to verify current authoring conventions. The agent is responsible for fetching from `docs.claude.com` and synthesizing the answer; relying on training-knowledge conventions is hallucination risk.

**Why agent-only, not "agent OR docs":** an OR clause creates an escape hatch — I can claim "I checked the docs" without an observable verification step. Agent invocation is a real tool call that leaves a trace; claimed-but-unperformed doc checks don't. Routing through the agent makes the verification observable and harder to skip silently.

Framework-internal conventions (per-project `.devforge/`, helper APIs like `configure_helper` / `constitute_helper`, etc.) live in this CLAUDE.md + spec files — those are framework-authoritative and don't need the agent. Claude-Code-integration conventions are external and require the agent per edit.
